done: v1.20.0 - Claude's in-place patch replaced with one that works. The mechanism already in the repo overwrote a "sentry-dbid-<uuid>" literal inside index.pre.js; tested against a copy of the live 38.9 MB archive, that literal does not exist in this build, so the install would have thrown. It also rejected the obvious alternative claiming Node cannot resolve ../ out of an asar via package.json main - tested, and that is not true: path.join normalises the .asar component away and the require works. Now `main` is rewritten in the archive's package.json to ../wintage-shim.cjs, space-padded to the identical byte length so every header offset stays valid, with the per-file integrity hash recomputed. The archive never moves, so Claude's resourcesPath helpers (MCP runtime, shell-path worker) keep working. Full cycle verified on a copy: size unchanged, package.json parses, integrity matches, shim hands off, revert byte-identical by md5.
remaining: T-065 YouTube Studio analytics bars; T-029 shadow-DOM live check.
awaiting: close Claude completely (check the tray) and run `desktop\install.ps1 -Target claude` - the launch test is the one thing nothing here can do, since Claude hosts this session. Undo is the same command with -Revert. For T-065: Studio > Analytics, devtools on one bar, send its class plus computed background-color and background-image.
