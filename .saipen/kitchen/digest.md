done: v1.18.1. Claude was opening its WEB build after patching because its main resolves the renderer via app.getAppPath(), which the archive relocation repointed at the shim folder - the local load failed and it fell back to claude.ai. The shim now restores getAppPath() to the archive before handing over, so it reports what an unpatched launch would. Installed in all three Electron targets, all repainted to golden.
remaining: T-058 - Claude ALSO hardcodes path.join(process.resourcesPath, "app.asar", ...) for its MCP runtime and shell-path worker. getAppPath cannot fix that: those features break silently while the archive is relocated. Real fix is to stop relocating for such apps - patch `main` INSIDE the archive to ../wintage-shim.cjs, space-padded to the identical byte length so no header rewrite is needed (legal now the integrity fuse is off). Needs Claude closed to install and launch-test.
awaiting: restart Claude and confirm the desktop UI loads (not claude.ai). If MCP servers or the shell PATH misbehave, that is T-058, and `desktop\install.ps1 -Target claude -Revert` puts everything back. T-029 (shadow-DOM check) still open too.
