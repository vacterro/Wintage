done: v1.21.0, an audit pass rather than new features - four bugs, three of them already shipped and one of them mine from the previous release. (1) The `Set-Content -Encoding UTF8` + `Get-Content` pair that mojibaked SAIPENVIEW was still writing five more targets: both wincmd.ini, WildRift's .py source, CodeNomad's css, BetterDiscord's css and Obsidian's appearance.json - and a BOM there makes JSON.parse throw. Found a real one already on disk in your parent vault and stripped it; everything now goes through one Read-Utf8/Write-Utf8 helper. (2) Total Commander was the only target editing a file you have owned for years and the only one with no backup - its revert deleted every colour key in the whole ini, including ones you set before Wintage existed. Backed up once before the first write now, with an honest partial fallback where no backup exists. (3) The GUI died outright on a BOM'd pack or on any pack predating a token (link was added 19th). (4) My own backdrop fix from v1.19.0 called getBoundingClientRect on every url() background - forced layout on hundreds of sprites, the exact thrash ADR-004/ADR-006 exist to prevent; now gated behind a zero-cost DOM-depth test.
remaining: T-065 YouTube Studio analytics bars; T-029 shadow-DOM live check.
awaiting: nothing blocking. When you get to it: close Claude and run `desktop\install.ps1 -Target claude` (v1.20.0's in-place patch, verified on a copy but never launched). For T-065: Studio > Analytics, devtools on one bar, send its class plus computed background-color and background-image.
