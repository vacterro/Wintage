# LOG

- 19.07.26 00:00 [E-001] [T-001] SCOUT: live-tested blanket animation-duration:0.001s on real qwen — rc-motion dropdown opens, hit-test itemClickable=true, 7 items, page responsive. Old "animations break rc-motion" fear = DISPROVEN. Snapback risk = only hits already-broken sites (no fill-mode reveal). Flood = Chromium coalesces to <=1/frame. Decision: restore animation taming, verified ground.
- 19.07.26 00:00 [E-002] [parent: E-001] [T-001] BUILD: added animation-duration/-delay:0.001s to motion rule GLOBAL_CSS+SHADOW_CSS, rewrote stale "animation NOT forced" comment. node --check PASS.
- 19.07.26 00:00 [E-003] [parent: E-002] [T-001] SHIP: release.ps1 minor bump -> v1.2.0, pushed origin/main 22a9bae.
- 19.07.26 00:00 [E-004] [parent: E-003] [T-001] VERIFY: published 1.2.0 fetched+injected on chat.qwen.ai -> rc-motion "+" dropdown itemClickable=true, 7 items; scan 1757 real els -> 0 hidden-by-snapback. GREEN. T-001 DONE.
- 26.07.26 14:09 [E-005] [T-none] H: user reports ~10% idle CPU + sites needing several reloads. Hypothesis: sweeper interleaves getComputedStyle with inline-style writes, so each write invalidates style and the next read forces a full recalc. Measured live on en.wikipedia.org/wiki/World_War_II (16921 els) with published v1.2.1 eval'd in-page: 2500 els read-only = 70.6ms; 2500 els read+inline-write interleaved = 1993.6ms (2nd run 1069.2ms); same 2500 batched read-then-write = 230.9ms, of which ~190ms is ONE whole-document recalc. VERDICT: CONFIRMED, 4.6-8x self-inflicted overhead.
- 26.07.26 14:09 [E-006] [parent: E-005] [T-none] RUN: instrumented setInterval(1500) wrapper measured one real sweeper tick = 253ms => 16.9% of a 1.5s tick. Light sweep on a settled page is cheap (qsa '*:not([data-w95-done])' = 0.7ms, 0 dirty), so the burn is entirely the unconditional force pass every 3rd tick. Boot (injectLate + runSweeper(true)) = one 716ms long task, plus another full force sweep 1s later.
- 26.07.26 14:09 [E-007] [parent: E-006] [T-none] DEC: no @noframes and no window.top guard anywhere in wintage.user.js (grepped) — with @match *://*/* @run-at document-start the whole engine (2 MutationObservers + 1500ms interval + force sweeps) runs once per iframe, multiplying the above by the frame count on ad-heavy pages. Ticketed T-002..T-005.
- 26.07.26 14:20 [E-008] [parent: E-007] [T-002] BUILD: v1.3.0 - process() now reads only and appends [el,prop,val] triples to a caller-owned flat queue, flushed once per sweep across all roots (T-002); force sweeps gated behind requestForceSweep(), fired by new DOM / changed stylesheet / tab return / 30s heartbeat, NOT by attribute churn (T-004); sub-frames get CSS + observer + 3 bounded settling sweeps but no interval, @noframes rejected as it would leave embeds unthemed white (T-003); mutation batches de-duplicated by skipping records whose ancestor is also in the batch, plus already-detached nodes (T-005). node --check PASS. ADR-002 written.
- 26.07.26 14:20 [E-009] [parent: E-008] [T-002] DEC: kept a 30s requestForceSweep() heartbeat. The old blind 4.5s force pass accidentally healed a case nothing else covers - a site mutating an existing element's own `style`/custom properties - and `style` can NEVER go in the observer's attributeFilter because setImp writes inline styles and the observer would fire on its own output and spin. Dropping the blind pass outright would have been a real regression, so it survives 20x rarer (~0.7% averaged vs the measured 16.9%).
- 26.07.26 14:20 [E-010] [parent: E-009] [T-002] H: does batching change pixels? Inherited `color` means a child now reads its parent's ORIGINAL color instead of the just-corrected one. Test: snapshot color+background-color+border-top-color of every 17th element (996 samples) on the same wikipedia page, v1.2.1 vs v1.3.0, across a reload via sessionStorage. VERDICT: 0 differences. Batching is pixel-identical; the only effect is a redundant explicit declaration on descendants.
- 26.07.26 14:20 [E-011] [parent: E-010] [T-002] VERIFY: same page/method, v1.3.0 (d489eb5) fetched from perf-verify branch and eval'd on a fresh load. boot 716ms -> 327ms. 21 ticks over ~31s: light 0.8-1.3ms, force 19-42ms, avg 19.2ms = 1.28% CPU while settling; once settled, ticks are 1.3ms and 0 long tasks = 0.09%. done=16921/16921 (full coverage kept). 37 light backgrounds remain, ALL of them IMG (deliberate TAG_SKIP). GREEN.
- 26.07.26 14:25 [E-012] [parent: E-011] [T-none] SHIP: v1.3.0 merged to main and pushed, 19a534c..6cceff3. Local scratch branch perf-verify NOT deleted (squash-merge, so git reports it unmerged and -D is a destructive op needing explicit confirmation).
- 26.07.26 14:26 [E-013] [parent: E-012] [T-006] DEC: read UI.md, opened a goal wave T-006..T-012 for conformance. Gap list: 1px+box-shadow bevels break laws 2+3; no zero-shadow/blur/gradient layer; no type ladder; #9DD9F9 link colour traces to NO token (iron law 5); accentTeal/success/warning/danger/selection/compareBack tokens absent; disabled buttons flatten their bevel; scrollbars built from inset shadows.
- 26.07.26 14:30 [E-014] [parent: E-013] [T-006] RUN: careless `git checkout main -- wintage.user.js` while juggling branches DESTROYED the uncommitted v1.4.0 CSS work. Nothing was staged so it was unrecoverable from git. Rebuilt from the same v1.3.0 base by re-applying the identical edit sequence. Lesson: commit the candidate BEFORE any branch operation, never mid-edit.
- 26.07.26 14:35 [E-015] [parent: E-014] [T-007] BUILD: v1.4.0 - 2px 4-value bevels per UI.md's literal CSS (B_SUNK collapsed into B_INNER); global box-shadow/text-shadow/backdrop-filter none, filter none except media; type ladder 12/14/16/10 + line-height 1.2 + two font weights; full token block, T object the only colour source; links --borderHighlight (7.8:1) not off-palette #9DD9F9; disabled keeps raised bevel + --textMuted label, opacity banned; th/dialogs raised; scrollbars rebuilt from borders; inputs 20px/compareBack/sunken, textarea 64px; ALL gradient hues killed in JS (progress/meter/slider exempt); saturated backgrounds snap to --success/--warning/--danger by hue sector. node --check + off-palette grep PASS.
- 26.07.26 14:38 [E-016] [parent: E-015] [T-009] H: first live measurement showed h1/h2 at 12px and font-weight 700 nowhere. Cause: the base type selector's six :not([class*="…" i]) attribute matches score (0,6,4), so a plain `h1 {font-size:16px}` at (0,0,1) loses even with !important on both sides; `*:not(svg):not(path)` (0,0,2) likewise beat a bare `b` (0,0,1). Fixed by making the size rules DISJOINT (exceptions carved out of the base selector) and prefixing the weight/style exceptions with :root / :host. VERDICT: CONFIRMED and fixed.
- 26.07.26 14:40 [E-017] [parent: E-016] [T-009] H: second measurement showed the ladder not applying AT ALL (p=16px, 14.4px on 6232 elements). Cause: an explanatory paragraph was pasted AFTER a comment's closing */, leaving loose prose plus a stray */ inside GLOBAL_CSS. `node --check` PASSED - a template literal is just a string to JS - and the browser's CSS parser silently discarded rules while recovering. Zero errors anywhere; only computed-style measurement on a live page exposed it. VERDICT: CONFIRMED.
- 26.07.26 14:41 [E-018] [parent: E-017] [T-009] BUILD: added tools/check-css.js (stray/unclosed/nested CSS comments, brace balance, off-palette hex) and gated release.ps1 on it. Guard validated by injecting all 3 fault classes into a copy: stray */ CAUGHT, off-palette #ff00ff CAUGHT, brace imbalance CAUGHT; file restored, both checks PASS.
- 26.07.26 14:43 [E-019] [parent: E-018] [T-012] VERIFY: v1.4.0 (7e53dcd) live on en.wikipedia.org WWII, 16921 elements. Font sizes present: 10/12/14/16px and NOTHING else. Weights: 400 and 700 only. h1=16px h2=14px p=12px b=700. Off-palette backgrounds 0, off-palette text colours 0, box-shadows 0, rounded corners 0, gradients 0. Idle CPU settled = 2.1ms/tick = 0.14% (v1.3.0 0.09%, pre-fix 16.9%); the 5.13% seen earlier is the one-off settling burst at load, not steady state. GREEN.
- 26.07.26 14:43 [E-020] [parent: E-019] [T-012] DEC: ADR-003 records what was applied literally and what was NOT, with reasons - transition/animation stay 0.001s (ADR-001 live evidence beats the literal law), no global margin:0 (would break UI.md's own legibility principles), no global box-sizing (already reverted once for flex breaks), no body overflow-x:hidden (hides real content on pages we did not lay out), no class-name status colours, semantic tokens never used as text colour (1.8:1 fails AA).
- 26.07.26 14:50 [E-021] [parent: E-020] [T-012] RUN: squash-merge of perf-verify into main conflicted on all three .saipen files (main already carried the v1.3.0 checkpoint) AND on wintage.user.js; the commit aborted on unmerged paths and the push was a no-op. Resolved with `git checkout --theirs` on the four files (perf-verify is the strict superset), verified 0 conflict markers remain and both checks still pass, then committed.
- 26.07.26 14:52 [E-022] [parent: E-021] [T-012] SHIP: v1.4.0 pushed to origin/main 6cceff3..4bcec5a. Re-verified the PUBLISHED commit by fetching 4bcec5a into a fresh wikipedia load: version 1.4.0, sizes {10:545, 12:14586, 14:39, 16:1}, weights {400:15003, 700:168}, off-palette bg 0, off-palette text 0, box-shadows 0, rounded corners 0, gradients 0, button bevel = 2px solid #C0A060 top / #0E0803 bottom with box-shadow none, link #C0A060, html bg #1A0F05. GREEN.
- 26.07.26 14:54 [E-023] [parent: E-022] [T-013] H: does animation-duration 0.001s actually STOP an infinite animation? Measured via Web Animations API (exact, no compositing needed): duration 1ms + iterations Infinity gives currentIteration 16 after ONE 60fps frame and 1000 after one second, vs 0 and 1 for the intended 1s. Rendered angle over 6 consecutive frames: 240,120,0,240,120,0 deg - 16.667ms/1ms leaves a repeating 2/3 remainder so a spinner STROBES between three rotations forever, it does not freeze. ADR-001 checked only that events do not flood and called it fine. VERDICT: CONFIRMED, theme's "zero animations" claim was false for infinite.
- 26.07.26 14:54 [E-024] [parent: E-023] [T-013] RUN: BUILD+VERIFY v1.4.1 pauses infinite animations (animation-play-state:paused) from the JS repainter, keeping 0.001s for finite ones. Safe because an infinite animation's animationend NEVER fires, so the animationend-driven state machines that motivated 0.001s cannot depend on one. Check runs BEFORE shouldSkip - the two commonest spinner shapes are both in the skip set (svg.animate-spin via TAG_SKIP, spinner inside a loading <button> via closest('button')). Live: infinite div/svg/in-button all paused, FINITE animation still running. GREEN. Pushed 147e906. ADR-004.
- 27.07.26 02:16 [E-025] [parent: E-024] [T-014] DEC: user reworked the scheduler during a session cutoff - replaced the 1.5s setInterval + 30s heartbeat with a self-rescheduling one-shot chain that doubles its delay to 60s when idle, added 'style' to attributeFilter with a 100ms self-mute, and added a capture-phase LINK load listener. Direction is right (zero standing intervals: grep confirms no setInterval left in the file). Reviewed for correctness rather than accepted as-is.
- 27.07.26 02:16 [E-026] [parent: E-025] [T-014] H: is the 100ms self-mute sound? Measured on a static article (16595 els, 12s window): site alone = 0 style mutation records, with theme = 9466. All self-inflicted. Mute discards them but only AFTER allocation + microtask delivery + pendingMuts growth, and the filter runs at the END of the 60ms debounce - any flush landing >100ms before its debounce fires (exactly when the main thread is busy during a heavy sweep) leaks our own writes into re-processing. VERDICT: CONFIRMED unsafe, race not suppression.
- 27.07.26 02:16 [E-027] [parent: E-026] [T-014] BUILD: v1.4.2 replaces the time window with identity-based suppression - flushWrites records the exact elements it wrote, then takeRecords() drains both observers BEFORE any callback runs, dropping only style records on those elements and pushing everything else back into pendingMuts (re-arming the debounce if needed).
- 27.07.26 02:16 [E-028] [parent: E-027] [T-014] VERIFY: v1.4.2 (c50f624) live, 13075 els + 200 planted shadow roots. Self-inflicted style records 9466 -> 0. Backoff ladder now observed reaching 1500/3000/6000/12000/24000 -> 60000 (it was never broken; forcePassesOwed=7 pins the cadence ~31s and the earlier 12s window ended inside that drain). 200 pierced shadow roots: light sweep 1.0-1.5ms, same as with none. Regression test for throwing away FOREIGN records: inline background-color+color #ffffff !important written onto a live element -> theme repainted to rgb(30,20,8) and rgb(212,184,122). GREEN.
- 27.07.26 02:16 [E-029] [parent: E-028] [T-014] DEC: measured and deliberately did NOT change two things - draining the force debt every tick instead of every 3rd would cut the elevated window from ~31s to ~10s but turns a sustained 0.9% into 2.7% on churny pages (current cost is ~1% for 31s then ~0); and dirtyRoots gating for shadow-root sweeps, rejected because 200 roots cost the same as none. Recorded in ADR-005 so neither gets "optimised" later without a measurement.
- 27.07.26 02:18 [E-030] [parent: E-029] [T-014] SHIP: v1.4.2 merged to main and pushed, 147e906..a2676a1 (clean squash, no conflicts). ADR-004 + ADR-005 committed.
- 27.07.26 07:39 [E-031] [parent: E-030] [T-015] H: user reports ~8% idle CPU on a real chatgpt.com conversation vs 0.14% measured on wikipedia. Ran four hypotheses, THREE were wrong: (a) universal :not([class*=" i]) selectors slow on long Tailwind class strings - benchmarked 88ms current vs 91ms attribute-flag rewrite vs 79.7ms no-exclusions, ~10% not 100x; (b) our CSS makes recalc expensive - disabling both style[data-w95] sheets in place changed NOTHING, 13532ms long tasks with CSS on vs 13525ms with CSS off; (c) :hover rule surgery re-walking sheets - full walk of 20945 rules measured 22ms, sheets stable. (d) sweeper thrashing - data-w95-done on 3060/3062, 0 removals in 15s. Clean control: script fully disabled = 2517ms long tasks, enabled = 13532ms. VERDICT: our JS, none of the usual suspects.
- 27.07.26 07:39 [E-032] [parent: E-031] [T-015] H: counted the primitives directly by wrapping them on the prototypes from the console. chatgpt.com, 3392 els, 15s: querySelectorAll 151 (=~10 sweeps per SECOND), getComputedStyle 42563, closest 80114, setAttribute 43080, long tasks 14158ms of 15000. Cause: requestForceSweep() ended in scheduleNextSweep(true) - a 0ms timer - and it is called from the mutation handler on every batch with added nodes, so a React app inserting nodes queued back-to-back full sweeps forever. VERDICT: CONFIRMED. Reproduced locally at 148 sweeps/15s by driving wikipedia with a 100ms DOM-insertion loop.
- 27.07.26 07:39 [E-033] [parent: E-032] [T-015] RUN: BUILD+VERIFY v1.4.3 adds MIN_SWEEP_GAP=1000ms between end-of-sweep and start-of-next, forbids any 0ms schedule, and never replaces a pending timer that fires sooner. Same synthetic churn before/after: sweeps 148 -> 11, getComputedStyle 135823 -> 20844, closest 388194 -> 59444, setProperty 68388 -> 11730. GREEN.
- 27.07.26 07:39 [E-034] [parent: E-033] [T-016] H: cross-site control found UI.md violations CSS cannot fix. stackoverflow.com: a.bar-sm kept border-radius 4px, h1.fs-headline1 kept 27px, #onetrust-banner-sdk kept a box-shadow. Cause: our universal rules score (0,0,0), a site !important with any specificity beats them, and an ID rule at (1,0,0) cannot be outranked by any number of :root prefixes. VERDICT: CONFIRMED - only inline !important (setImp) outranks author rules unconditionally.
- 27.07.26 07:39 [E-035] [parent: E-034] [T-016] BUILD: border-radius:0, box-shadow:none and the type ladder are now re-asserted from process() when the computed value disagrees (nearly free - cs already resolved, write skipped on 3348 of 3362 els). Plus a palette clamp for control subtrees: #onetrust-consent-sdk #onetrust-accept-btn-handler (2,0,0) and #onetrust-banner-sdk * (1,0,0) beat our button rule, leaving near-black text on near-black inside the banner; skipped elements are now clamped but ONLY when provably off-palette.
- 27.07.26 07:39 [E-036] [parent: E-035] [T-016] BUILD: closed two dark-surface gaps found on amazon.com - #nav-belt #131921 (grayish, lum 0.0094) sat under the old 0.015 near-black floor, and #nav-main #232f3e (spread 27) fell through BOTH branches, over the grayish cutoff of 24 and under the saturated cutoff of 60. Neutral band widened to spread<=60, branches merged, floor dropped to 0.004 (true black still exempt). Also: link descendants now take the link colour when merely OFF-PALETTE, not only when unreadable - span#nav-cart-count kept #f08804 and span.navFooterDescText kept #999999 at 7.1:1 and 6.3:1, legible but not on-palette.
- 27.07.26 07:39 [E-037] [parent: E-036] [T-016] H: did widening the dark band make the repainter re-grade its OWN output? Live on wikipedia: body went #1E1408 -> #2A1C0A. --backgroundSoft (lum 0.0088) and --surfaceRaised (lum 0.0234) both land in the "<0.05" bucket, and --textSecondary (spread 88) would be flattened to --textPrimary by the not-grayish branch. VERDICT: CONFIRMED regression, self-inflicted. Fixed by returning early when the computed colour is already a palette token. Re-verified: 6 elements snapshotted, 8s wait, re-snapshotted -> ZERO drift.
- 27.07.26 07:39 [E-038] [parent: E-037] [T-016] VERIFY: cross-site control on v1.4.3 (4da2e21). stackoverflow.com 3360 els / amazon.com 8363 els / en.wikipedia.org 16921 els -> rounded 0, box-shadows 0, gradients 0, bad font sizes 0, off-palette bg 0, off-palette text 0, low-contrast text 0, JS errors 0 on all three. Long-task ms 253 / 589 / 546. Wikipedia sizes: 10px x545, 12px x14586, 14px x39, 16px x1. GREEN. ADR-006 written.
- 27.07.26 07:45 [E-039] [parent: E-038] [T-016] SHIP: v1.4.3 squashed into one release commit and pushed, 373f7ad..6390403. Re-verified the PUBLISHED commit on stackoverflow.com: version 1.4.3, 3363 els, querySelectorAll 13 calls in 14s (floor holding), long tasks 266ms, rounded 0, box-shadows 0, gradients 0, bad font sizes 0, off-palette bg 0, off-palette text 0, JS errors 0. GREEN.
- 27.07.26 17:10 [E-040] [T-none] RUN: validate -> PASS (4 structural fixes: dates w/o time, parent/ticket order, BUILD+VERIFY taxonomy). 18 taxonomy warnings on old history left as-is.
- 28.07.26 06:41 [E-041] [parent: E-040] [T-none] DEC: RECOVER before anything else (RFC 1.11 prio 1) - STATE carried `next_action: "HUNT — validate + ship done..."`, which matches none of the five legal prefixes, so a cold agent had nothing executable. Rebuilt during this checkpoint; no other field was stale (phase DONE + empty DOING/TODO agreed with BOARD, LOG tail E-040 agreed with both).
- 28.07.26 06:41 [E-042] [parent: E-041] [T-none] DEC: goal pivot - objective "five additional colour themes: Antigravity, Claude Code, K-Lite media player, FreeBuff, NomadCode". goal_mode true, goal_waves 0, goal_tickets 0. Nothing demoted: TODO was already empty, no DOING ticket to checkpoint.
- 28.07.26 06:41 [E-043] [parent: E-042] [T-none] DEC: PLAN wave 1 = 10 tickets T-017..T-026. Shape forced by the code as it stands: every CSS rule already interpolates `${T.*}`, so a palette swap is nearly free at the CSS layer - but three things are hardcoded to the golden palette outside it and each is its own ticket. (1) the pre-token literal hex at wintage.user.js:44-45 that paints before `T` exists; (2) tools/check-css.js holds its own frozen 17-colour PALETTE set, so any second palette FAILs the release gate by construction; (3) the repainter's luminance heuristics are tuned to a dark surface - near-black floor 0.004, neutral spread<=60, darkBg 0.008 for the contrast test, and the "already a palette token" early return added at E-037 - all of which mis-grade a light palette. Also decided: the switch is GM_registerMenuCommand + GM_getValue, not a source-edit constant, which means moving off `@grant none`; script touches DOM only, no page globals, so the sandbox move is safe.
- 28.07.26 06:41 [E-044] [parent: E-043] [T-none] DEC: goal_waves 0->1
- 28.07.26 06:48 [E-045] [parent: E-044] [T-017] RUN: SCOUT -- the palette is already better factored than expected. All 60-odd colour uses in GLOBAL_CSS/SHADOW_CSS are `${T.x}` interpolations, and PALETTE_RGB at :799 already derives itself from `Object.keys(T)`, so a swap of the `T` object propagates everywhere except three places: the literal hex at :44-45 (fixable by declaring the token table above the early paint instead of below it -- nothing between the EXCLUDE guard and that paint reads `T`), semanticToken()/the repainter constants (T-019's job, not this ticket's), and tools/check-css.js's frozen 17-colour PALETTE set. DEC on the gate's new shape: since check-css.js reads the file as TEXT, every `${T.x}` is not a hex to it -- so any hex it finds inside a CSS literal is by definition a hardcoded one, and the correct rule is ZERO literal hex in the CSS bodies rather than a membership test against one palette. Palette validity moves to a separate check over the THEMES table itself (every theme carries the full 17-token set, every value a well-formed 6-digit hex). Strictly stronger than what it replaces and it does not weaken as themes are added.
- 28.07.26 07:05 [E-046] [parent: E-045] [T-017] RUN: BUILD -- THEMES registry (slug -> {label, tokens}) declared ABOVE the first paint; `T = THEMES[THEME_ID].tokens`; the first paint now emits T.background/T.textPrimary instead of '#1A0F05'/'#D4B87A' and stamps data-w95-theme. check-css.js: the frozen 18-colour PALETTE set is gone, replaced by "zero literal hex inside a CSS body" (this script reads the file as text, so an interpolation is not a hex to it -- any hex it finds there is hardcoded by definition) plus a per-theme table check for the complete 18-token set, well-formed 6-digit values, a label, and no unknown keys.
- 28.07.26 07:05 [E-047] [parent: E-046] [T-017] H: does the golden render survive the refactor byte-for-byte? Checked offline instead of by eye, which also makes it repeatable: both CSS template bodies diffed against `git show HEAD:wintage.user.js`, all 18 token values compared old vs new, and the two changed emitters (the pre-token paint) resolved and compared against the literals they replaced. GLOBAL_CSS identical, SHADOW_CSS identical, 18/18 tokens identical, paint resolves to the same #1A0F05 / #D4B87A. VERDICT: unchanged, no browser round trip needed.
- 28.07.26 07:05 [E-048] [parent: E-047] [T-017] H: does the rewritten gate actually reject what it claims to? Injected four fault classes into throwaway copies -- hardcoded hex in a CSS body, a deleted token, an unknown token, a non-hex value. All four exit 1 with the right message. VERDICT: CONFIRMED (an unvalidated validator is just a comment).
- 28.07.26 07:06 [E-049] [parent: E-048] [T-017] DEC: REVIEW found one P0 in my own new code and it is worth recording, because the bug was in the validator rather than the product. checkThemes() listed themes with /(\w+):\s*\{\s*\n\s*label:/ -- so a theme that forgot `label` did not match, was never listed, and none of the token checks ran on it: the gate would report PASS on exactly the malformed entry it exists to catch. A validator must never decide WHAT to validate by looking for well-formedness. Rewritten as a brace-depth walk over the table's top level; verified by deleting the golden label -> "FAIL: theme golden: missing label", exit 1. Verdict: SHIP after FIXES, fix applied and re-verified in the same pass.
- 28.07.26 07:06 [E-050] [parent: E-049] [T-017] DEC: goal_tickets 0->1
- 28.07.26 07:12 [E-051] [parent: E-050] [T-017] RUN: ship v1.5.0 -> pushed 6f68019..d539ff2 (release.ps1: node --check PASS, check-css PASS on GLOBAL_CSS/SHADOW_CSS/theme golden). No tag -- this repo has never tagged, and starting now would leave every earlier release looking untagged by comparison; @version + the commit are the release identity here.
- 28.07.26 07:13 [E-052] [parent: E-051] [T-none] DEC: found shipping v1.5.0 -- release.ps1 bumps ONLY the `// @version` header line, but the file also carries `const W95_VERSION` (added in v1.4.4 precisely so a console diagnostic can confirm which build is live). It now reads '1.4.7' on a 1.5.0 build. A version stamp that lies is worse than none: the one measurement it exists to protect -- "am I looking at a stale install?" -- silently answers wrong. Ticketed T-028 rather than fixed inline; it is a release-tooling bug, not part of the theme work in flight.
- 28.07.26 07:20 [E-053] [parent: E-052] [T-018] RUN: SCOUT -- the switch is not the hard part, the sandbox is. wintage.user.js:635 overrides `Element.prototype.attachShadow` to seed SHADOW_CSS into every shadow root a site opens, and that only works from the PAGE context: adding a `@grant` moves a Tampermonkey script into an isolated world, where `Element.prototype` is a different object and the interception silently themes nothing while every other feature keeps working. That is why the file has carried `@grant none` from the start. DEC: `@grant GM_getValue/GM_setValue/GM_registerMenuCommand` PLUS `@sandbox raw` -- Tampermonkey's documented "page context, GM API still injected" mode, which is the one combination that keeps both. Every GM call is typeof-guarded and falls back to the default palette, so a manager without the API degrades to today's behaviour instead of throwing at document-start.
- 28.07.26 07:34 [E-054] [parent: E-053] [T-018] RUN: BUILD -- header moved to @grant GM_getValue/GM_setValue/GM_registerMenuCommand + @sandbox raw; THEME_ID resolved from GM storage (key 'w95-theme') with a total fallback chain (no API -> default, unknown slug -> default, throw -> default), since this runs at document-start and an exception there means the page paints unthemed white. Menu registers one entry per theme, top frame ONLY (the script runs in every frame, so per-frame registration would stack a duplicate entry per ad iframe). Switching reloads: the repainter has already written thousands of inline !important values keyed to the old tokens and there is no correct cheap way to unwind them.
- 28.07.26 07:35 [E-055] [parent: E-054] [T-018] H: do the fallback paths actually hold? None of them is exercised by opening a page in a healthy browser, so they were tested by running the REAL source slice (THEMES table through the menu block) under stubbed globals in node:vm -- shipped text, not a paraphrase. 22 checks: no-GM -> golden + no menu, unknown slug -> golden, valid slug honoured AND followed by the first paint, data-w95-theme stamped, one menu entry per theme with the active one marked, sub-frame registers nothing, clicking the active entry is a no-op, clicking another persists then reloads, and a THROWING GM_setValue must not reload (a reload without a stored value loops straight back onto the same theme). All 22 PASS. Promoted to tools/test-theme-switch.js and gated into release.ps1. VERDICT: CONFIRMED for everything measurable offline.
- 28.07.26 07:36 [E-056] [parent: E-055] [T-018] DEC: one half of this ticket is NOT verifiable from here and saying so beats a green checkbox. `@sandbox raw` is the claim that the script keeps running in page context, which is what the attachShadow interception at :635 needs; nothing in node can observe that, it needs a live Tampermonkey. The failure mode if the claim is wrong is quiet -- shadow roots simply stop being themed while everything else looks fine -- so it gets its own ticket (T-029) with a concrete console check rather than being folded into this one's verdict. Everything this ticket could prove offline is proven; the sandbox claim is stated as unproven.
- 28.07.26 07:36 [E-057] [parent: E-056] [T-018] DEC: goal_tickets 1->2
- 28.07.26 07:42 [E-058] [parent: E-057] [T-018] RUN: ship v1.6.0 -> pushed d539ff2..1a98fec. Note for later: release.ps1 aborted mid-run the first time because the invocation piped it through `2>&1`, which turns git's harmless "LF will be replaced by CRLF" stderr warnings into terminating PowerShell error records. The script is fine; the pipe was the bug. Version was already bumped, so the commit + push were completed by hand from the same staged tree.
- 28.07.26 08:05 [E-059] [parent: E-058] [T-019] RUN: SCOUT -- found seven places pinned to the golden palette, not the two the ticket guessed: the background bucket thresholds (0.45 flashbang / 0.004 floor / 0.13 / 0.05), `const darkBg = 0.008` in the text branch, the text buckets (fgLum 0.4 / 0.15), the border rule `lum(bc) > 0.18`, `color-scheme: dark` in GLOBAL_CSS, and the `data-w95-dark` attribute. All of them encode "our theme is dark, so BRIGHT means the site is shouting".
- 28.07.26 08:06 [E-060] [parent: E-059] [T-019] DEC: normalise the input instead of duplicating the decision tree per polarity. `elev(L) = DARK ? L : 1 - L` answers "how far is this colour from MY background, in the direction that reads as raised on MY theme", and every existing threshold keeps its number against that. On a dark palette elev() is the identity function, which makes the no-op on golden provable rather than argued -- verified across all 256 grays. Rejected the alternative (a second set of light-theme constants): two trees drift, and the golden one is the tuned one, carrying live measurements from three sites in its comments.
- 28.07.26 08:07 [E-061] [parent: E-060] [T-019] RUN: BUILD -- polarity block (BG_LUM/BG_SOFT_LUM/DARK/elev + hexLum/contrast helpers) hoisted ABOVE the first paint, because both the paint and GLOBAL_CSS now read DARK and const has no hoisting; lum() moved with it and is a function declaration, so its later callers are unaffected. Text contrast now measured against the theme's own backgroundSoft instead of the 0.008 constant -- on a light palette that constant claimed every dark text colour was perfectly readable, disabling the 4.5:1 rescue exactly where it matters most. check-css.js gained a per-theme WCAG AA floor on the three tokens that carry text (textPrimary/textSecondary/borderHighlight vs backgroundSoft); textMuted 3.34 and accentTeal 3.80 are deliberately exempt as non-text roles.
- 28.07.26 08:08 [E-062] [parent: E-061] [T-019] H: does golden survive, and does a light palette actually invert? tools/test-repainter-polarity.js pins 21 decisions against literal expectations. Three of my own expectations were wrong and the CODE was right, which is worth recording: #2A2A2A lands on --surface not --surfaceRaised (lum 0.024, under the 0.05 step); #999999 as plain text is 6.25:1 = legible, so it takes --textSecondary rather than being rescued as unreadable (the amazon case that forced iron law 5 was that colour inside a LINK, a different branch); and pure white on a light theme is exempt at elev 0, the exact mirror of true black on a dark theme, while #FDFDFD is already over the floor and does get graded. Final: 21/21 PASS. AA gate separately proven by forcing textPrimary to #6A5A38 -> "measures 2.70:1, under the WCAG AA 4.5:1 floor", exit 1. Both tests gated into release.ps1.
- 28.07.26 08:08 [E-063] [parent: E-062] [T-019] DEC: goal_tickets 2->3
- 28.07.26 08:15 [E-064] [parent: E-063] [T-019] RUN: ship v1.7.0 -> pushed 1a98fec..28c5f48 (all three gates green: check-css, theme-switch 22/22, polarity 23/23).
- 28.07.26 08:30 [E-065] [parent: E-064] [T-027] RUN: BUILD -- themes/<slug>.json is now the palette source; tools/apply-themes.js generates the THEMES block into any wintage.user.js; install-themes.ps1 wraps it for humans. The design point that matters: the generator locates its target TWO ways -- between its own markers if they exist, otherwise by brace-walking the `const THEMES = {` declaration and adding the markers around the replacement. Only the second path makes it work on a file this tool has never seen, which is the entire use case: Tampermonkey re-downloads the script on every upgrade, so a palette edited in by hand is gone the next time upstream ships. Generating golden from its own pack reproduced the existing block byte-for-byte (the diff is the two marker lines and nothing else), which is the strongest evidence available that the renderer is faithful.
- 28.07.26 08:31 [E-066] [parent: E-065] [T-027] H: does it survive the cases that actually break generators? tools/test-theme-packs.js, 18 checks. The load-bearing one runs the generator against `git show HEAD:wintage.user.js` -- the last RELEASED commit, which predates the markers entirely -- and asserts it gains markers, still parses under node --check, and is byte-identical on a second run. Plus: a non-Wintage target is refused with a reason and left untouched; a half-marked file (opening marker, no closing) refuses to guess where the block ends rather than eating the rest of the file; six malformed-pack classes each fail with their own message (missing token, non-hex value, unknown token, non-identifier slug, apostrophe in a label that is emitted inside single quotes, invalid JSON); and menu order comes from the pack's `order` field rather than from filename sort, verified by naming files aaa/zzz against slugs zeta/alpha. 18/18 PASS. Gated into release.ps1 alongside an `--check` drift guard, so a release can never ship a script whose block wandered from themes/.
- 28.07.26 08:31 [E-067] [parent: E-066] [T-027] DEC: goal_tickets 3->4
- 28.07.26 08:50 [E-068] [parent: E-067] [T-020] RUN: BUILD+VERIFY themes/claudecode.json -- Anthropic's terminal palette: #1F1E1D / #262624 surfaces, cream #FAF9F5 text, coral #D97757 as the link/highlight. Measured on its own backdrop: primary 14.39:1, secondary 8.31:1, link 4.86:1 -- the coral clears AA by 0.36, which is thin but real, and it is the colour that makes the theme recognisable. accentTeal is repurposed as a muted slate blue since Claude's palette has no teal.
- 28.07.26 08:51 [E-069] [parent: E-068] [T-021] RUN: BUILD+VERIFY themes/antigravity.json -- #131314 / #1B1B1F surfaces on Google's Material dark greys, #E3E3E3 text, blue-300 #8AB4F8 as the link. primary 13.38:1, secondary 9.49:1, link 8.15:1. Semantic trio taken from the same Material set (#81C995 / #FDD663 / #F28B82) rather than invented.
- 28.07.26 08:52 [E-070] [parent: E-069] [T-020] H: two tests failed on the first run with three themes and BOTH were the test's fault, not the product's -- worth recording because each was a test that would have quietly stopped covering anything. test-theme-switch hardcoded "2 menu entries" and indexed the injected palette at [1]; now it counts from the declared table and finds the entry by label. test-theme-packs built its marker-less fixture from `git show HEAD` -- which stopped being marker-less the moment v1.8.0 shipped, so it would have kept passing while testing the easy path; the fixture is now built by stripping the markers from the current file. VERDICT: product green, 18/18 + 22/22 + 23/23 after the test fixes.
- 28.07.26 08:53 [E-071] [parent: E-070] [T-none] DEC: found while designing the K-Lite palette, and it blocks every LIGHT theme: `borderHighlight` is doing two incompatible jobs. It is the bevel's light edge (B_OUTER paints it top/left) AND the link colour, and on a dark palette one value serves both -- #C0A060 is a plausible Win95 highlight and a 7.28:1 link. On a light palette the bevel highlight has to be near-white to read as raised, and near-white fails AA as link text by a mile. Not fixable inside a palette; the token has to split into `borderHighlight` (bevel) and a new `link` token, which touches GLOBAL_CSS, SHADOW_CSS, the repainter's link branch, the pack schema and both gates. Ticketed T-030, and T-022 (K-Lite, the first light palette) now needs it.
- 28.07.26 08:53 [E-072] [parent: E-071] [T-none] DEC: goal_tickets 4->6
- 28.07.26 08:58 [E-073] [parent: E-072] [T-021] RUN: ship v1.9.0 -> pushed 065b59b..25a6b02 (5 gates green).
- 28.07.26 09:30 [E-074] [parent: E-073] [T-023] DEC: user's call, and it changes the whole approach -- every theme is UI.md, not a reproduction of the product's own UI. So a palette is golden's STRUCTURE (near-black ground, three surface steps, ~56% highlight, the 9.4/6.3/3.3 text ladder, desaturated semantics) rotated to another hue family. tools/derive-palette.js does exactly that: converts each golden token to HSL, preserves its offset from golden's 30 degree base, retints, writes the pack. accentTeal/accentTealDeep and success/warning/danger are NEVER rotated -- a green that means "ok" must not become a brand colour, and a status that changes hue per theme stops reading as a status.
- 28.07.26 09:31 [E-075] [parent: E-074] [T-023] RUN: hues read off the installed apps rather than recalled, per user instruction. Antigravity: extensions/antigravity/tailwind.config.js -> brand-dark #09b6a2 teal on a cool gray ladder topped by #2D3142 (NOT the Google blue I would have guessed). FreeBuff: strings in app.asar -> #0C0D0F ground, #1F2937 slate, lime #7CFF3F / #22C55E. NomadCode: the Electron cache under AppData/@neuralnomads -> #0E1116 ground, #24292E/#2F363D surfaces, indigo #624AFF / #615CED, #E1E4E8 text (the cache also contained OUR golden tokens from pages it had rendered -- filtered out, they are not its palette). K-Lite: HKCU\Software\MPC-HC\MPC-HC\Settings MPCTheme=1 confirms the dark UI is active and MPC's dark chrome is deliberately neutral, so it is graphite + silver rather than a hue. Each pack records its own provenance in a `source` field.
- 28.07.26 09:32 [E-076] [parent: E-075] [T-024] H: does rotating a hue preserve contrast? NO, and the gate caught it: at golden's lightness NomadCode's indigo landed at 4.02:1 and its secondary at 3.51:1, both under AA, while the amber they were derived from sits at 7.28. Fixed by lifting L on the three text-carrying tokens until they clear 4.6:1 -- borderHighlight 56%->60%, textSecondary 52%->59%, reported per lift, never silent. Final measured ladder (primary/secondary/link vs each theme's own backgroundSoft): golden 9.44/6.29/7.28, antigravity 10.64/7.16/8.67, freebuff 10.49/7.06/8.40, claudecode 8.18/5.34/6.13, klite 7.56/4.84/5.67, nomadcode 5.65/4.72/4.77. All six PASS. VERDICT: structure preserved, floor held.
- 28.07.26 09:33 [E-077] [parent: E-076] [T-024] DEC: goal_tickets 6->9
- 28.07.26 09:36 [E-078] [parent: E-077] [T-024] RUN: ship v1.10.0 -> pushed 25a6b02..01daeee. All six palettes live, six gates green (derive --check, apply --check, check-css, theme-switch, polarity, packs).
- 28.07.26 09:55 [E-079] [parent: E-078] [T-none] DEC: user clarified the objective and it is a different one than I built for. The five names are TARGETS, not colour inspirations -- he wants the Wintage look applied to those applications on his machine, plus installers that survive an app update. The browser work stays (his words: "эти нововведения оставь"). New wave, goal_waves 1->2, tickets T-031..T-037.
- 28.07.26 09:56 [E-080] [parent: E-079] [T-none] RUN: feasibility scouted per target before ticketing, because each one has a different and non-obvious ceiling. Antigravity is a VS Code fork -> a colour-theme extension in ~/.antigravity/extensions/ (he already hand-made one, vintage-win95-theme, and settings.json points at it) -- user-profile install, so an app update cannot touch it. Claude Code is a terminal program -> the colours are the TERMINAL's, so the deliverable is a Windows Terminal scheme plus the ~/.claude/settings.json wiring. MPC-HC is native Win32 with no stylesheet -- registry only (HKCU\Software\MPC-HC\MPC-HC\Settings already has MPCTheme=1), so the honest ceiling is "dark theme on + whatever colour keys it actually exposes", and I will report what it does NOT reach rather than pretend. FreeBuff and NomadCode are Electron: real theming means injecting CSS into a packed app.asar, which modifies installed software and is the only genuinely invasive target here -- backup + reversible + never run without the user saying so.
- 28.07.26 09:56 [E-081] [parent: E-080] [T-none] DEC: goal_waves 1->2
- 28.07.26 10:20 [E-082] [parent: E-081] [T-031] RUN: SCOUT+BUILD -- desktop/ subsystem. tools/build-desktop.js reads the SAME themes/*.json the userscript uses and fills ${token} placeholders in per-target templates, so a colour cannot drift between the browser and the editor. An unknown token throws instead of surviving as a literal: VS Code's theme loader accepts `${textPrimaryy}` as a colour value and then renders that element with no colour at all, which is the same invisible-failure class the CSS gate was written for. desktop/install.ps1 installs into the USER PROFILE (~/.antigravity/extensions, ~/.vscode/extensions) rather than the program directory -- that is the whole reason an app update cannot eat the theme -- with -WhatIf, -Revert, a timestamped backup of anything it replaces, and a refusal to install a desktop/out that is stale against themes/ unless -Force.
- 28.07.26 10:21 [E-083] [parent: E-082] [T-032] DEC: did NOT hand-author a VS Code theme. The machine already had one -- vintage-win95-theme, written by the user, and settings.json points at it -- so the template was DERIVED from it by replacing each hex with the token whose value it equalled: 96 of 99 colour keys mapped exactly, the other 3 are #00000000 (deliberate "no shadow") and stay literal. The six generated themes are therefore the user's own mapping of which editor surface deserves which token, re-tinted per palette, rather than my guess at it.
- 28.07.26 10:22 [E-084] [parent: E-083] [T-032] H: is the generated golden theme really the same theme? Compared the installed output against the hand-made original: all 99 colour keys identical, all 14 tokenColors identical. Then checked a second palette actually differs (editor.background #1A0F05 -> #0A0D15) and that no ${placeholder} survived anywhere. Installed to both Antigravity and VS Code, re-ran to confirm the backup-then-replace path works. VERDICT: CONFIRMED -- golden is byte-equal to what he had, five more palettes alongside it.
- 28.07.26 10:22 [E-085] [parent: E-084] [T-032] DEC: goal_tickets 9->11
- 28.07.26 10:26 [E-086] [parent: E-085] [T-032] DEC: the new release gate failed on its first real use, correctly. build-desktop stamps the extension's version from the `// @version` line, and release.ps1 bumps that line before running its gates -- so `build-desktop --check` is guaranteed stale on every release. Changed to actually BUILD at that point rather than check: the staleness is caused by the bump, its resolution is deterministic, and there is nothing for a human to review in it. The --check form still guards the developer's own working tree via install.ps1, where a stale build IS worth refusing.
- 28.07.26 10:30 [E-087] [parent: E-086] [T-032] RUN: ship v1.11.1 -> pushed 01daeee..b94dd79. Six desktop themes live and installed to both editors.
- 28.07.26 10:31 [E-088] [parent: E-087] [T-none] DEC: release.ps1 does `git add -A`, so this ship also committed `wintage.user_old.js` -- an untracked leftover that predates this session. Not deleting it: it is the user's file and deletion is his call (RFC 1.1). Ticketed T-038 to ask.
- 29.07.26 02:40 [E-089] [parent: E-088] [T-033] DEC: screenshot corrected the target -- Claude Code here is the DESKTOP app (Electron), not the CLI in a terminal, so T-033 is an Electron target and not a Windows Terminal colour scheme. Same mechanism as FreeBuff and NomadCode; the terminal-scheme idea is dropped rather than built for an app nobody asked about.
- 29.07.26 02:41 [E-090] [parent: E-089] [T-035] H: does `resources/app/` shadow `resources/app.asar` in Electron? NO, and it fails SILENTLY -- Electron searches app.asar FIRST and only falls back to app/ when the archive is absent. Freebuff started perfectly, looked normal, and the theme never ran; there was no error anywhere to notice. VERDICT: the whole first design was wrong. Replaced by MOVING the archive to resources/app/app.asar (its app.asar.unpacked sibling moves too -- that pairing is by filename and separating it breaks native modules) so the shim takes the empty app/ slot. Nothing of the app is rewritten, only relocated, and --revert moves it straight back.
- 29.07.26 02:42 [E-091] [parent: E-090] [T-035] H: second failure, this one loud. The shim's package.json is COPIED from the app's own (so name/version are preserved and userData does not move to an empty profile) -- and Freebuff's declares "type": "module", so a .js shim was parsed as ESM and died at its first `require` with a main-process error dialog. Renamed to shim.cjs, which pins CommonJS regardless of what the copied manifest says. VERDICT: fixed and proven -- wintage-status.txt now reads `injected 39350 bytes into http://127.0.0.1:53935/` and the app runs.
- 29.07.26 02:43 [E-092] [parent: E-091] [T-035] DEC: added a status stamp to the shim, written on every insertCSS result. A themed-looking window is not evidence -- these apps have dark themes of their own -- so "is Wintage actually live here?" needed an answer that does not depend on a screenshot or a devtools port. Same reasoning as data-w95-ver on the browser side. It is what caught the silent failure above.
- 29.07.26 02:44 [E-093] [parent: E-092] [T-035] RUN: the CSS is not written for this target -- it is EXTRACTED from wintage.user.js (GLOBAL_CSS + SHADOW_CSS with ${T.x} and the bevel constants resolved per palette, 515 lines, 39 KB) so every bevel, scrollbar and type-ladder fix made for the browser lands in the apps too, with no second copy to rot. The repainter JS deliberately does not come along: for one known application it buys much less than it does on the open web.
- 29.07.26 02:45 [E-094] [parent: E-093] [T-035] RUN: installed and verified live -- Freebuff (freebuff palette, injection confirmed, window opens) and Antigravity agent app (antigravity palette, name/version preserved: antigravity 2.4.3). Claude desktop refused with EBUSY because it is the app running this very session; the installer now catches that, removes its half-written payload, and says "close it completely and run this again" instead of a stack trace -- verified by running it against the live app. NomadCode is not installed on this machine (only AppData leftovers), so its target is declared and skipped rather than faked.
- 29.07.26 02:46 [E-095] [parent: E-094] [T-035] DEC: goal_tickets 11->13
- 29.07.26 02:52 [E-096] [parent: E-095] [T-035] RUN: ship v1.12.0 -> pushed b94dd79..6650dbf. Electron target live for Claude desktop, Freebuff, the Antigravity agent app and NomadCode-when-installed.
- 29.07.26 03:05 [E-097] [parent: E-096] [T-040] DEC: user wants golden everywhere -- picking a per-app palette was my invention, not his ask, and UI.md's Dark Golden is the point of the whole project. Repainted Freebuff and the Antigravity agent app to golden. `-Palette` already defaults to golden, so the other five stay available for anyone who wants them without being the default for anything.
- 29.07.26 03:06 [E-098] [parent: E-097] [T-040] H: changing palette on an ALREADY themed app failed -- "no app.asar in <resources>", because the archive had already moved into app/ and the installer still demanded it at the original path. The most ordinary request there is ("same app, different colour") was the one path not handled. Fixed with an in-place repaint branch: swap wintage.css + shim.cjs, update wintagePalette, leave the archive alone -- which also means a repaint works while the app is running, unlike a first install. VERDICT: fixed, verified live -- Freebuff status reads `injected 39361 bytes` and its wintage.css carries --background: #1A0F05.
- 29.07.26 03:06 [E-099] [parent: E-098] [T-040] DEC: goal_tickets 13->14
