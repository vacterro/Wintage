# Wintage — Audit Handoff

RUN_ID: acb-mtlweqbo-058883880e4a4c109f61
GENERATED_AT: 2026-09-03T22:45:58.452364
PROJECT_NAME: Wintage
TARGET: /mnt/data/Wintage_03.09.26-T22-07-12.zip -> extracted project root /mnt/data/_audit_wintage_core
BASELINE: archive SHA256 fc1fb54fb7255391cf5a820bb932f0cd06535f60e158d7a42f0538bcb45453e9; Wintage userscript v1.30.0; AUDAPACK reports dirty main@ffc13fd5065855239642a6d479806e6c14956814 with 64 changed files
GIT_CONTEXT: UNREADABLE - raw HEAD/refs and AUDAPACK identify main@ffc13fd5065855239642a6d479806e6c14956814 with origin/main at the same ref and dirty=true/64 changed files, but .git/objects is absent, so git status/log/diff cannot execute
SAIPEN_CONTEXT: STALE - STATE/BOARD remain centered on T-228 and "Nothing is shipped", while LOG E-769 records v1.30.0 shipped and E-772/T-233 records prior defects; current dirty source already contains partial post-E-772 repairs not reflected in STATE/BOARD/LOG
TOTAL_TICKETS: 19
STATUS: AUDIT_ALL_3: COMPLETE

## Summary of Wave Audits
- Wave 1 (Core): 5 tickets (baseline: archive SHA256 fc1fb54fb7255391cf5a820bb932f0cd06535f60e158d7a42f0538bcb45453e9; Wintage userscript v1.30.0; AUDAPACK reports dirty main@ffc13fd5065855239642a6d479806e6c14956814 with 64 changed files)
- Wave 2 (Second Wave): 7 tickets (baseline: archive SHA256 fc1fb54fb7255391cf5a820bb932f0cd06535f60e158d7a42f0538bcb45453e9; Wintage userscript v1.30.0; raw main ref ffc13fd5065855239642a6d479806e6c14956814; AUDAPACK baseline reports dirty=true with 64 changed files)
- Wave 3 (Performance): 7 tickets (baseline: archive SHA256 fc1fb54fb7255391cf5a820bb932f0cd06535f60e158d7a42f0538bcb45453e9; Wintage userscript v1.30.0; raw main ref ffc13fd5065855239642a6d479806e6c14956814; AUDAPACK reports dirty=true with 64 changed files)

---
## 01 — AUDIT CORE
PROJECT_NAME: Wintage
DATE_TIME: 2026-09-03T22:08:00+03:00
CAMPAIGN_PROFILE: quick3
CAMPAIGN_PROFILE_VERSION: 1.0.0
CAMPAIGN_RUN_ID: acb-mtlweqbo-058883880e4a4c109f61
CAMPAIGN_MANIFEST_SHA256: 97d9d053e37e6597a8559008911f84b28f799f326a97c69ebc5b304690defa72
WAVE_ID: core
WAVE_INDEX: 1
WAVE_COUNT: 3
WAVE: AUDIT CORE
TARGET: /mnt/data/Wintage_03.09.26-T22-07-12.zip -> extracted project root /mnt/data/_audit_wintage_core
BASELINE: archive SHA256 fc1fb54fb7255391cf5a820bb932f0cd06535f60e158d7a42f0538bcb45453e9; Wintage userscript v1.30.0; AUDAPACK reports dirty main@ffc13fd5065855239642a6d479806e6c14956814 with 64 changed files
PREVIOUS_WAVE_SHA256: NONE
GIT_CONTEXT: UNREADABLE - raw HEAD/refs and AUDAPACK identify main@ffc13fd5065855239642a6d479806e6c14956814 with origin/main at the same ref and dirty=true/64 changed files, but .git/objects is absent, so git status/log/diff cannot execute
SAIPEN_CONTEXT: STALE - STATE/BOARD remain centered on T-228 and "Nothing is shipped", while LOG E-769 records v1.30.0 shipped and E-772/T-233 records prior defects; current dirty source already contains partial post-E-772 repairs not reflected in STATE/BOARD/LOG
AUDIT_SCOPE: project map; userscript startup/exclusion/theme state/menu/observer paths; desktop target dispatcher, ownership snapshots and revert paths; Electron relocation transaction/recovery; Windows Terminal state ownership/recovery; Total Commander/qBittorrent INI restoration; release/test wiring; manifests/lockfile; SAIPEN STATE/BOARD/LOG/ADRs; current Node/build gates
TEST_STATUS: TEST_FAILED
TEST_LIMITATION: Windows PowerShell unavailable in audit environment: command -v pwsh -> NOT_FOUND; command -v powershell -> NOT_FOUND, so tests/Run-Tests.ps1 and PowerShell-only ownership/integration suites could not execute. node tools/import-fastprompter.js --check exited 0 but explicitly SKIPPED freshness because no FastPrompter source was available.
VERIFIED_INSTEAD: 17 current-source Node/build checks were invoked in a disposable copy: 15 PASS, tools/test-theme-switch.js FAIL, import-fastprompter freshness SKIPPED. Fresh real-tool fixtures independently reproduced Windows Terminal configuration loss and Electron relocation rollback drift.
STATUS: AUDIT_CORE: COMPLETE
TICKETS: 5
HANDOFF: IMPLEMENTATION_AGENT
COVERAGE_INSPECTED: wintage.user.js; package.json; package-lock.json; release.ps1; desktop/install.ps1; desktop/WintageInstaller.ps1; desktop/modules/common.ps1; desktop/modules/targets.ps1; tools/install-electron.js; tools/install-terminal.js; tools/test-theme-switch.js; tools/test-spa-exclude.js; tools/test-electron-state.js; tools/test-terminal-font.js; tools/check-css.js; tools/test-diag-counters.js; tools/test-perf-bounded.js; tools/test-fs-retry.js; tools/test-repainter-polarity.js; tools/test-electron-shim.js; tools/test-theme-packs.js; tools/test-shim-payloads.js; tools/build-desktop.js; tools/apply-themes.js; tools/derive-palette.js; tools/import-fastprompter.js; tools/check-wiki-mirror.js; tests/Run-Tests.ps1; .saipen/STATE.md; .saipen/BOARD.md; .saipen/LOG.md; .saipen/IDENTITY.md; .saipen/KNOWLEDGE/ADR-001..006; audit/1.md; _AUDAPACK_MANIFEST.json; raw .git HEAD/refs
COVERAGE_DEFERRED: dynamic Windows registry/WinForms/installed-application behavior requiring PowerShell/Windows; real external application installations; real browser navigation cancellation behavior; external services/accounts/endpoints; non-core wave-2 and performance-specific surfaces
CROSS_WAVE_REFERENCES: NONE - this is wave 1/3. audit/1.md is a prior 2026-09-02 campaign artifact used only for orientation; every retained finding below was revalidated against the current dirty source and, where runnable, freshly reproduced.
RESIDUAL_UNCERTAINTY: git object history/diff is unavailable in the packaged artifact; PowerShell-only regression suites could not run; current archive is explicitly dirty, so raw main ref is an identity anchor rather than an exact content commit

[P0] [CORE-001] tools/install-terminal.js / owned-state snapshot + Apply/Revert
EVIDENCE:
- Current snapshot logic at tools/install-terminal.js:301-306 stores owned profile values directly, using null both when a field is absent and when the user's explicit value is null.
- mergeOwnedField at :191-193 treats null/undefined as "delete this field".
- Apply at :308-313 converts a legacy top-level profiles array into an object with defaults/list, but the snapshot records no original profiles container shape.
- Apply at :355-358 removes every existing scheme named "Wintage" before inserting Wintage's scheme; the removed user scheme is not captured in ownership state.
- Fresh exact-tool fixtures against the current source reproduced all three losses:
  1. Before:
     {"profiles":{"defaults":{"colorScheme":null,"font":null,"antialiasingMode":null,"historySize":null}},"schemes":[],"keep":{"x":1}}
     After Apply -> Revert:
     {"profiles":{"defaults":{}},"keep":{"x":1}}
     Explicit null-valued properties were deleted.
  2. Before:
     {"profiles":[{"name":"p1"}],"schemes":[],"keep":1}
     After Apply -> Revert:
     {"profiles":{"defaults":{},"list":[{"name":"p1"}]},"keep":1
     The legacy array representation was not restored.
  3. Before schemes:
     [{"name":"Wintage","custom":"USER-SCHEME"},{"name":"KeepMe","custom":"KEEP"}]
     After Apply -> Revert only KeepMe remained; the pre-existing user-owned "Wintage" scheme was destroyed.
- Both Apply and Revert returned exit code 0 in these reproduction fixtures, so this is silent successful-path configuration loss, not merely an error-path artifact.
DEFECT:
The Terminal ownership snapshot does not encode presence separately from value and does not capture every structure that Apply destructively normalizes/replaces. Consequently Revert cannot reconstruct the pre-operation document. It silently deletes explicit-null fields, fails to restore legacy profiles shape, and permanently removes a pre-existing user scheme named Wintage. This violates the tool's ownership/reversibility invariant and is concrete user configuration data loss.
REPAIR:
- Represent every owned scalar as presence-aware state, e.g. {present:true,value:null} versus {present:false}, rather than overloading null.
- Snapshot the original profiles container form before any array -> object normalization and restore that form when Wintage-owned changes are reverted, while preserving unrelated post-Apply user edits where the ownership model permits.
- Before replacing schemes named Wintage, capture the displaced pre-existing scheme entries and whether schemes itself was absent, empty, or populated.
- On Revert remove only the scheme installed by this Wintage operation and restore displaced entries exactly; do not infer ownership solely from the scheme name.
- Version/migrate the state payload if necessary so older snapshots are handled deliberately rather than misinterpreted as the new presence-aware representation.
VERIFY:
- Add current-tool Apply -> Revert fixtures for: absent field, explicit null, explicit empty object/string where legal, legacy profiles array, normal profiles object, absent schemes, empty schemes, and a pre-existing user "Wintage" scheme.
- Assert semantic pre/post equality for all Wintage-owned structures plus survival of unrelated edits made after Apply.
- node tools/test-terminal-font.js remains PASS.
- Run the applicable PowerShell ownership/integration suite on Windows and tests/Run-Tests.ps1 after implementation.

[P1] [CORE-002] tools/install-electron.js / relocation Revert rollback
EVIDENCE:
- The current source partially fixes the earlier relocation snapshot defect: captureAppDir at tools/install-electron.js:634-642 snapshots both app.asar and app.asar.unpacked recursively, and desktop/modules/targets.ps1:196-243 now uses the full snapshot for non-Repaint operations.
- Revert still performs a paired move at tools/install-electron.js:414-417:
  moved app.asar -> root app.asar;
  moved app.asar.unpacked -> root app.asar.unpacked;
  then removes appDir.
- When WINTAGE_TEST_FAIL_AFTER_REVERT triggers at :418, catch/rollback at :421-462 recreates the moved archive, removes root app.asar, and restoreAppDir(appPre) recreates the moved tree, but never removes the root app.asar.unpacked created by the preceding rename.
- rollbackOk at :446 verifies rollbackFailures plus movedAsar restoration only. It does not validate root/moved app.asar.unpacked state before reporting at :448 that the operation was "rolled back to the exact pre-operation state."
- Fresh current-tool reproduction:
  initial root contained app.asar and app.asar.unpacked/native/addon.node = "NATIVE-BYTES";
  relocation Apply succeeded and produced:
  movedAsar=true, movedUnpacked=true, rootAsar=false, rootUnpacked=false.
  Revert with WINTAGE_TEST_FAIL_AFTER_REVERT=1 exited 1 and reported exact rollback.
  Actual post-failure state:
  movedAsar=true, movedUnpacked=true, rootAsar=false, rootUnpacked=true.
  Both moved and root unpacked trees contained NATIVE-BYTES.
DEFECT:
Relocation Revert is not failure-atomic for the app.asar/app.asar.unpacked pair. A failure after the unpacked-tree rename leaves an orphan duplicate at the application root while also restoring the moved copy, yet the recovery verifier declares exact restoration. Subsequent updates/reapply logic can therefore start from a topology that never existed before the operation.
REPAIR:
- Treat app.asar and app.asar.unpacked as one relocation transaction in both forward and rollback paths.
- Track each successful move explicitly; on rollback undo every completed move, including deleting/restoring root app.asar.unpacked according to its captured pre-state.
- Do not define rollback success from movedAsar alone. Compare the full captured pre-state for both root locations and the relocation directory, including existence/type and recursive unpacked content.
- Emit "exact pre-operation state" only after that structural verification succeeds; otherwise retain recovery artifacts and report incomplete rollback.
VERIFY:
- Extend tools/test-electron-state.js with a real app.asar.unpacked tree and trigger WINTAGE_TEST_FAIL_AFTER_REVERT=1.
- Assert after the failed Revert that root app.asar and root app.asar.unpacked exactly match their pre-Revert absence/presence and that moved app.asar plus moved app.asar.unpacked are byte/tree-identical to the captured pre-state.
- Add a failure seam covering rollback of the unpacked sibling itself so the incomplete-recovery path is tested.
- node tools/test-electron-state.js and node tools/test-electron-shim.js must PASS.

[P1] [CORE-003] wintage.user.js / setupRouteGuard + theme reload/pending-state handling
EVIDENCE:
- Exclusion handling is defined at wintage.user.js:35-40 and setupRouteGuard at :58-110.
- On an allowed -> excluded SPA route, the guard sets window.__wintageExcludedReload=true and calls location.reload(). The latch is reset only when reload throws synchronously.
- There is no excluded-state check in the mutation/sweeper repaint path and setupRouteGuard does not call suspendRepainter or otherwise quarantine Wintage before attempting the reload.
- Therefore if navigation/reload is refused, cancelled, intercepted, or otherwise returns without unloading the document and without a synchronous exception, the document remains live with Wintage's mutation machinery active on a route that the source explicitly classifies as excluded.
- tools/test-spa-exclude.js currently passes but tests successful reload/idempotency behavior; it does not exercise the "reload requested but document remains alive" safety state.
- The same reload-as-state-transition assumption breaks theme recovery:
  wintage.user.js:384-398 computes
  requested = stored slug;
  THEME_ID = THEMES[requested] ? requested : fallback;
  STORED_THEME_ID = requested.
- The "Apply pending theme" menu at :487-490 requires:
  STORED_THEME_ID &&
  STORED_THEME_ID !== THEME_ID &&
  THEMES[STORED_THEME_ID].
  This condition is unreachable by construction: whenever THEMES[STORED_THEME_ID] is true, THEME_ID was assigned that same STORED_THEME_ID at :390; when THEME_ID differs due fallback, THEMES[STORED_THEME_ID] is false.
- Theme callbacks persist the new slug before reload, while STORED_THEME_ID/THEME_ID remain startup constants if the page does not unload, so a failed/cancelled reload leaves desired and painted state split without the advertised recovery row.
DEFECT:
Reload is being used as an implicit state commit without modeling the case where the current document survives. For excluded-route transitions this violates the safety boundary because active theming continues on a route deliberately excluded from modification. For theme switching it makes the intended pending-state recovery command algebraically unreachable and leaves runtime state inconsistent after a non-completing reload.
REPAIR:
- Introduce explicit runtime state for "document is now excluded/suspended" and "desired theme differs from currently painted theme"; do not infer either solely from whether reload was called.
- On transition to an excluded URL, enter quarantine before requesting reload: stop/suppress repaint observers/timers and prevent further Wintage injections/writes for that document. Keep quarantine in force if the reload does not complete.
- Make the exclusion latch correspond to current route/state, not a one-way boolean that only synchronous exceptions can clear.
- For theme switching, track the newly persisted desired theme in mutable runtime state or make the recovery command read storage when invoked. Remove the impossible STORED_THEME_ID/THEME_ID predicate.
- Preserve the current normal successful-reload behavior and avoid introducing repeated reload loops.
VERIFY:
- Extend tools/test-spa-exclude.js with a reload stub that returns normally without unloading; assert the excluded state is immediately suspended and subsequent mutations cannot invoke repaint work.
- Cover synchronous reload throw and a later excluded -> allowed route transition, verifying the latch/state can recover without a reload storm.
- Add executable theme-switch cases where storage changes but reload does not complete; assert a usable retry/recovery path exists and identifies the desired theme correctly.
- Verify normal startup on an excluded URL remains a no-op and existing SPA exclusion tests remain PASS.

[P1] [CORE-004] tools/test-theme-switch.js + release.ps1 / stale real-source harness
EVIDENCE:
- Fresh execution of the current release-wired test fails:
  node tools/test-theme-switch.js
  -> ReferenceError: IS_GOOGLE is not defined
  at evalmachine.<anonymous>:285
  from tools/test-theme-switch.js:63:6.
- wintage.user.js defines IS_GOOGLE near :141, but tools/test-theme-switch.js extracts/evaluates a later source slice beginning at `const THEMES`; that slice now contains the use at wintage.user.js:436 while excluding the declaration.
- The VM environments in tools/test-theme-switch.js provide IS_TOP, IS_X, IS_REDDIT and other globals but not IS_GOOGLE, including the main run context and its specialized contexts.
- release.ps1:152 executes node tools/test-theme-switch.js and :153 aborts release when it returns non-zero. This is therefore an active release-gate failure, not an optional test.
- Other freshly invoked related gates passed, including tools/test-spa-exclude.js, tools/test-diag-counters.js, tools/test-perf-bounded.js, tools/test-repainter-polarity.js, tools/test-theme-packs.js and tools/test-shim-payloads.js.
DEFECT:
The test harness's extracted-source contract drifted when production code gained an IS_GOOGLE dependency. The mandatory release pipeline now aborts before the intended theme-switch assertions execute. Because the harness manually enumerates external bindings, future source dependencies can produce the same failure without a targeted diagnostic.
REPAIR:
- Update the harness boundary so the evaluated production slice is self-contained, or explicitly provide IS_GOOGLE and every other deliberate external binding required by that slice.
- Prefer a small declared harness contract/preflight for required globals rather than allowing an arbitrary ReferenceError deep in VM execution.
- Add a Google-host case so IS_GOOGLE is behaviorally exercised rather than merely stubbed.
- While repairing the suite, replace the current source-regex-only proof for the pending-theme menu with an executable assertion covering the runtime behavior fixed under CORE-003.
VERIFY:
- node tools/test-theme-switch.js exits 0 and reaches all assertions.
- A Google host/context case executes successfully and validates the expected data-w95-google behavior.
- Deliberately removing a required harness binding causes a clear contract/test failure rather than an unrelated runtime ReferenceError.
- release.ps1 on a Windows-capable runner proceeds past the theme-switch gate to the next release check.

[P2] [CORE-005] desktop/modules/targets.ps1 / Restore-TotalCmdOwned + qBittorrent owned INI Revert
EVIDENCE:
- Get-IniKey at desktop/modules/targets.ps1:632-639 distinguishes an absent key ($null) from a present empty `Key=` value ('').
- Total Commander snapshot code at :645-663 therefore captures that distinction.
- Restore-TotalCmdOwned at :691-700 restores a value only when it is both non-null and non-empty; an original empty string falls into Remove-IniKey, collapsing "present empty" into "absent".
- qBittorrent's snapshot at :2559-2564 likewise captures Get-IniKey results, but its Revert at :2501-2506 uses the same non-null AND non-empty condition and deletes an originally present empty key.
- This is directly contradictory within the current implementation: capture preserves the three-state distinction, restoration discards one state.
- Dynamic fixture execution was unavailable because neither pwsh nor Windows PowerShell exists in the audit environment.
DEFECT:
Both ownership restorers use string emptiness as an absence sentinel even though the parser already has an unambiguous absence sentinel, $null. Reverting a setting that originally existed as `Key=` silently changes configuration shape by deleting the key.
REPAIR:
- Use only `$null` to mean "key was absent".
- If the captured value is `''`, call Set-IniKey with an empty value so the original `Key=` entry remains present.
- Apply the same presence rule consistently to Total Commander and qBittorrent rather than fixing one target independently.
- Preserve the existing behavior for genuinely absent keys and non-empty values.
VERIFY:
- Add PowerShell fixtures whose pre-state contains the affected owned keys explicitly as `Key=`.
- Run Apply -> Revert and assert key presence remains true with an empty value for both Total Commander and qBittorrent.
- Include corresponding absent-key fixtures and assert those keys remain absent.
- Run tools/test-ownership.ps1 if present/applicable and tests/Run-Tests.ps1 on Windows.

CORE_DONE_WHEN: CORE-001 Terminal Apply/Revert is presence- and shape-preserving for all owned settings and displaced schemes; CORE-002 Electron failed relocation Revert restores both app.asar and app.asar.unpacked topology/content exactly before claiming success; CORE-003 excluded-route and theme reload state remain correct even when the current document survives reload(); CORE-004 the mandatory theme-switch release gate executes all behavioral assertions successfully; CORE-005 empty INI keys survive Apply/Revert as present-empty; all relevant Node gates pass and Windows PowerShell ownership/integration suites pass on a capable Windows runner.

---
## 02 — AUDIT SECOND WAVE
PROJECT_NAME: Wintage
DATE_TIME: 2026-09-03T22:24:21+03:00
CAMPAIGN_PROFILE: quick3
CAMPAIGN_PROFILE_VERSION: 1.0.0
CAMPAIGN_RUN_ID: acb-mtlweqbo-058883880e4a4c109f61
CAMPAIGN_MANIFEST_SHA256: 97d9d053e37e6597a8559008911f84b28f799f326a97c69ebc5b304690defa72
WAVE_ID: second
WAVE_INDEX: 2
WAVE_COUNT: 3
WAVE: AUDIT SECOND WAVE
TARGET: /mnt/data/Wintage_03.09.26-T22-07-12.zip -> extracted project root /mnt/data/_audit_wintage_core
BASELINE: archive SHA256 fc1fb54fb7255391cf5a820bb932f0cd06535f60e158d7a42f0538bcb45453e9; Wintage userscript v1.30.0; raw main ref ffc13fd5065855239642a6d479806e6c14956814; AUDAPACK baseline reports dirty=true with 64 changed files
PREVIOUS_WAVE_SHA256: NONE
GIT_CONTEXT: UNREADABLE - raw HEAD/refs identify main@ffc13fd5065855239642a6d479806e6c14956814 and origin/main at the same ref, but .git/objects is absent so normal git status/log/diff cannot verify object history; no .git object crawl was attempted
SAIPEN_CONTEXT: STALE - STATE/BOARD still center T-228 and state "Nothing is shipped", while LOG E-769 records v1.30.0 shipped and E-772 records the prior audit; current dirty live source also contains partial post-audit repair comments/changes not reflected coherently in STATE/BOARD
AUDIT_SCOPE: startup/revert/finalize lifecycles; recovery epochs and provenance; helper/parent transaction boundaries; partial mutation and manifest commit rollback; native rollback error propagation; dry-run/WhatIf isolation; OBS serializer/parser symmetry; qBittorrent recovery preflight; GUI/CLI paths.json ownership and concurrent writers; relevant regression/release gates
TEST_STATUS: TEST_FAILED
TEST_LIMITATION: Windows PowerShell unavailable in the audit environment: command -v pwsh -> NOT_FOUND and command -v powershell -> NOT_FOUND, so PowerShell ownership/concurrency/WhatIf/registry suites and tests/Run-Tests.ps1 could not execute. node tools/import-fastprompter.js --check exited 0 but explicitly skipped freshness because no FastPrompter source was available.
VERIFIED_INSTEAD: 17 current Node/build checks were invoked in a disposable copy: 16 exited 0 and the mandatory tools/test-theme-switch.js gate failed with ReferenceError: IS_GOOGLE is not defined, matching CORE-004. Fresh exact-helper fixtures reproduced the Windows-theme epoch-finalization and OBS recovery/parser defects. An injected OBS late-write failure reproduced partial live mutation before manifest commit. PowerShell-only race/rollback/WhatIf/path defects were verified from reachable live source paths plus regression-suite coverage gaps.
STATUS: SECOND_WAVE: COMPLETE
TICKETS: 7
HANDOFF: IMPLEMENTATION_AGENT
COVERAGE_INSPECTED: tools/install-windows-theme.js; tools/install-obs.js; tools/write-atomic.js; desktop/modules/common.ps1; desktop/modules/targets.ps1; desktop/install.ps1; desktop/WintageInstaller.ps1; tools/install-browsers.ps1; tools/test-epoch.ps1; tools/test-ownership.ps1; tools/test-reapply.ps1; tools/test-recovery-consumption.ps1; tools/test-dir-prestate.ps1; tools/test-terminal-recorded-set.ps1; tests/Run-Tests.ps1; release.ps1; package.json; package-lock.json; .saipen/STATE.md; .saipen/BOARD.md; .saipen/LOG.md; relevant .saipen/KNOWLEDGE ADRs; audit/1.md as orientation only
COVERAGE_DEFERRED: dynamic Windows registry/WinForms/theme activation and PowerShell child-process behavior; real installed applications; live multi-session Windows mutex behavior; external accounts/services/hosts/endpoints; performance-specific wave-3 surfaces
CROSS_WAVE_REFERENCES: CORE-001 Terminal ownership-state loss, CORE-002 Electron relocation rollback drift, CORE-003 userscript reload-state drift, CORE-004 broken theme-switch release gate, and CORE-005 empty INI-key restoration remain separate valid wave-1 findings. W2-004/W2-005 repair must preserve and strengthen the exact-state recovery contracts required by CORE-001/CORE-002 rather than replacing them with best-effort cleanup.
RESIDUAL_UNCERTAINTY: PowerShell runtime was unavailable, so Windows-only interleavings were not dynamically executed; OBS failure injection exercised the exact Node helper rather than the unavailable PowerShell parent; real Windows CurrentTheme behavior after snapshot deletion was not exercised; git objects/history remain unavailable in the archive

[P1] [W2-001] tools/install-windows-theme.js / finalizeRevert + epochRetired lifecycle
EVIDENCE:
- tools/install-windows-theme.js:41-49 explicitly defines the intended lifecycle: completed Revert retires an epoch, the old snapshot must remain while Windows may still reference it as CurrentTheme, and the next fresh Apply must re-baseline from the then-current active theme.
- Actual finalizeRevert at :99-110 does the opposite: it deletes Wintage.original.theme, .wintage-original-theme-path, backup/created/palette/active-path markers, AND removes .wintage-epoch-retired.
- No live path creates .wintage-epoch-retired. The only lifecycle actions on that marker are the firstApply check at :129 and removal at :107/:145.
- Consequently `firstApply = !exists(original) || exists(epochRetired)` at :129 contains a retirement branch that production code cannot establish.
- Revert at :113-120 returns Wintage.original.theme as the file to activate.
- Fresh exact-helper fixture against the current source:
  Apply -> firstApply=true and Wintage.original.theme exists.
  Revert -> returned activate=<...>/Wintage.original.theme; returned activation file existed; retired marker absent.
  Finalize -> finalized=true; returned activation file no longer existed; retired marker still absent.
DEFECT:
The documented epoch lifecycle and executable lifecycle contradict each other. Successful Revert activates Wintage's restoration snapshot and successful finalize immediately deletes that active-path file instead of retiring the epoch. The marker intended to distinguish "live epoch baseline" from "old baseline awaiting re-baseline" is unreachable. A later Apply therefore cannot reliably distinguish a completed prior lifecycle from an in-epoch repaint, and Windows may be left with CurrentTheme pointing at a file Wintage just removed.
REPAIR:
- Make finalizeRevert establish the retired epoch state rather than deleting it.
- Keep the restoration snapshot and its original-path evidence while the completed Revert may still reference that file.
- On the next non-repaint Apply after a retired epoch, capture the THEN-current active theme as the new epoch baseline before changing the active theme, then clear the retired marker only after the fresh snapshot is committed.
- Delete obsolete retired snapshot material only when it is provably no longer the active/recovery source.
- Preserve existing repaint behavior within one active epoch: repaint must continue reusing that epoch's original baseline.
VERIFY:
- Exact-helper Apply -> Revert -> finalize must leave the returned Revert activation path present and create the retired marker.
- Simulate a user theme change after completed Revert, then Apply again; assert firstApply=true and the new snapshot equals that new current theme, not the previous epoch's baseline.
- Repaint inside an unretired epoch must remain firstApply=false and preserve the original baseline byte-for-byte.
- Revert/finalize/reapply cycles repeated at least twice must not accumulate stale managed themes or delete the currently referenced recovery theme.

[P1] [W2-002] tools/install-obs.js / readThemeSnapshot + currentThemeKeyValue + Apply snapshot parsing
EVIDENCE:
- parseIni at tools/install-obs.js:90-107 stores section and key names with original case.
- Mutation helpers removeIniKey/setIniValue at :110-150 deliberately match section/key names case-insensitively.
- Recovery reads do not: readThemeSnapshot at :164-166 and currentThemeKeyValue at :169-173 use exact `sections[THEME_SECTION][THEME_KEY]`; first-Apply snapshot capture at :275-278 does the same.
- readThemeSnapshot at :158-163 treats a file beginning with `{` as JSON, but malformed JSON OR parsed JSON lacking boolean `existed` silently falls through and is reinterpreted as legacy INI.
- assertRecoveryComplete at :187-193 only tests whether readThemeSnapshot returns a truthy object. Reinterpreting invalid JSON as an INI snapshot returns `{existed:false,value:null}`, so corrupt/wrong-shape recovery can pass the supposedly fail-closed preflight.
- Fresh exact-helper fixture with live user.ini `[Appearance]\nTheme=CurrentTheme\nOther=keep` and `user.ini.wintage.bak` containing `{}`: Revert exited 0 and resulting user.ini retained Other but deleted Theme.
- Fresh exact-helper fixture with live user.ini `[appearance]\ntheme=System\nOther=keep`: Apply recorded `{"existed":false,"value":null}`; subsequent Revert removed the original lowercase theme key instead of restoring it.
DEFECT:
OBS has asymmetric INI semantics and an ambiguous recovery parser. Writers correctly treat INI names case-insensitively, while readers/snapshot capture treat them case-sensitively. Separately, malformed or wrong-schema JSON is silently reclassified as legacy INI. Together these allow Wintage to certify invalid recovery as valid and erase an existing user Theme selection during Revert.
REPAIR:
- Once a recovery payload is identified as JSON, parse and schema-validate it strictly; JSON parse/schema failure must be terminal and must never fall through to legacy INI interpretation.
- Validate `existed` and the corresponding `value` contract explicitly.
- Centralize case-insensitive INI section/key lookup and use it for currentThemeKeyValue, initial snapshot capture, and legacy snapshot reads.
- Preserve original spelling/casing and unrelated lines during writes; only lookup semantics need normalization.
- Keep legacy whole-file INI recovery support, but select it by an unambiguous format rule rather than failed JSON parsing.
VERIFY:
- `{}`, malformed `{...`, and JSON with invalid existed/value types must make Revert exit nonzero before any live file changes.
- `[appearance]\ntheme=System`, mixed-case section/key variants, and canonical `[Appearance]\nTheme=...` must each Apply -> Revert back to their original semantic value while preserving unrelated text.
- Existing valid JSON recovery and genuine legacy INI recovery must still restore successfully.
- Add byte/semantic assertions proving malformed recovery never deletes a live Theme key.

[P1] [W2-003] desktop/modules/common.ps1:Get-InstallEpoch / first-creation concurrency
EVIDENCE:
- Get-InstallEpoch resolves an existing epoch at desktop/modules/common.ps1:346-370.
- On absence it generates a private GUID at :371, writes a unique temporary file at :375-376, then performs `Move-Item ... -Force` into the shared install-epoch.json at :377 and immediately returns its own private GUID at :378.
- No lock or create-if-absent arbitration surrounds the absent check plus first creation.
- Two processes can therefore both observe "absent", generate A/B, each write a valid temp, then overwrite the same destination in sequence. The losing process still returns its own ID even if the persisted winner is the other ID.
- Write-RecoveryProvenance at :383-389 stamps recovery using the ID returned by Get-InstallEpoch.
- Assert-RecoveryProvenance at :397-407 later compares that stamp with the currently persisted epoch. A same-install recovery created by the losing process can consequently be classified as foreign immediately.
- tools/test-epoch.ps1 covers absent creation, repeated sequential reads, valid/corrupt/empty/array states and provenance, but contains no concurrent first-create race.
DEFECT:
Atomic replacement protects against torn JSON but does not provide first-writer identity arbitration. The install epoch is a singleton identity used as a recovery ownership boundary, yet concurrent first creation can return different identities to concurrent targets. That can poison otherwise legitimate recovery provenance and cause later Revert to fail closed against Wintage's own backup.
REPAIR:
- Serialize epoch initialization with a dedicated app-data-scoped lock, or use an exclusive create primitive where exactly one creator wins and every loser re-reads the winner.
- The function must never return an ID until that exact ID is either proven to be the persisted singleton or the persisted winner has been read.
- Preserve the existing fail-closed behavior for malformed existing epoch files; never "repair" corruption by rotating identity.
- Define lock ordering explicitly if a process can enter Get-InstallEpoch while already holding a target lock so no inverse acquisition path can deadlock.
- Clean abandoned temporary creation files deterministically.
VERIFY:
- Add a barrier-based 2-process and 4-process first-creation test against one empty WintageAppData.
- Every child must return the same ID, install-epoch.json must contain that same ID, and recovery provenance written by every child must pass Assert-RecoveryProvenance afterward.
- Repeat enough times to exercise both creation orderings.
- Existing corrupt/foreign-provenance tests must remain fail-closed and byte-preserving.

[P1] [W2-004] desktop/modules/targets.ps1:Invoke-Obs/Invoke-Qbittorrent/Invoke-Conhost/Invoke-MpcHc + desktop/install.ps1 generic target / mutation-before-transaction boundary
EVIDENCE:
- Invoke-TargetCommit at desktop/modules/targets.ps1:787-807 only surrounds the manifest commit. Its restore callback executes only when the commit scriptblock throws.
- Multiple handlers capture exact pre-state, then mutate the target BEFORE entering Invoke-TargetCommit. A mutation-phase exception therefore bypasses the provided restore callback entirely.
- OBS: Save-ObsPreState runs at :2302; the helper mutates at :2307; a nonzero helper exit throws at :2308. Invoke-TargetCommit is not reached until :2310/:2318.
- Fresh injected late-write failure against the exact current OBS helper forced the palette-marker atomic rename to fail after earlier writes. The helper exited nonzero, while user.ini had already changed from Theme=System to Theme=com.wintage.OBS, themes/Wintage.ovt had already been installed, recovery artifacts had already been created, and no committed palette marker existed. This is exactly the state the PowerShell parent snapshots at :2302 but never restores when :2308 throws.
- qBittorrent Revert snapshots at :2497-2499, rewrites the owned INI at :2501-2507, and only afterward checks required pristine recovery at :2509-2510. Missing pristine therefore throws after live mutation but before Invoke-TargetCommit at :2526.
- qBittorrent Apply snapshots at :2569-2571, then performs directory creation/copies, INI write and marker write at :2573-2588 before Invoke-TargetCommit at :2591.
- Generic VS Code-family Apply in desktop/install.ps1 snapshots `$dest` at :926, then creates/copies output at :927-929 before Invoke-TargetCommit at :932.
- MPC-HC writes each owned registry value at desktop/modules/targets.ps1:2412-2415 before Invoke-TargetCommit at :2417. Conhost follows the same multi-mutation-before-commit structure.
DEFECT:
The code names these flows transactions, but the transaction boundary begins only at ledger commit. Exact pre-state snapshots are therefore used for manifest-write failures but not for failures during the actual target mutation they were intended to protect. Any mid-copy, mid-registry-write, helper failure, missing late recovery dependency, or verification error can leave the application partially themed while the manifest remains old/absent. Retry/revert then starts from a state the ownership ledger never recorded.
REPAIR:
- Move the rollback boundary outward to cover preflight -> snapshot -> mutate -> verify -> manifest commit as one operation.
- Preflight every required recovery dependency before the first mutation; qBittorrent's required pristine directory is a concrete example.
- After snapshot creation, ANY exception/nonzero result before successful commit must attempt restoration from that snapshot.
- Verify restoration before claiming exact pre-operation state. If restoration is incomplete, keep all persistent recovery evidence and report an explicit INCOMPLETE rollback.
- Prefer one shared transaction primitive for handlers with the same contract rather than repeating subtly different try/catch patterns.
- Do not consume persistent recovery until both target transition and manifest transition have committed.
VERIFY:
- OBS: inject failure at each recovery/live/theme/marker write boundary; target files and manifest must exactly equal pre-operation state after every failure or explicitly report incomplete recovery.
- qBittorrent Revert with missing pristine must fail before INI/theme/marker mutation. Inject mid-Apply copy/write failures and assert exact restoration plus unchanged manifest.
- Generic extension: inject failure after directory creation and during copy; destination and manifest must return exactly to pre-state.
- Conhost/MPC-HC: inject failure after at least one owned registry value is changed; every earlier value must be restored and the manifest must remain unchanged.
- Successful Apply/Revert controls must continue consuming recovery only after commit.

[P1] [W2-005] desktop/modules/targets.ps1:Invoke-TargetCommit + manifest-failure restore callbacks / unchecked native rollback and error drift
EVIDENCE:
- Invoke-TargetCommit comments at :788-793 state rollback native commands are checked and "exact-restoration" is printed only after the entire restore succeeds.
- The implementation does not enforce that contract on restore callbacks.
- Windows Terminal's manifest-failure restore at :1043-1047 executes `node ... --revert` directly for every item and discards stderr; it never checks `$LASTEXITCODE` and does not use Invoke-Native.
- MPC-HC's manifest-failure restore at :2417-2422 executes `reg import` directly and ignores its exit status, despite the normal MPC-HC paths explicitly checking native `reg` exit codes elsewhere.
- Invoke-Native exists at :815-823 specifically to convert native nonzero exit status into a terminating rollback failure, proving these raw call sites bypass the intended common contract.
- If a restore callback does throw, Invoke-TargetCommit's nested catch at :801-803 executes bare `throw` from the rollback catch. The resulting propagated exception is the rollback exception; the original manifest-commit exception is only implied by the preceding log prefix and its actual error detail is lost.
- If a raw native rollback merely returns nonzero without throwing, the wrapper reaches :800 and explicitly reports that the target was restored to its "exact pre-operation state".
DEFECT:
Rollback success is caller-dependent despite comments claiming a checked common phase. Native rollback can fail silently and still produce an exact-restoration success message. When rollback does throw, the original commit failure can be discarded in favor of the rollback exception, degrading recovery diagnostics precisely when both phases failed.
REPAIR:
- Route every native executable used by a restore callback through Invoke-Native or make Invoke-TargetCommit enforce a structured rollback result contract.
- Never suppress stderr/exit status for recovery operations.
- Capture the original commit exception before entering rollback.
- On double failure, throw/report a composite error containing BOTH the original commit failure and every rollback failure.
- Emit exact-restoration only after all rollback actions pass and, where an exact snapshot exists, verification confirms the restored state.
- Audit all Invoke-TargetCommit restore callbacks for raw native executables, not only Terminal and MPC-HC.
VERIFY:
- Inject manifest write failure plus a nonzero Terminal helper revert: command must fail, must not say exact restoration, and output must identify both manifest commit failure and Terminal rollback failure.
- Repeat with failing `reg import` for MPC-HC.
- Inject a PowerShell restore exception and assert the final diagnostic retains the original commit exception as well as the restore exception.
- Successful rollback controls must still report exact restoration only after verification.

[P1] [W2-006] desktop/install.ps1 generic VS Code-family Apply / first-touch recovery under -WhatIf
EVIDENCE:
- For generic extension targets, built-output validation ends at desktop/install.ps1:909.
- First-ever recovery capture begins immediately at :914, BEFORE the target mutation's `$PSCmdlet.ShouldProcess(...)` gate at :925.
- That branch creates recovery directories, may copy the existing destination into pristine recovery, and calls Write-Utf8 for recovery.json at :915-922.
- Write-Utf8 is a direct .NET file write in desktop/modules/common.ps1 and has no ShouldProcess/WhatIf contract of its own.
- Consequently `-WhatIf` reaches first-touch recovery code before the script has decided not to perform the requested installation. At minimum the direct recovery.json write is outside the dry-run boundary; when surrounding ShouldProcess-aware cmdlets suppress their directory creation, that same unguarded direct write can instead fail because its directory was never created.
- The actual destination installation is correctly gated only afterward at :925-934.
- Existing WhatIf regressions cover several special handlers, including FreeBuff/browser/reapply surfaces, but no first-ever generic vscode/antigravity extension recovery fixture establishes zero mutation and successful dry-run behavior.
DEFECT:
The public dry-run boundary is placed after first-install recovery side effects. A first `-WhatIf` for a generic VS Code-family target is therefore not guaranteed to be read-only and can also fail solely because a recovery write escaped ShouldProcess while its prerequisite directory creation did not. Dry-run is supposed to validate/describe an operation, not become the first writer of persistent recovery state.
REPAIR:
- Keep all persistent recovery creation/copy/write operations inside the committed Apply branch, after ShouldProcess approves mutation.
- For WhatIf, perform only read-only validation and report what recovery/install actions would occur.
- Explicitly guard direct .NET writes; do not rely on cmdlet-level WhatIf propagation to protect helper functions that bypass ShouldProcess.
- Preserve first-Apply pristine capture ordering for real Apply: recovery must still be durably captured before destination mutation starts.
VERIFY:
- On a clean temporary vscode target with no recovery directory, `-Target vscode -WhatIf` must exit 0, create no recovery files/directories, and leave destination/manifest byte-identical.
- Repeat for antigravity and for a pre-existing destination that would require pristine capture.
- Repeat with existing recovery state to ensure WhatIf neither refreshes nor consumes it.
- Real Apply afterward must still capture pristine state before destination mutation and pass its normal ownership tests.

[P2] [W2-007] desktop/modules/common.ps1:Save-PathPreference + desktop/WintageInstaller.ps1:Save-CustomPaths/Ask-CustomPath / paths.json multi-writer lost update
EVIDENCE:
- Save-PathPreference at desktop/modules/common.ps1:495-511 performs unlocked read -> merge one CLI-owned key -> temporary write -> forced replacement of the shared paths.json.
- Save-CustomPaths at desktop/WintageInstaller.ps1:247-294 independently performs unlocked read -> preserve foreign keys/merge GUI-owned keys -> temporary write -> forced replacement of that same paths.json.
- Atomic rename prevents torn JSON but does not serialize read-modify-write. If GUI and CLI read state S concurrently, each can successfully replace the file with its own merge of S; the later rename silently discards the other writer's freshly added key.
- No common paths.json lock encloses the read/merge/replace sequence in either writer.
- Save-CustomPaths correctly returns `$false` on persistence failure at :287-293.
- Ask-CustomPath at :297-305 ignores that return value: it mutates `$script:customPaths`, calls Save-CustomPaths, then unconditionally returns `$true`.
- The checkbox handler at :309-315 therefore leaves a target checked after a save failure, and the right-click handler at :327-335 can log that the folder was "set" even though persistence failed.
- Existing tests validate sequential preservation of the other writer's keys, not simultaneous read-modify-write or Ask-CustomPath failure propagation.
DEFECT:
paths.json has two acknowledged writers but only atomic replacement, not atomic update. Concurrent GUI/CLI saves can lose a valid preference while both calls report success. Separately, the GUI converts a known persistence failure into success and continues as if the chosen path were durable, creating session/disk source-of-truth drift.
REPAIR:
- Use one shared paths.json mutex/lock held across read -> merge -> temp write -> replace for BOTH GUI and CLI writers, or route both through one common serialized update primitive.
- Re-read the file only after acquiring the lock so the merge is based on the latest committed state.
- Keep temp+rename atomicity inside that lock.
- Make Ask-CustomPath propagate Save-CustomPaths failure. On failure either restore the previous in-memory value or explicitly retain it as session-only while returning failure; do not claim persistence.
- Ensure checkbox/log behavior reflects that failure.
VERIFY:
- Barrier two writer processes so both attempt simultaneous GUI-owned and CLI-owned updates; final paths.json must contain both changes and valid JSON.
- Repeat with the writers started in opposite order.
- Inject Save-CustomPaths failure; Ask-CustomPath must return false, the checkbox must not remain selected on the premise of a saved path, and no "folder set" success message may be emitted.
- Sequential preservation tests for foreign keys must remain green.

SECOND_WAVE_DONE_WHEN: completed Windows-theme Revert retires rather than deletes its active recovery baseline and the next epoch re-baselines correctly; OBS rejects corrupt recovery and round-trips case-insensitive INI keys; concurrent first epoch creation yields one persisted/returned identity; every post-snapshot mutation failure across representative targets restores exact pre-state or reports verified incomplete recovery before ledger drift; manifest rollback checks all native exits and preserves both commit/rollback errors; generic extension -WhatIf is strictly zero-write; paths.json writers are serialized and GUI save failures propagate truthfully; all relevant Node gates pass and the Windows PowerShell ownership/concurrency/WhatIf/recovery suites pass on a capable Windows runner, with CORE-004's independent release-gate failure resolved before declaring the full release pipeline green.

---
## 03 — AUDIT PERFORMANCE
PROJECT_NAME: Wintage
DATE_TIME: 2026-09-03T22:38:27+03:00
CAMPAIGN_PROFILE: quick3
CAMPAIGN_PROFILE_VERSION: 1.0.0
CAMPAIGN_RUN_ID: acb-mtlweqbo-058883880e4a4c109f61
CAMPAIGN_MANIFEST_SHA256: 97d9d053e37e6597a8559008911f84b28f799f326a97c69ebc5b304690defa72
WAVE_ID: performance
WAVE_INDEX: 3
WAVE_COUNT: 3
WAVE: AUDIT PERFORMANCE / STABILITY / EFFECTIVENESS
TARGET: /mnt/data/Wintage_03.09.26-T22-07-12.zip -> extracted project root /mnt/data/_audit_wintage_core
BASELINE: archive SHA256 fc1fb54fb7255391cf5a820bb932f0cd06535f60e158d7a42f0538bcb45453e9; Wintage userscript v1.30.0; raw main ref ffc13fd5065855239642a6d479806e6c14956814; AUDAPACK reports dirty=true with 64 changed files
PREVIOUS_WAVE_SHA256: NONE
GIT_CONTEXT: UNREADABLE - raw HEAD/refs identify main@ffc13fd5065855239642a6d479806e6c14956814 and origin/main at the same ref, but .git/objects is absent so git status/log/diff cannot verify object history
SAIPEN_CONTEXT: STALE - STATE/BOARD still center T-228 and "Nothing is shipped", while LOG records v1.30.0 shipped and later audit work; current dirty implementation contains partial PERF/CORE repair comments and code not coherently reflected in STATE/BOARD
AUDIT_SCOPE: userscript mutation intake, light/force sweep scheduler, shadow-root lifetime and memory bounds; Electron SCROLL_FIX/AD_BLOCK/WCO_FIX hot paths and document injection epochs; Electron Apply/Revert transaction snapshots and fuse scanning; WinForms target enumeration/dispatch/build validation; generated shim parity; performance/release regression gates
TEST_STATUS: TEST_FAILED
TEST_LIMITATION: Windows PowerShell is unavailable in the audit environment: command -v pwsh -> NOT_FOUND; command -v powershell -> NOT_FOUND. Therefore tests/Run-Tests.ps1 and Windows-only WinForms/ownership/process-count suites could not execute.
VERIFIED_INSTEAD: 18 current Node/build checks ran in a disposable copy: 17 exited 0 and tools/test-theme-switch.js failed with ReferenceError: IS_GOOGLE is not defined (CORE-004); import-fastprompter --check was among the exit-0 checks but explicitly SKIPPED freshness because no FastPrompter source was available. Fresh exact-current-source probes reproduced 192.1 MiB transaction RSS growth, 20,000-node mutation intake beyond the nominal 500-node cap, 501,500 SCROLL_FIX style reads for a 1,000-node nested batch, 499,500 AD_BLOCK overlapping-subtree visits, 12,000-node light-query materialization before a 2,500-node processing cap, two sweeps from one isolated light request, 100 WCO rAF callbacks/100,000 rect reads from one resize burst, repeated 64 MiB fuse scans reading 128 MiB over two unchanged probes, and duplicate same-document Electron injection after an in-page epoch bump
STATUS: PERFORMANCE: COMPLETE
TICKETS: 7
HANDOFF: IMPLEMENTATION_AGENT
COVERAGE_INSPECTED: wintage.user.js pierceShadow/onMutations/suspendRepainter/requestLightSweep/requestForceSweep/scheduleSweep/runSweeper/startSweeping; desktop/targets/electron/shim.cjs SCROLL_FIX/AD_BLOCK/WCO_FIX/web-contents-created injection lifecycle; generated desktop/out/electron shims via build parity gate; tools/install-electron.js installInPlace/captureRevertPreState/captureAppDir/snapshotPath/Revert paths; tools/electron-fuses.js scanner; desktop/modules/targets.ps1 Save-ElectronStateSnapshot; desktop/install.ps1 target listing/build gate/Electron dispatch; desktop/WintageInstaller.ps1 Load-Targets/Invoke-ChildPowerShell/Apply/Revert; tools/test-perf-bounded.js; tools/test-shim-payloads.js; tools/test-electron-state.js; release.ps1; package.json/package-lock.json; .saipen STATE/BOARD/LOG/ADRs; audit/1.md as prior orientation only
COVERAGE_DEFERRED: real Windows PowerShell/WinForms wall-clock and UI responsiveness profiling; real Chromium/Electron long-task and GC profiling; real installed-app binary/archive sizes and filesystem cache effects; external applications/services/endpoints; git history requiring missing object database
CROSS_WAVE_REFERENCES: CORE-001/CORE-002 and W2-004/W2-005 constrain PERF-001: memory/I/O reduction must preserve byte-exact failure recovery and truthful incomplete-rollback reporting. CORE-003 constrains PERF-002/PERF-003 scheduler changes around exclusion/suspension. W2-003/W2-007 constrain PERF-005 batching through shared epoch/path ownership and locking. CORE-004 independently keeps the current release gate red and must not be mistaken for a performance regression.
RESIDUAL_UNCERTAINTY: exact wall-clock savings depend on Windows process startup, filesystem cache and real renderer DOM/layout costs; event frequency for Electron in-page/frame-load sequences was not measured in a real external application. The reported work-count explosions, allocations, queue behavior and transaction RSS were reproduced directly from the current source.

[P1] [PERF-001] PROVEN BOTTLENECK tools/install-electron.js:captureRevertPreState/captureAppDir/installInPlace + desktop/modules/targets.ps1:Save-ElectronStateSnapshot
EVIDENCE:
- tools/install-electron.js:573-584 reads the complete live ASAR, ASAR backup, executable and fuse backup into Buffers in captureRevertPreState().
- Relocation Revert adds another complete `fs.readFileSync(movedAsar)` at :407.
- captureAppDir() at :634-642 recursively snapshots moved app.asar and app.asar.unpacked; snapshotPath() at :620-622 materializes every file as another Buffer.
- The same moved ASAR is therefore simultaneously represented by `pre.movedAsar` and by `appPre['app.asar'].buf`.
- Fresh exact-current-source relocation fixture with a 64 MiB moved app.asar plus 64 MiB app.asar.unpacked file produced:
  before RSS = 24.6 MiB;
  after snapshot RSS = 216.7 MiB;
  delta = 192.1 MiB;
  max RSS = 222,072 KiB;
  held large buffers = moved ASAR twice + unpacked file once.
- Independent captureRevertPreState fixture using 64 MiB live ASAR + 64 MiB backup + 32 MiB EXE + 32 MiB fuse backup held 201,326,592 bytes and raised RSS by 192.1 MiB.
- In-place Apply repeats the pattern: tools/install-electron.js:685 loads the complete ASAR into `preAsar`, while :694 also creates an on-disk `.bak` containing the same archive.
- The parent PowerShell transaction separately copies `app`, app.asar, app.asar.unpacked, EXE and fuse backup to disk at desktop/modules/targets.ps1:196-242 before invoking the child installer.
ISSUE:
Recovery is implemented as stacked whole-binary snapshots across both parent and child transaction layers. Relocation Revert can hold the same ASAR twice in RAM plus the complete unpacked tree and EXE recovery bytes while an independent full disk snapshot also exists. Memory and I/O scale with application size rather than with Wintage's tiny mutation set. Large Electron applications can therefore suffer hundreds of MiB of avoidable RSS and archive-sized duplicate I/O precisely during Apply/Revert, where memory pressure is least welcome.
OPTIMIZE:
- Make one durable on-disk transaction snapshot the recovery authority for large files.
- Snapshot presence, metadata and recovery paths in RAM; keep only small sidecars as Buffers.
- Remove the duplicate `pre.movedAsar` + `captureAppDir()['app.asar']` representation.
- For in-place Apply, restore from checked on-disk recovery instead of retaining a second full ASAR Buffer.
- Where parent recovery must span child + manifest/FreeBuff operations, allow the child transaction to reuse or reference that durable snapshot instead of independently materializing the same bytes.
- Use streaming/hash verification when byte identity must be checked.
GUARDRAIL:
- Do not weaken CORE-002/W2-004/W2-005 failure atomicity.
- Failed Apply/Revert must still restore ASAR, unpacked tree, pre-existing backup, EXE/fuses and sidecars byte-exactly.
- Recovery evidence must survive incomplete rollback.
- Never assume rename is atomic across volumes; copy/rename strategy must detect volume boundaries.
- Existing EBUSY/EPERM retry semantics must remain bounded and checked.
VERIFY:
- Add 64 MiB and 256 MiB ASAR/EXE/unpacked fixtures.
- Child-process peak RSS must remain approximately fixed as fixture size increases rather than growing roughly one byte of RSS per recovery byte.
- Assert no relocation path retains two copies of movedAsar.
- Exercise every existing Apply/Revert failure seam and byte-compare all restored large files.
- Parent manifest failure and child rollback failure cases must retain W2 exact/incomplete-recovery semantics.
- tools/test-electron-state.js, tools/test-fs-retry.js and the Windows transaction suites must remain green.

[P1] [PERF-002] PROVEN BOTTLENECK wintage.user.js:onMutations + desktop/targets/electron/shim.cjs:SCROLL_FIX/AD_BLOCK
EVIDENCE:
- wintage.user.js defines ADDED_NODE_BUDGET=500, but onMutations at :2523-2549 continues iterating every remaining `addedNodes` entry after the first 500; the overflow branch only stops retaining references.
- noteMutationPressure() counts MutationRecord objects, not added-node cardinality, so one record containing tens of thousands of nodes bypasses the mutation-record circuit breaker.
- Fresh execution of the exact current onMutations() with one 20,000-node childList produced:
  iteratedAddedNodes = 20,000;
  processCalls = 500;
  forceRequests = 1.
- SCROLL_FIX at shim.cjs:136-160 owns unbounded `trees`/`treeRoots`; observer :208-218 calls queueTree() once for every added node before its BUDGET=200 is enforced.
- queueTree() deduplicates only object identity, not ancestry, and nextTreeNode() uses `trees.shift()` at :166.
- Exact current SCROLL_FIX probe:
  20,000 flat added roots -> 20,001 additional getComputedStyle calls over 101 rAF frames.
  1,000 nested queued roots -> 501,500 getComputedStyle calls over 2,508 rAF frames.
- AD_BLOCK at shim.cjs:565-577 has the same overlapping-root shape: every added element is retained in a Set until rAF, then hideAds(root) performs a descendant query for every retained root.
- Exact current AD_BLOCK nested-1,000 probe performed 499,500 descendant visits in one queued rAF.
ISSUE:
The nominal budgets are applied after mutation intake and, in the Electron helpers, after overlapping roots have already been retained. Parent + descendant records therefore multiply subtree traversal toward O(n²). A legal framework hydration burst can generate thousands of consecutive animation-frame slices or one enormous AD_BLOCK frame while the code still claims bounded work.
OPTIMIZE:
- Userscript: stop consuming addedNodes once the intake budget is reached; mark overflow conservatively and request one force continuation. Use `addedNodes.length`/indexed access where available rather than iterating the tail only to discover it exists.
- SCROLL_FIX and AD_BLOCK: ancestor-collapse queued roots so a queued parent subsumes descendants.
- Put a hard cap on retained roots per mutation batch; represent overflow with one bounded continuation/root token rather than one Set/array entry per node.
- Replace `trees.shift()` with a head index/deque so queue removal is O(1).
- Reuse one common subtree-root coalescing helper if possible rather than maintaining three slightly different mutation batching contracts.
GUARDRAIL:
- Overflow must eventually converge over all relevant DOM.
- Stylesheet additions beyond the userscript cutoff must still trigger conservative style re-verification.
- SCROLL_FIX must retain its <=200 processed-elements-per-frame ceiling.
- AD_BLOCK must continue hiding advertisements inserted below any overflowed subtree.
- Do not replace bounded queues with synchronous `querySelectorAll('*')` over the entire document.
VERIFY:
- Userscript fixtures at 500/501/20,000/100,000 nodes must show intake visits bounded near the configured budget, not proportional to record size.
- SCROLL_FIX 20,000-flat must not allocate ~20,000 queue roots or consume ~100 frames merely draining intake.
- SCROLL_FIX 1,000-nested and AD_BLOCK 1,000-nested work counts must be near O(nodes), not ~500,000 subtree/style visits.
- Add overflow convergence tests proving the final DOM result matches an unbounded reference implementation.
- Extend tools/test-perf-bounded.js or a dedicated payload runtime gate so these algorithms cannot regress behind static comments again.

[P1] [PERF-003] PROVEN BOTTLENECK wintage.user.js:runSweeper/requestLightSweep/scheduleSweep light lane
EVIDENCE:
- Current runSweeper does correctly cap actual light processing with the shared FORCE_BUDGET=2500 at :2738-2784; the prior unbounded process-count defect is therefore partially repaired.
- Discovery is still unbounded: :2779 first executes `root.querySelectorAll('*:not([data-w95-done])')`, which materializes the complete matching NodeList before the 2,500-node processing limit is consulted.
- Exact current runSweeper(false) fixture with a 12,000-entry light query result produced:
  querySelectorCalls = 1;
  materializedMatches = 12,000;
  processCalls = 2,500;
  lightRequests = 1.
- Thus the advertised global processing budget does not bound selector traversal/result allocation.
- requestLightSweep() at :2713-2717 sets `lightPending=true`.
- scheduleSweep() executes runSweeper(false) at :2675 without first consuming the current light token.
- After that same pass returns, :2683-2687 sees the still-true token, clears it and schedules another light pass.
- Fresh exact scheduler-source probe for ONE isolated requestLightSweep() produced:
  timerCallbacks = 2;
  sweeps = 2.
- The redundant terminal pass executes the same full-root dirty selector even when the first pass completed all requested light work.
ISSUE:
The light lane now bounds expensive per-element processing but not discovery, and its pending-token accounting guarantees an unnecessary final pass. On large settled documents, "zero matching nodes" still requires the selector engine to search the roots. On dirty documents, every 2,500-element slice additionally materializes all remaining matches before discarding most of them.
OPTIMIZE:
- Represent light-dirty elements explicitly with a bounded Set/queue when data-w95-done is cleared, instead of rediscovering them through a document-wide negative selector.
- If overflow exceeds the dirty queue bound, promote once to the already-incremental force lane.
- Consume the light request that caused the current pass BEFORE runSweeper(false).
- After the pass, re-arm only if a NEW light request arrived during execution or bounded dirty work remains.
- If selector-based discovery is retained, replace full NodeList materialization with an incremental traversal that itself stops at the work budget.
GUARDRAIL:
- attrCooldown elements must still be revisited after their cooldown.
- Force debt keeps priority and must remain resumable under the one-second floor.
- A light request arriving DURING a light or force pass must not be lost.
- Overflow must converge without creating a perpetual timer.
- Preserve the existing no-idle-polling contract.
VERIFY:
- 12,000 dirty nodes: no slice may materialize/retain 12,000 matches merely to process 2,500.
- One isolated light request with <=budget work -> exactly one sweep.
- Multiple requests before the timer -> one coalesced sweep.
- A request arriving during a running sweep -> exactly one subsequent sweep.
- >budget dirty work drains over bounded slices with >=MIN_SWEEP_GAP and terminates with no pending timer.
- Measure selector/query counts on a settled large DOM; a light request should no longer require whole-document rediscovery.

[P1] [PERF-004] STRONGLY EVIDENCED WASTE wintage.user.js:piercedRoots/shadowObserver detached shadow-root lifetime
EVIDENCE:
- piercedRoots is a strong Set at wintage.user.js:1762.
- pierceShadow() adds each ShadowRoot at :1767-1768 and registers that root with the shared shadowObserver at :1772 onward.
- onMutations() contains no removedNodes handling at all.
- Fresh exact onMutations() removal-only fixture produced:
  forceRequests = 0;
  lightRequests = 0.
- Normal cleanup occurs only when a future runSweeper() happens at :2723.
- Even that cleanup only deletes the root from piercedRoots/forceRootCursors. It does NOT unregister the detached root from shadowObserver.
- Repository search shows shadowObserver.disconnect() only in permanent suspendRepainter() at :2418; MutationObserver has no per-target unobserve call in this implementation.
- Therefore a detached ShadowRoot can remain both strongly registered in piercedRoots until unrelated work and registered as an observed target even after Set pruning, until the entire shared observer is disconnected or the page dies.
ISSUE:
Long-lived component-heavy SPAs can accumulate detached shadow subtrees and observer registrations. The current Set pruning fixes only one retention owner and is event-dependent; quiet removal does not schedule cleanup, and shared-observer registration survives ordinary pruning. This can turn a zero-idle renderer into a steadily growing memory consumer.
OPTIMIZE:
- Detect removal-bearing childList batches and schedule a bounded shadow-registry cleanup, not a full DOM sweep.
- Prune disconnected roots by scanning piercedRoots, which is cheaper than recursively walking every removed subtree.
- When any observed root is retired, rebuild the shared shadowObserver registration set: preserve/take pending records as needed, disconnect it, then re-observe only currently connected roots.
- Alternatively use independently disconnectable per-root observers if measurement shows that model is cheaper, but do not create one without a lifecycle owner.
- Centralize the observer options so re-registration exactly matches initial registration.
GUARDRAIL:
- Do not add periodic polling.
- Do not drop pending legitimate shadow mutations during observer rebuild.
- Connected roots must remain observed.
- Temporarily moved/reconnected hosts must be able to re-register through pierceShadow.
- suspendRepainter must still disconnect everything and clear all strong registries.
VERIFY:
- Repeatedly attach/remove hundreds or thousands of shadow hosts using removal-only mutation batches.
- After cleanup, piercedRoots and forceRootCursors must contain no disconnected root.
- Instrument observer registration lifecycle so detached roots are no longer registered after cleanup.
- Reattach/recreate hosts and prove their shadow DOM is themed again.
- Heap/WeakRef-based browser regression, where supported, should show detached test trees becoming collectible without waiting for unrelated sweeps or page suspension.

[P1] [PERF-005] STRONGLY EVIDENCED WASTE desktop/WintageInstaller.ps1:Load-Targets/Apply/Revert + desktop/install.ps1 build/fuse status paths
EVIDENCE:
- WintageInstaller.ps1:1116-1126 launches one synchronous `powershell ... install.ps1 -Target <key>` process PER checked Apply target.
- Revert repeats the same one-process-per-target model at :1160-1169.
- Invoke-ChildPowerShell at :1022-1028 blocks until each child exits, so these processes execute serially on the WinForms click handler.
- The GUI defaults every selectable installed target to checked at :761-772, making multi-target dispatch the normal case rather than an edge case.
- Every explicit build-consuming target reruns `node tools/build-desktop.js --check` at desktop/install.ps1:543-556.
- The same script already demonstrates that repetition is unnecessary: `-Target all` performs that validation ONCE and caches the result at :558-563.
- Fresh local measurement: ten unchanged `node tools/build-desktop.js --check` executions took 0.92 s in this sandbox before adding any Windows PowerShell startup/import cost.
- The PowerShell surface reparsed by those children totals 4,359 lines across install.ps1/common.ps1/targets.ps1.
- Load-Targets synchronously starts another complete install.ps1 listing child at WintageInstaller.ps1:741-775. It runs at startup (:1223), after Apply (:1127), and after Revert (:1171).
- Each listing loops all five configured Electron targets. For each installed one, install.ps1:372-387 starts node electron-fuses.js and scans the executable until the fuse sentinel is found or EOF is reached.
- The fuse scanner is now memory-bounded, but has no unchanged-file result cache.
- Fresh current scanner probe against an unchanged 64 MiB no-sentinel executable called fuseVerdict twice:
  readCalls = 128;
  bytesRead = 134,217,728 (128.0 MiB).
ISSUE:
The GUI multiplies fixed process startup, module parsing, build validation and executable scanning by target count while blocking its own UI thread. A single "Apply selected apps" operation can pay the same build check repeatedly, then immediately pay a full discovery/fuse pass again. The bounded fuse scanner solved peak memory but not repeated startup I/O.
OPTIMIZE:
- Add a selected-target batch mode to install.ps1, reusing its existing `$names` dispatcher in one worker process.
- Reuse one build verification result across the selected batch exactly as `-Target all` already does.
- Return structured per-target PASS/FAIL/SKIP results so the GUI retains granular reporting.
- Run the single batch worker off the WinForms UI thread and marshal progress/status updates back to the UI.
- Batch Electron fuse status queries and cache each verdict in the GUI/listing lifetime by executable identity such as canonical path + size + LastWriteTimeUtc; invalidate immediately when any of those change.
- Do not persist a stale "safe" verdict across executable replacement without revalidation.
GUARDRAIL:
- Do not bypass build verification with -Force merely to make batching faster.
- Preserve per-target locks, manifest transactions and independent failure reporting.
- W2-003 epoch identity and W2-007 paths.json locking must remain correct in the new batch process.
- Fuse status stays fail-closed: an unreadable/changed executable must be rescanned, never accepted from an unverifiable cache key.
- One target failure must not falsely mark later targets successful or silently skip them.
VERIFY:
- With N selected build-consuming targets, instrument exactly one worker PowerShell startup and one build-desktop --check.
- Compare final manifests/files against the existing serial path for mixed PASS/FAIL/SKIP targets.
- Artificially slow one target and verify the WinForms window remains responsive.
- Two immediate Load-Targets refreshes with unchanged Electron executables should not re-read the same executable bytes.
- Changing size/mtime/path must invalidate fuse status and perform a fresh scan.
- Single-target CLI and `-Target all` behavior remain unchanged.

[P2] [PERF-006] PROVEN BOTTLENECK desktop/targets/electron/shim.cjs:WCO_FIX resize/geometrychange scheduling
EVIDENCE:
- WCO_FIX apply() at shim.cjs:684-715 scans `document.querySelectorAll(SEL)` and calls getBoundingClientRect() for every matching control.
- resize and geometrychange handlers at :721-722 independently execute `requestAnimationFrame(apply)` with no pending-frame latch.
- requestAnimationFrame does not coalesce separately queued callbacks by itself.
- Fresh exact current WCO_FIX probe with 1,000 matching controls and 100 resize events delivered before a frame produced:
  queuedRaf = 100;
  additionalRectReads = 100,000.
- Every callback measured the same final frame geometry; 99 scans were superseded before execution.
ISSUE:
Interactive resize/overlay geometry bursts can queue dozens of complete layout-reading scans for one rendered frame. The work scales as events × matching controls and can introduce visible resize jank even though only the latest geometry matters.
OPTIMIZE:
- Add one `frameQueued` latch and queueApply() helper.
- resize and geometrychange both call queueApply().
- Clear the latch immediately before apply() executes so an event arriving afterward can schedule the next rendered frame.
- Preserve the existing initial Apply and bounded settle passes separately.
GUARDRAIL:
- Do not replace rAF coalescing with a long debounce that makes controls visibly lag window movement.
- Latest geometry at frame execution time must win.
- Initial and three delayed settling passes must continue covering startup layout changes.
VERIFY:
- 100 same-frame resize events -> exactly one queued rAF and one additional control scan.
- Mixed resize + geometrychange burst -> one scan for that frame.
- Events spread across N rendered frames -> at most N event-driven scans.
- Final transform results must match the current implementation for the same final overlay rectangle.

[P2] [PERF-007] STRONGLY EVIDENCED WASTE desktop/targets/electron/shim.cjs:web-contents-created injection epoch
EVIDENCE:
- The webContents injection state at shim.cjs:825-834 uses `injectedEpoch` as the document identity and skips only when `injectedFor === injectedEpoch`.
- A full did-navigate correctly increments the epoch at :870.
- did-navigate-in-page ALSO increments it for main-frame same-document navigation at :871.
- dom-ready/did-finish-load/did-frame-finish-load all call inject() at :863-866.
- Once the in-page event increments the epoch, any later frame-finish event causes the same top renderer to be treated as a fresh uninjected document.
- Exact current handler state-machine probe:
  initial dom-ready + did-finish-load + did-frame-finish-load -> inserts=1, generic executeJavaScript calls=4;
  main-frame did-navigate-in-page followed by did-frame-finish-load -> inserts=2, executeJavaScript calls=8;
  subsequent true did-navigate + dom-ready -> inserts=3, executeJavaScript calls=12.
- insertCSS() stores only the newest key in `wc.__wintageCssKey` at :856-857.
- Repository search found no removeInsertedCSS call, so the previous same-document injected stylesheet is not explicitly retired when the key is replaced.
ISSUE:
Same-document SPA/history navigation is modeled as a new document epoch. A later frame-load trigger can therefore stack another stylesheet and repeat IPC/promise/status work in the same renderer. Renderer-side JavaScript latches reduce the functional damage, but duplicate CSS remains possible and the sole stored removal key advances to the newest insertion.
OPTIMIZE:
- Increment injection epoch only for true top-level document replacement.
- Keep did-navigate for same-URL full reload correctness.
- Do not increment the document epoch on did-navigate-in-page.
- If intentional same-document reinjection is ever added, remove the previous inserted stylesheet by its key before installing a replacement and only replace the stored key after success.
GUARDRAIL:
- Ctrl+R/application-triggered reload to the identical URL must still inject exactly once into the new document.
- Child-frame events must not manufacture or consume a top-document epoch.
- Every WebContents/BrowserWindow/WebContentsView keeps independent injection state.
- DevTools exclusion remains unchanged.
VERIFY:
- Initial document's dom-ready/did-finish-load/frame-finish sequence -> one insertion.
- Main-frame did-navigate-in-page followed by child-frame completion -> still one insertion and no repeated generic payload execution.
- Same-URL full did-navigate -> exactly one new insertion for the replacement document.
- Repeated real reloads -> one insertion per document.
- Assert no unrecoverable same-document CSS keys accumulate.
- Add this state-machine regression to tools/test-shim-payloads.js or a dedicated Electron injection-lifecycle test.

PERFORMANCE_DONE_WHEN: Electron transaction memory no longer scales with whole ASAR/EXE/unpacked recovery bytes while CORE/W2 exact rollback remains intact; userscript/SCROLL_FIX/AD_BLOCK mutation intake is bounded before arbitrary node retention and nested roots no longer multiply traversal; light discovery itself is bounded and one isolated light request yields one pass; detached shadow roots are removed from both strong registries and observer registrations without polling; multi-target GUI work reuses one worker/build verification and unchanged fuse state without blocking WinForms; WCO performs at most one event-driven geometry scan per rendered frame; same-document Electron navigation cannot stack insertCSS payloads while real reload still reinjects once; exact-source large-binary RSS, 20k/100k mutation, nested-root, 12k-light, shadow lifecycle, process/build/fuse-count, WCO burst and injection-epoch regressions pass; existing Node gates pass including repaired CORE-004 theme-switch test, and the complete Windows PowerShell/WinForms suite passes on a capable Windows runner.

---
ALL_3_DONE_WHEN: 3/3 waves validated and combined into canonical handoff package.