# Wintage — Audit Handoff

RUN_ID: acb-mtf4qjtn-6afcd4a2d692402d9762
GENERATED_AT: 2026-08-30T04:53:49.473894
PROJECT_NAME: Wintage
TARGET: Wintage_30.08.26-T04-25-36.zip, fully unpacked and inspected at the archive project root
BASELINE: archive SHA256 9b9ea5186c368c39b9b36f8d518398cf34668d9b5c7d2adf212f38c123f0f8d0; embedded Git worktree main@a9399dc9d053b0cd333e0c343fbe6eed26d40185 with supplied uncommitted changes
GIT_CONTEXT: PRESENT main@a9399dc9d053b0cd333e0c343fbe6eed26d40185 - origin/main resolves to the same commit; supplied archive worktree is dirty with tracked, deleted and untracked state, so live archive files rather than HEAD alone were audited
SAIPEN_CONTEXT: STALE - .saipen is present and records v1.29.0/T-223 historical fixes and prior green gates, but its current SC-1 BLOCKED state concerns legacy saitest OUTBOX evidence and does not describe the newer supplied dirty worktree; implementation files remained authoritative
TOTAL_TICKETS: 22
STATUS: AUDIT_ALL_3: COMPLETE

## Summary of Wave Audits
- Wave 1 (Core): 8 tickets (baseline: archive SHA256 9b9ea5186c368c39b9b36f8d518398cf34668d9b5c7d2adf212f38c123f0f8d0; embedded Git worktree main@a9399dc9d053b0cd333e0c343fbe6eed26d40185 with supplied uncommitted changes)
- Wave 2 (Second Wave): 6 tickets (baseline: archive SHA256 9b9ea5186c368c39b9b36f8d518398cf34668d9b5c7d2adf212f38c123f0f8d0; embedded dirty worktree main@a9399dc9d053b0cd333e0c343fbe6eed26d40185)
- Wave 3 (Performance): 8 tickets (baseline: archive SHA256 9b9ea5186c368c39b9b36f8d518398cf34668d9b5c7d2adf212f38c123f0f8d0; embedded dirty worktree main@a9399dc9d053b0cd333e0c343fbe6eed26d40185)

---
## 01 — AUDIT CORE
PROJECT_NAME: Wintage
DATE_TIME: 2026-08-30T04:34:34+03:00
WAVE: AUDIT CORE
TARGET: Wintage_30.08.26-T04-25-36.zip, fully unpacked and inspected at the archive project root
BASELINE: archive SHA256 9b9ea5186c368c39b9b36f8d518398cf34668d9b5c7d2adf212f38c123f0f8d0; embedded Git worktree main@a9399dc9d053b0cd333e0c343fbe6eed26d40185 with supplied uncommitted changes
GIT_CONTEXT: PRESENT main@a9399dc9d053b0cd333e0c343fbe6eed26d40185 - origin/main resolves to the same commit; supplied archive worktree is dirty with tracked, deleted and untracked state, so live archive files rather than HEAD alone were audited
SAIPEN_CONTEXT: STALE - .saipen is present and records v1.29.0/T-223 historical fixes and prior green gates, but its current SC-1 BLOCKED state concerns legacy saitest OUTBOX evidence and does not describe the newer supplied dirty worktree; implementation files remained authoritative
AUDIT_SCOPE: entry/state map across wintage.user.js theme/menu/host handling; desktop/install.ps1 target dispatch, persistent recovery, WhatIf and manifest transactions; desktop/modules/common.ps1 and targets.ps1 state ownership/rollback; tools/install-electron.js classification, fuse handling, relocation/in-place apply, repaint and revert transactions; build/release gates and focused regression harnesses. Final handoff follows the attached AUDIT CORE contract. :contentReference[oaicite:0]{index=0}
TEST_STATUS: TEST_FAILED
TEST_LIMITATION: PowerShell test runner unavailable in this execution environment: `pwsh -NoProfile -File tests/Run-Tests.ps1` could not start and returned exit 127 with `bash: line 1: pwsh: command not found`; Windows/PowerShell-only verification therefore could not execute here
VERIFIED_INSTEAD: `node --check` syntax checks completed without reported syntax failures; `node tools/test-electron-shim.js`, `test-repainter-polarity.js`, `test-shim-payloads.js`, `test-terminal-font.js`, `test-theme-packs.js`, `build-desktop.js --check`, `check-css.js`, and `check-wiki-mirror.js` passed; `node tools/test-electron-state.js` executed and produced 6 failures; `node tools/test-theme-switch.js` executed and crashed with the ticketed ReferenceError; a focused real tools/install-electron.js relocation/revert failure fixture also reproduced archive loss
STATUS: AUDIT_CORE: COMPLETE
TICKETS: 8
HANDOFF: IMPLEMENTATION_AGENT

[P0] [CORE-001] tools/install-electron.js / themed-relocated --revert rollback
EVIDENCE: In the `themed-relocated` revert branch, lines 356-358 snapshot the root archive and only four small app/ files. The live moved archive is read separately into `pre.movedAsar`; lines 363-366 rename `app/app.asar` back to root and then recursively delete `app/`. On failure, lines 371-373 attempt `fs.writeFileSync(movedAsar, pre.movedAsar)` before recreating the deleted `app/` parent, silently swallow that write failure, and then unlink the root `app.asar`. `captureAppDir()` at lines 534-540 does not snapshot `app.asar` or `app.asar.unpacked`, so `restoreAppDir()` cannot recover them. A focused real-tool fixture performed a successful relocation apply and then `--revert` with `WINTAGE_TEST_FAIL_AFTER_REVERT=1`; the command exited nonzero claiming exact restoration, but afterward both root `app.asar` and `app/app.asar` were absent and the recreated app/ contained only shim/package/CSS state.
DEFECT: A failed relocation revert can destroy the only application archive while claiming that the exact themed pre-operation state was restored. `app.asar.unpacked` has the same ownership hole. This is direct local application/data loss and can leave the Electron target unusable.
REPAIR: Make relocation revert transactional around the whole relocated app directory. Preserve the original app/ directory or rename it to a rollback location until root archive/unpacked restoration and verification have committed. On failure, restore root archive/unpacked back into that preserved directory before restoring the directory name and fuse state. Do not unlink any surviving root archive until restoration of its relocated counterpart has succeeded. Never swallow rollback filesystem failures or print the exact-restoration claim unless archive, unpacked directory, app/ payload and fuse state all match the captured pre-state.
VERIFY: Extend tools/test-electron-state.js with relocation-mode `WINTAGE_TEST_FAIL_AFTER_REVERT` cases both with and without `app.asar.unpacked`. After the injected failure assert nonzero exit, byte-identical relocated `app/app.asar`, exact unpacked-tree restoration, original app/ sidecars/package, original fuse/exe state, absence of an unintended root archive, and no success/exact-restoration message unless every invariant is true. Then assert a subsequent clean revert succeeds.

[P0] [CORE-002] desktop/install.ps1 / VS Code + Antigravity extension recovery revert
EVIDENCE: desktop/install.ps1 lines 831-840 read persistent recovery metadata. Only the combination `mode == 'replaced'` AND an existing pristine directory takes the restoration branch. Every other recovery record enters lines 838-840, recursively deletes `$dest`, and labels it “Wintage-created”. Therefore a valid `mode='replaced'` ledger whose pristine directory was deleted, corrupted, or never captured is treated exactly like `mode='created'`. The same project already demonstrates the required fail-closed invariant for BetterDiscord at desktop/modules/targets.ps1 lines 1711-1712: `mode='replaced'` with missing pristine recovery throws rather than deleting the live target.
DEFECT: Persistent metadata explicitly records that the destination pre-existed, but loss of its pristine snapshot flips revert semantics from “restore user directory” to “delete directory”. A partial/corrupt recovery state can therefore cause destructive deletion of pre-Wintage extension contents.
REPAIR: Branch on recovery `mode` first. For `replaced`, require a valid pristine recovery directory and throw without touching `$dest`, the manifest, or recovery metadata when it is missing. Only `mode='created'` may delete `$dest` without restoring prior contents. Reject unknown mode values instead of treating them as created.
VERIFY: Add PowerShell regression fixtures for `replaced + pristine present`, `replaced + pristine missing`, `created`, and unknown mode. The missing-pristine/unknown cases must fail nonzero and preserve destination bytes, manifest entry and recovery evidence exactly; only the created case may remove the destination.

[P1] [CORE-003] desktop/install.ps1 / first-touch extension recovery capture under -WhatIf
EVIDENCE: desktop/install.ps1 lines 870-882 create the persistent first-touch recovery state before the explicit `$PSCmdlet.ShouldProcess(...)` gate at line 884. `New-Item` and `Copy-Item` participate in PowerShell WhatIf semantics, but line 881 calls project helper `Write-Utf8` directly. desktop/modules/common.ps1 line 28 implements that helper with `[System.IO.File]::WriteAllText` and contains no `$WhatIfPreference` guard; by contrast, `Write-Manifest` explicitly returns on `$WhatIfPreference` at lines 169-170. With an existing destination, a dry run can therefore skip the pristine-copy mutations yet still write `recovery.json` saying `mode='replaced'`; with a nonexistent recovery directory the direct write can instead make the dry run fail after its surrounding WhatIf-aware operations were suppressed.
DEFECT: `-WhatIf` is not side-effect-free for VS Code/Antigravity first-touch recovery and can create a recovery ledger claiming that a pristine snapshot exists when it was never captured. That poisoned state feeds directly into destructive revert logic.
REPAIR: Move the entire first-touch persistent-recovery transaction under the same successful ShouldProcess/apply path, or explicitly short-circuit it when `$WhatIfPreference` is set. Write recovery metadata only after pristine capture has completed successfully. Prefer an atomic helper that establishes directory/snapshot/metadata as one transaction and removes partial artifacts on failure.
VERIFY: On supported PowerShell, run first apply with `-WhatIf` for both an absent destination and a pre-existing destination. Hash/compare destination, recovery root and manifest before/after and require zero changes and exit success. Then perform a real apply and verify `mode='replaced'` is never written unless the pristine snapshot exists and matches the original directory.

[P1] [CORE-004] tools/install-electron.js / installRelocation successful-rollback catch path
EVIDENCE: installRelocation catches swap failures at lines 714-759. When reverse operations and fuse restoration satisfy `rollbackOk`, lines 732-734 merely remove staging/oldReloc and then fall through out of the catch. Execution continues to lines 762-765, prints `installed palette ...` and exits 0. The canonical `node tools/test-electron-state.js` run reproduced this: `partial-move failure exits NONZERO`, `WINTAGE_TEST_FAIL_AFTER_STAGING_MOVE`, `WINTAGE_TEST_FAIL_AFTER_ASAR_MOVE`, `WINTAGE_TEST_FAIL_UNPACKED_MOVE`, and `WINTAGE_TEST_FAIL_VERIFY` all reported `got=false want=true`; the suite ended with 6 failures.
DEFECT: A relocation installation that throws after live-state mutation is reported as a successful installation whenever rollback itself succeeds. Callers can consequently commit manifest/runtime state for an operation that was deliberately rolled back, while logs falsely tell the user the palette was installed.
REPAIR: A successfully rolled-back failed operation must still terminate nonzero. After verified rollback, remove only disposable transaction leftovers and immediately `die(...)` with a truthful “apply failed; pre-operation state restored” result. The normal installed-message/exit-0 path must be reachable only when the original try block reaches commit without an exception.
VERIFY: The existing failure-seam assertions in tools/test-electron-state.js must all report nonzero while continuing to prove byte-exact pre-state restoration. Additionally assert stdout/stderr for every seam contains no `installed palette` success message.

[P1] [CORE-005] tools/install-electron.js / installRelocation updated-relocated rollback validation
EVIDENCE: Before an update reapply, `isUpdate` means the pre-operation app/ directory is intentionally Wintage-owned. The rollback undo at lines 687-690 renames that existing app/ to `oldReloc` and records an undo that restores it. After a failure, however, lines 727-730 define `originalAppDirRestored` as true only when app/ is absent or its package has `wintage !== MARKER`. Thus a correctly restored Wintage-owned pre-state is classified as rollback failure. The canonical suite confirms the mismatch: `relo AFTER_OLD_RETIRE: exits NONZERO`, root archive restoration and old moved archive restoration all PASS, but `relo AFTER_OLD_RETIRE: no staging/old leftovers` FAILS because the false rollback-incomplete classification preserves transaction debris.
DEFECT: Rollback verification checks a hardcoded stock-state expectation instead of the actual pre-operation state. Updated-relocated failures therefore cannot be recognized as exact rollbacks even when the old relocation was correctly restored, leaving unnecessary recovery directories and misleading failure diagnostics.
REPAIR: Capture the relocation pre-state before mutation, including whether app/ existed, whether it was Wintage-owned, and the expected archive layout. Validate rollback against that captured state rather than `wintage !== MARKER`. A restored prior Wintage relocation is correct for update/reapply and should permit cleanup of transaction-only staging paths.
VERIFY: Keep the existing AFTER_OLD_RETIRE fixture and require all four assertions to pass, including no `.wintage-*` leftovers. Add an injected failure after each update-swap stage and compare root archive, old relocated archive/package/palette and fuse state against the captured pre-operation values.

[P1] [CORE-006] tools/install-electron.js / rollback helpers and unchecked exact-restoration claims
EVIDENCE: `restoreSidecars()` at lines 472-479 catches and discards every write/unlink failure and returns no result. `restoreRevertPreState()` was partially converted to return a `failures` array at lines 504-532, but line 519 calls `restoreSidecars(pre.sidecars)` without collecting sidecar failures. Several revert catch sites still ignore returned rollback failures entirely: updated/stock in-place at lines 345-347, themed-relocated at 369-378, and updated-relocated at 394-397, then unconditionally call `die(... restored the exact pre-operation state.)`. The in-place apply catch at lines 609-618 likewise swallows archive-write failure, uses unchecked `restoreSidecars`, ignores `restoreFuseIfDefused`, and makes the same exact-restoration claim.
DEFECT: The current checked-rollback refactor is incomplete end-to-end. Filesystem failures during the rollback itself can leave archive, sidecar, app-directory or fuse state torn while the command reports exact recovery, defeating both diagnosis and safe retry behavior.
REPAIR: Give every rollback primitive one explicit success/failure contract and aggregate it at every caller. Include sidecar writes/unlinks, archive restoration, app-directory restoration and fuse restoration. Only consume/delete recovery artifacts after all required rollback operations and post-state checks succeed. Any failed rollback must report `rollback INCOMPLETE`, list the failed phase/path, preserve surviving recovery material and never emit an exact-restoration statement.
VERIFY: Add deterministic failure-injection or mocked-fs cases for rollback-time archive write, sidecar write/unlink, app-dir recreation and fuse restore. Each must return nonzero, retain the relevant recovery artifact, identify rollback as incomplete and avoid the exact-restoration claim. Existing clean rollback tests must remain green.

[P1] [CORE-007] desktop/modules/targets.ps1 / Restore-DirPreState snapshot lifecycle
EVIDENCE: Save-DirPreState lines 707-712 captures an existing directory into the only byte-preserving temporary snapshot. Restore-DirPreState lines 729-733 copies that snapshot to a temp sibling, removes the live destination with `-ErrorAction SilentlyContinue`, and renames the temp into place. If removal/rename fails, lines 734-736 rethrow, but the `finally` block at lines 737-739 unconditionally deletes the original captured snapshot even on failure. Additionally, when `Existed` is true but `SnapshotPath` is missing, line 725 silently returns rather than signaling that exact restoration is impossible.
DEFECT: The rollback routine does not preserve its recovery source until restoration has committed. A failed restore can destroy the only pristine snapshot, and an already-missing snapshot can be treated as successful no-op, so higher transaction layers cannot reliably distinguish exact recovery from lost recovery evidence.
REPAIR: Treat `Existed=true` as requiring a readable SnapshotPath. Throw if it is absent. Consume the original snapshot only after the replacement directory has been installed and verified successfully. Do not silently suppress failure to remove the live destination when that prevents the swap; on restoration failure preserve the original snapshot and any useful staged copy for recovery.
VERIFY: Extend tools/test-dir-prestate.ps1 with forced destination-removal/rename failure and missing-SnapshotPath cases. Require a thrown failure and an intact original snapshot after failed restore; require successful restores to consume the snapshot only after destination content is verified byte-equivalent; first-install `Existed=false` behavior must remain unchanged.

[P2] [CORE-008] tools/test-theme-switch.js / release-gated userscript source-slice harness
EVIDENCE: wintage.user.js now declares host flags at lines 67-68 and the source region later executed by the theme test contains `if (IS_X)` and `if (IS_REDDIT)` at lines 354-355. tools/test-theme-switch.js slices from `const THEMES` to `const W95_VERSION` at lines 7-12, so those new statements are included, but its VM contexts at lines 42-61 and 117-128 provide `IS_TOP` without defining `IS_X` or `IS_REDDIT`. The actual command `node tools/test-theme-switch.js` exits 1 immediately with `ReferenceError: IS_X is not defined` at the injected source slice. release.ps1 line 125 directly executes this test, making the stale harness a release-gate failure rather than a product-runtime failure.
DEFECT: The behavioural harness no longer supplies all globals required by the real source slice it deliberately executes. A valid host-specific userscript change therefore breaks the release gate before the theme assertions run.
REPAIR: Keep the harness grounded in the real source, but supply the source slice's host-state dependencies in each VM context, at minimum `IS_X=false` and `IS_REDDIT=false`, or expand the evaluated source/context so those flags are derived from a stubbed hostname. Prefer adding explicit X/Reddit cases if the data attributes are meant to remain covered.
VERIFY: `node tools/test-theme-switch.js` must exit 0 and execute its full existing assertion set. Add focused contexts proving X sets `data-w95-x`, Reddit sets `data-w95-reddit`, and ordinary hosts set neither, without weakening the theme/menu/storage regressions the gate already protects.

CORE_DONE_WHEN: CORE-001 through CORE-008 are repaired without broad redesign; relocation apply/revert failure seams return truthful nonzero results and preserve byte/layout/fuse pre-state; `mode=replaced` recovery can never delete a live directory without pristine evidence; `-WhatIf` creates no recovery/manifest/payload state; directory rollback preserves its snapshot until successful commit; `node tools/test-electron-state.js` and `node tools/test-theme-switch.js` pass; the focused Electron rollback regressions pass; and the complete `tests/Run-Tests.ps1` plus release JS gates pass in a supported Windows PowerShell environment.

---
## 02 — AUDIT SECOND WAVE
PROJECT_NAME: Wintage
DATE_TIME: 2026-08-30T04:44:04+03:00
CAMPAIGN_PROFILE: quick3
CAMPAIGN_PROFILE_VERSION: 1.0.0
CAMPAIGN_RUN_ID: acb-mtf4qjtn-6afcd4a2d692402d9762
CAMPAIGN_MANIFEST_SHA256: c01ec812cf5952fdab101d5e7bc83c8251e0d9c21bb0ab06e426f5d58baedff2
WAVE_ID: second
WAVE_INDEX: 2
WAVE_COUNT: 3
WAVE: AUDIT SECOND WAVE
TARGET: Wintage_30.08.26-T04-25-36.zip, unpacked project root /mnt/data/wintage_audit
BASELINE: archive SHA256 9b9ea5186c368c39b9b36f8d518398cf34668d9b5c7d2adf212f38c123f0f8d0; embedded dirty worktree main@a9399dc9d053b0cd333e0c343fbe6eed26d40185
PREVIOUS_WAVE_SHA256: NONE
GIT_CONTEXT: PRESENT main@a9399dc9d053b0cd333e0c343fbe6eed26d40185 - origin/main resolves to the same commit; supplied archive contains uncommitted working-tree changes, so live files were treated as authoritative
SAIPEN_CONTEXT: STALE - .saipen is present and records shipped v1.29.0/T-223 history plus a current BLOCKED state around unrelated saitest OUTBOX evidence; it predates or does not fully describe the supplied dirty implementation
AUDIT_SCOPE: lifecycle/boundary/repeatability audit across Electron classification, fuse ownership, update/reapply/revert generations, unpacked-archive topology, PowerShell parent transactions and snapshots, paths.json multi-writer persistence, persistent extension/browser recovery cleanup, manifest/target locking and focused failure paths
TEST_STATUS: TEST_FAILED
TEST_LIMITATION: Windows PowerShell verification unavailable in this sandbox because neither pwsh nor powershell is installed; tests/Run-Tests.ps1 and Windows-native fixtures therefore could not execute
VERIFIED_INSTEAD: real tools/install-electron.js fixtures reproduced stale-fuse generation rollback and app.asar.unpacked topology misclassification; node tools/test-electron-state.js ran and ended with 6 failures corresponding to prior CORE rollback defects; node tools/test-theme-switch.js ran and failed with ReferenceError: IS_X is not defined; test-electron-shim.js, test-repainter-polarity.js, test-shim-payloads.js, test-terminal-font.js and test-theme-packs.js passed
STATUS: SECOND_WAVE: COMPLETE
TICKETS: 6
HANDOFF: IMPLEMENTATION_AGENT
COVERAGE_INSPECTED: tools/install-electron.js classifyState/health/status/repaint/install/revert/fuse paths; tools/electron-fuses.js defuse backup ownership; desktop/modules/common.ps1 Read-PathsJson/Save-PathPreference/manifest and target locks; desktop/WintageInstaller.ps1 Save-CustomPaths; desktop/modules/targets.ps1 Save/Restore-ElectronStateSnapshot and Save/Restore-DirPreState; desktop/install.ps1 browser stage and VS Code/Antigravity recovery transactions; relevant Node regression tests and release-facing test gates
COVERAGE_DEFERRED: performance-only scheduler/repaint cost analysis for wave 3; purely visual CSS/palette correctness; localization prose; exhaustive registry/Windows shell behavior and real installed-application launches requiring Windows/PowerShell
CROSS_WAVE_REFERENCES: CORE-001/CORE-004/CORE-005/CORE-006 remain the primary Electron rollback-integrity defects; CORE-007 covers Restore-DirPreState failure-path snapshot destruction while W2-006 below covers distinct successful-operation snapshot leakage; CORE-008 remains the failing theme-switch harness. W2 tickets below do not duplicate those root causes.
RESIDUAL_UNCERTAINTY: PowerShell findings are statically verified against concrete read/write/lock/cleanup paths but could not be exercised under Windows in this sandbox; W2-001 and W2-002 were independently reproduced through the real Node installer; the canonical Electron state suite remains red because prior CORE defects are still present

[P0] [W2-001] tools/electron-fuses.js:defuse / tools/install-electron.js:restoreFuseBackupForRevert
EVIDENCE: tools/electron-fuses.js lines 127-139 creates `<exe>.wintage-fuse.bak` only when that file does not already exist and explicitly treats the existing backup as permanent "INSTALL-EPOCH authority". tools/install-electron.js lines 406-414 later performs a real revert by blindly copying that backup over the current executable and deleting the backup. Updated-inplace/updated-relocated reapply paths can encounter a genuinely newer executable after the application itself updates, but ensureDefused/defuse deliberately refuses to refresh the old backup. A focused real installer fixture established generation V1, applied Wintage, replaced the stock archive and executable with V2 to simulate an application update, reapplied successfully, then reverted. Observed checks were: `backup-is-v1 true`, `after-reapply-backup-still-v1 true`, `final-exe-is-v1 true`, `final-exe-is-v2 false`, while the restored archive reported application version `2.0.0`.
DEFECT: Fuse recovery ownership is bound to the first Wintage installation epoch rather than to the current application generation. After an application update, successful reapply can pair a V2 archive with a stale V1 fuse backup; later revert overwrites the current V2 executable with V1 bytes. This is a cross-generation destructive rollback capable of producing an internally mismatched or unusable application.
REPAIR: Bind fuse backup state to the current executable/application generation. When an updated-inplace or updated-relocated state proves that a new stock application generation has arrived, transactionally rebase the fuse recovery source from that current pristine executable before modifying its fuses. Store or otherwise validate an executable hash/generation identity so an old backup is never restored over a different generation. Rebase must itself participate in rollback: a failed reapply must restore the exact pre-operation executable and previous recovery metadata/backup rather than consuming either generation accidentally.
VERIFY: Add both relocation and in-place V1 -> app update V2 -> Wintage reapply -> revert fixtures. Require the final executable after revert to be byte-identical to pristine V2, the archive/unpacked tree to be V2, and no V1 recovery bytes to survive as authoritative state. Inject failures before and after backup rebasing and require exact restoration of the pre-operation executable plus whichever backup generation was authoritative before the attempt.

[P1] [W2-002] tools/install-electron.js:classifyState / health / repaintRelocation
EVIDENCE: tools/install-electron.js lines 83-85 explicitly models `app.asar.unpacked` and its relocated counterpart `app/app.asar.unpacked`, and the file-level contract states that unpacked content must move with its archive because separating them breaks native modules. However classifyState lines 185-194 classifies relocation solely from root app.asar, Wintage ownership of app/, and moved app/app.asar; it never checks the unpacked pair. requiredSidecars/health lines 225-275 likewise checks only package.json, shim.cjs and wintage.css. A real fixture with valid moved `app/app.asar`, valid Wintage sidecars, NO moved `app/app.asar.unpacked`, and an orphan `resources/app.asar.unpacked/native.node` at the root returned `state:"themed-relocated"` and `healthy:true`. Running the normal palette apply then exited 0 through the repaint path; afterward the orphan root unpacked directory still existed and the moved unpacked directory still did not, while status remained healthy.
DEFECT: The structural state machine omits one half of Electron's archive-layout invariant. A runtime-broken relocation can therefore be advertised as healthy and routed to lightweight repaint, which cannot repair the misplaced unpacked tree. Native modules/resources expected beside the moved archive can remain broken indefinitely despite successful status and apply results.
REPAIR: Include archive/unpacked topology in relocation classification and health. Absence of any unpacked directory must remain valid for applications that do not have one. When one exists, it must be paired with the archive generation it belongs to. Root-unpacked + moved-archive, moved-unpacked + root-only archive, or duplicate root/moved unpacked layouts must become explicit unhealthy/ambiguous states unless generation ownership proves a safe transactional repair. Reapply may move an orphan only when ownership/current-generation evidence makes that operation unambiguous; otherwise fail closed rather than repainting.
VERIFY: Add status/apply fixtures for: archive with no unpacked directory; correct relocated archive+unpacked pair; root orphan unpacked with moved archive; duplicate root+moved unpacked trees; app-update state containing new root archive+unpacked plus stale old relocation. Healthy must be true only for consistent layouts, and any automatic repair must preserve the correct generation byte-for-byte under injected failure.

[P2] [W2-003] desktop/modules/common.ps1:Save-PathPreference / desktop/WintageInstaller.ps1:Save-CustomPaths
EVIDENCE: Save-PathPreference lines 495-511 performs a full read -> merge -> temporary write -> Move-Item replacement of paths.json. Save-CustomPaths lines 235-281 independently performs the same read -> merge -> temporary write -> Move-Item sequence. GUI comments at lines 239-245 explicitly acknowledge that paths.json has two writers with disjoint ownership. Neither writer acquires a common interprocess mutex around the read-modify-write transaction. By contrast, common.ps1 lines 192-290 correctly explains and solves the identical problem for manifest.json: atomic replacement protects file integrity but not concurrent read-modify-write, so manifest writers hold a named mutex across the whole transaction. With paths.json, GUI and CLI can both read state S0, independently add disjoint keys, then whichever Move-Item executes last replaces the first writer's newer state with its stale S0-based merge.
DEFECT: paths.json has multiple acknowledged writers but only atomic file replacement, not transaction serialization. Concurrent GUI/CLI/logon-driven saves can deterministically lose a valid preference written by another process without producing corrupt JSON or any error, leaving configuration history dependent on write ordering.
REPAIR: Introduce one shared named paths-preference mutex derived from the canonical Wintage app-data/paths.json identity and use it in BOTH Save-PathPreference and Save-CustomPaths. Hold it across read, parse, merge, serialization and atomic replacement. Handle abandoned mutex ownership the same way as manifest locking and fail closed on timeout. Preserve existing per-key ownership rules while serializing the transaction; do not solve this by discarding one writer's keys.
VERIFY: Add a PowerShell concurrency regression using two processes synchronized behind a barrier: one GUI-equivalent writer changes a PATH_TARGETS key while one CLI writer changes codenomad/workbuddy/portable. Repeat enough times to exercise both final-write orders. Every completed run must retain both updates, produce valid JSON, leave no temp files and recover correctly from an abandoned lock.

[P2] [W2-004] desktop/modules/common.ps1:Save-PathPreference / desktop/WintageInstaller.ps1:Save-CustomPaths parse-failure handling
EVIDENCE: Read-PathsJson lines 39-58 treats malformed preferences as unusable read-only input, warns, and returns an empty map. Mutation behavior is more destructive. Save-PathPreference lines 499-504 catches any read/ConvertFrom-Json failure with an empty catch, proceeds with a new ordered object, adds only the requested key, and replaces the existing paths.json. Save-CustomPaths lines 249-260 also catches unreadable JSON, explicitly treats it as carrying "nothing worth preserving", and then writes current GUI keys over the file. Thus malformed existing bytes are not merely ignored for the current lookup; the next unrelated preference save destroys them and every recoverable/unknown value they contained.
DEFECT: Parser failure is asymmetrical between read and write paths. Mutation interprets "could not parse existing state" as "existing state is empty", converting a recoverable configuration corruption into irreversible configuration loss and erasing forensic/recovery evidence.
REPAIR: Fail closed when an existing paths.json cannot be parsed as the expected object. Do not replace, truncate or normalize the original file on that save. Surface a useful save failure to the GUI/CLI while preserving the bytes. Validate the parsed top-level shape before merging and preserve valid keys owned by the other writer; malformed state should require explicit recovery rather than implicit overwrite.
VERIFY: Seed paths.json with malformed JSON, empty/truncated partial JSON, and valid JSON of an invalid top-level type. Invoke each writer and require failure/false result plus byte-identical original content and no surviving temp file. Seed a valid file containing keys owned by both surfaces and verify an unrelated update preserves all non-owned valid values.

[P2] [W2-005] desktop/modules/targets.ps1:Save-ElectronStateSnapshot / Restore-ElectronStateSnapshot
EVIDENCE: Save-ElectronStateSnapshot lines 131-143 selects lightweight repaint mode solely because status.state is `themed-inplace` or `themed-relocated`, without requiring status.healthy. The lightweight snapshot copies only mutation-set files that currently EXIST. Restore-ElectronStateSnapshot lines 188-201 deliberately does not remove top-level files absent from a repaint snapshot and restores app/ sidecars by iterating only files that were copied into `appfiles`; there is no existence bitmap for files absent before the operation. A structurally themed but unhealthy target with a missing Wintage sidecar therefore receives a sparse repaint snapshot, the Node helper can create that missing file during repaint, and a later parent transaction failure such as manifest-commit failure cannot restore the original absence.
DEFECT: The lightweight transaction snapshot records bytes but not existence. It therefore cannot represent the full pre-operation state for a repaint that repairs missing sidecars. Parent rollback can leave newly-created files behind after reporting operation failure, causing runtime state and manifest state to diverge.
REPAIR: Record existence explicitly for every file in the lightweight repaint mutation set, including both resources-root and app/ sidecars plus fuse backup state. Restore must overwrite files that existed before and remove only files proven absent before. Alternatively restrict lightweight snapshots to a verified healthy state where the helper cannot create missing files, but explicit existence metadata is the safer invariant and remains correct if the repaint mutation set expands.
VERIFY: In PowerShell fixtures, construct structurally themed but unhealthy in-place and relocated states with one or more sidecars absent, force failure after the Node repaint but during manifest commit, and compare every mutation-set path before/after for both existence and bytes. Manifest contents must also remain unchanged. Healthy repaint success paths must continue using the lightweight snapshot without copying app.asar-sized data.

[P2] [W2-006] desktop/modules/targets.ps1:Save-DirPreState lifecycle / desktop/install.ps1 browser and extension callers
EVIDENCE: Save-DirPreState creates a recursive `%TEMP%\wintage-dirstate-<guid>` snapshot for an existing directory. Restore-DirPreState consumes it only on rollback, with its failure-path lifecycle separately covered by CORE-007. Successful callers need an explicit disposal path. Browser dispatch captures `$preStage = Save-DirPreState $BrowserStageRoot` at desktop/install.ps1 line 642; after a successful transaction line 659 executes `Remove-Item $preStage`, but `$preStage` is the structured snapshot object containing Existed/SnapshotPath rather than the SnapshotPath itself, so the actual recursive temporary snapshot is not targeted. VS Code/Antigravity revert/apply paths capture `$preDest = Save-DirPreState $dest` at lines 822 and 885 and register it only in rollback closures at lines 844/857/893; on successful commit there is no snapshot-disposal call at all.
DEFECT: Directory snapshots have rollback cleanup but no reliable success lifecycle. Normal successful browser and extension operations can leave complete recursive copies in TEMP indefinitely; repeated applies/reverts accumulate stale data proportional to the target directories. The browser caller additionally attempts cleanup using the wrong object type.
REPAIR: Add one explicit snapshot-disposal helper that accepts the Save-DirPreState record and deletes only its SnapshotPath. Call it after a transaction has committed successfully. Coordinate its ownership with CORE-007: rollback success may consume the snapshot, rollback failure must retain recovery evidence, and the normal-success disposer must never run early enough to remove rollback material before commit.
VERIFY: On supported PowerShell, record existing `wintage-dirstate-*` temp entries, run successful browser apply/revert and VS Code/Antigravity apply/revert/repaint operations, then require no newly-owned snapshots remain. Inject manifest-commit failure and verify rollback still has its snapshot available until exact restoration succeeds; inject rollback failure and verify recovery evidence is deliberately retained rather than cleaned.

SECOND_WAVE_DONE_WHEN: W2-001 through W2-006 are repaired without broad redesign; fuse recovery is generation-bound and can never restore an old executable across an application update; Electron health enforces archive/unpacked pairing and cannot call a broken relocation healthy; paths.json writers serialize their complete read-modify-write and fail closed on malformed existing state; lightweight Electron rollback restores both bytes and prior file absence; successful directory transactions release their snapshots while failed rollback preserves recovery evidence; focused Electron generation/topology tests pass; concurrent/malformed paths tests pass under PowerShell; directory-snapshot lifecycle tests pass; and the full tests/Run-Tests.ps1 plus release-facing Node gates pass in a supported Windows environment.

---
## 03 — AUDIT PERFORMANCE
PROJECT_NAME: Wintage
DATE_TIME: 2026-08-30T04:51:10+03:00
CAMPAIGN_PROFILE: quick3
CAMPAIGN_PROFILE_VERSION: 1.0.0
CAMPAIGN_RUN_ID: acb-mtf4qjtn-6afcd4a2d692402d9762
CAMPAIGN_MANIFEST_SHA256: c01ec812cf5952fdab101d5e7bc83c8251e0d9c21bb0ab06e426f5d58baedff2
WAVE_ID: performance
WAVE_INDEX: 3
WAVE_COUNT: 3
WAVE: AUDIT PERFORMANCE / STABILITY / EFFECTIVENESS
TARGET: Wintage_30.08.26-T04-25-36.zip, unpacked project root /mnt/data/wintage_audit
BASELINE: archive SHA256 9b9ea5186c368c39b9b36f8d518398cf34668d9b5c7d2adf212f38c123f0f8d0; embedded dirty worktree main@a9399dc9d053b0cd333e0c343fbe6eed26d40185
PREVIOUS_WAVE_SHA256: NONE
GIT_CONTEXT: PRESENT main@a9399dc9d053b0cd333e0c343fbe6eed26d40185 - origin/main resolves to the same commit; live archive worktree is dirty, including wintage.user.js and tools/install-electron.js, so supplied implementation bytes were authoritative
SAIPEN_CONTEXT: STALE - .saipen records v1.29.0/T-223 and claims historical PERF-001..007 repairs green at shipped commit 9ff576c, but the supplied worktree is newer/dirty and direct execution below demonstrates several of those intended resource bounds are incomplete in the live implementation
AUDIT_SCOPE: hot-path and resource-bound audit of wintage.user.js mutation collection, force/light sweep scheduler, shadow-root ownership and CSSOM scanning; desktop Electron SCROLL_FIX/WCO_FIX/injection lifecycle; electron-fuses/install-electron large-binary transactions; WinForms paint/resource lifecycle; generated-shim parity and relevant release/test gates
TEST_STATUS: TEST_FAILED
TEST_LIMITATION: Windows PowerShell/WinForms verification could not run because neither `pwsh` nor `powershell` exists in the sandbox; live Playwright/Chromium verification could not start because `node -e "require('playwright')"` failed with MODULE_NOT_FOUND despite the dependency being declared in package.json
VERIFIED_INSTEAD: `node tools/test-electron-state.js` ran with 6 failures; `node tools/test-theme-switch.js` ran and failed with ReferenceError: IS_X is not defined; test-electron-shim.js, test-repainter-polarity.js, test-shim-payloads.js, test-theme-packs.js, test-terminal-font.js, build-desktop.js --check, check-css.js and check-wiki-mirror.js passed. Exact-production-source Node harnesses additionally proved: one light sweep processed 12000 nodes despite LIGHT_MAX_NODES=2500; one light request produced two light scans; a 20000-node userscript childList iterated all 20000 while processing only 500; SCROLL_FIX retained/drained a 20000-node burst across 101 animation frames and 20002 computed-style reads; 100 WCO resize events queued 100 RAF callbacks causing 101 full selector scans and 101000 rect reads in the fixture; a removed shadow host left piercedRoots size=1 with zero follow-up sweep requests; same-document navigation followed by a frame-load trigger increased insertCSS calls from 1 to 2. Real-tool 128 MiB fixtures measured max RSS of 176932 KiB for in-place apply, 307324 KiB for revert, 174692 KiB for fuse status, and 305196 KiB for defuse.
STATUS: PERFORMANCE: COMPLETE
TICKETS: 8
HANDOFF: IMPLEMENTATION_AGENT
COVERAGE_INSPECTED: wintage.user.js parse/process/flushWrites/pierceShadow/onMutations/suspendRepainter/requestLightSweep/requestForceSweep/scheduleSweep/runSweeper/startSweeping; desktop/targets/electron/shim.cjs SCROLL_FIX/WCO_FIX/web-contents-created injection lifecycle/status stamping; tools/electron-fuses.js readFuses/fuseVerdict/defuse; tools/install-electron.js snapshotSidecars/captureRevertPreState/installInPlace/revert transaction memory shape; desktop/WintageInstaller.ps1 Draw-Bevel/DrawItem/preview Paint/Refresh-Swatches; generated desktop/out/electron shims; relevant JS gates and historical SAIPEN PERF notes
COVERAGE_DEFERRED: real Chromium layout/style timing and heap snapshots; real Electron renderer profiling; WinForms GDI handle-count stress; Windows PowerShell tests and full tests/Run-Tests.ps1; purely visual CSS correctness; one-shot build-tool micro-optimisation without demonstrated runtime effect
CROSS_WAVE_REFERENCES: CORE-001/CORE-004/CORE-005/CORE-006 cover correctness failures in the same Electron transactions as PERF-001; PERF-001 optimization must preserve the stronger rollback semantics required by those CORE tickets. W2-001 also changes fuse-backup generation ownership and must be integrated with PERF-001 rather than introducing another full-memory copy. W2-006 covers temporary directory snapshot lifecycle, distinct from the RAM-bounded binary snapshots here. CORE-008 remains the separate failing theme-switch test harness.
RESIDUAL_UNCERTAINTY: browser-specific wall-clock savings for PERF-002..PERF-007 could not be measured in a real Chromium renderer because Playwright runtime is absent, but the work-count, queueing and retention defects were exercised from the exact production source. PERF-008 is statically verified against undisposed System.Drawing IDisposable allocations but native GDI handle growth could not be measured without Windows/WinForms.

[P1] [PERF-001] PROVEN BOTTLENECK tools/electron-fuses.js:readFuses/defuse + tools/install-electron.js:captureRevertPreState/installInPlace
EVIDENCE: electron-fuses.js lines 61-83 reads the entire executable with fs.readFileSync merely to find a short fixed sentinel and inspect a few fuse bytes. defuse() lines 112-139 reads the entire executable again and then duplicates it with `Buffer.from(data)` before changing two bytes. install-electron.js lines 490-502 snapshots complete app.asar, app.asar.bak, executable and fuse backup into Buffers; installInPlace lines 574-575 additionally reads the entire live asar into `preAsar` while also establishing an on-disk `.bak`. Direct 128 MiB fixtures using the real tools measured: fuse status max RSS 174692 KiB; direct defuse 305196 KiB; in-place apply 176932 KiB; in-place revert 307324 KiB. The revert fixture used a tiny fake executable, so a real Electron executable adds further memory pressure on top of the two archive-sized buffers.
ISSUE: Memory use scales with whole application binary/archive size rather than with the tiny metadata actually being inspected or modified. Revert can hold two full asars plus executable/fuse bytes simultaneously, and defuse duplicates an entire executable to change two fuse bytes. Large Electron installations can therefore push a simple status/apply/revert into hundreds of MiB or GiB of transient RSS and synchronous I/O, making the safety transaction itself a stability risk.
OPTIMIZE: Make fuse probing bounded: search the executable for the fixed sentinel using a chunked reader with overlap for sentinel boundaries, validate the small fuse wire, and patch only the verified byte offsets through an fd. Preserve the pristine executable on disk with copy/rename semantics rather than `Buffer.from` of the whole file. For archive rollback, replace full-file Buffer snapshots with transaction-owned temporary files/renames on the same filesystem. In installInPlace, use the already-created pristine on-disk backup as rollback data while separately preserving any pre-existing `.bak` that must be restored on transaction failure.
GUARDRAIL: Do not trade memory reduction for weaker recovery. CORE-001/004/005/006 and W2-001 require byte-exact rollback, generation-correct fuse backup ownership and retained recovery evidence after incomplete rollback. Temporary snapshots must be created before mutation, fsync/close as appropriate, survive failed rollback, and be consumed only after commit or verified restoration.
VERIFY: Add large sparse/fixture binaries at 128 MiB and at least one larger tier. Assert fuse status/defuse and in-place apply/revert still produce byte-identical results while peak RSS remains effectively bounded rather than growing by one or two complete binary sizes. Keep failure injection around fuse patch, archive patch, revert and recovery cleanup and prove exact rollback. A practical regression gate should show a 128 MiB archive/exe operation staying far below the current ~175-307 MiB peaks.

[P1] [PERF-002] PROVEN BOTTLENECK wintage.user.js:onMutations + desktop/targets/electron/shim.cjs:SCROLL_FIX mutation collector
EVIDENCE: wintage.user.js lines 2034-2043 says ADDED_NODE_BUDGET bounds collection itself, but lines 2088-2113 continue iterating every `m.addedNodes` entry after the 500 retained-node cutoff, merely switching to a stylesheet peek. An exact-source harness supplied one childList containing 20000 element nodes: all 20000 entries were iterated, only 500 were processed, and one force request was issued. The parallel Electron SCROLL_FIX has the more severe version: shim.cjs lines 136-160 keeps unbounded `trees`/`treeRoots`, and lines 208-217 calls `queueTree` for every added node before its per-frame BUDGET=200 becomes relevant. An exact SCROLL_FIX harness with one 20000-node childList required 101 consecutive animation-frame flushes and 20002 getComputedStyle calls.
ISSUE: Both implementations put the safety budget after the expensive intake step. The userscript avoids retaining more than 500 refs but still performs an unbounded synchronous walk of a huge MutationRecord; SCROLL_FIX retains every root and turns one bulk insertion into a long multi-frame backlog. A framework can therefore generate a single mutation large enough to bypass the intended latency/memory bound without ever violating the nominal processing budget.
OPTIMIZE: Apply the cap at intake. Once a childList crosses the retained/processed budget, stop walking that addedNodes collection and switch to a conservative deferred recovery path. In the userscript, mark styles dirty conservatively and request one force lap rather than examining every remaining node. In SCROLL_FIX, discard per-node enqueueing for the overflow and enqueue one appropriate ancestor/document-root rescan token, deduplicated against existing work. Also ancestor-deduplicate queued roots so a parent plus its descendants cannot schedule overlapping subtree walks.
GUARDRAIL: Overflow fallback must not miss late styles or permanently skip nodes. The userscript force lap remains globally floor-limited. SCROLL_FIX must retain its 200-elements-per-frame processing bound and must not replace overflow with one synchronous document-wide querySelectorAll/getComputedStyle pass.
VERIFY: Add exact-source tests with 500, 501, 20000 and 100000 added nodes. Intake iterations and retained references must stay O(budget), not O(addedNodes.length). The userscript must eventually cover overflow via bounded continuation. SCROLL_FIX must not allocate one queue entry per overflow node and must drain without 100+ consecutive frames for a 20000-node single-record burst.

[P1] [PERF-003] PROVEN BOTTLENECK wintage.user.js:runSweeper light path / LIGHT_MAX_NODES
EVIDENCE: lines 2176-2178 define `FORCE_BUDGET = 2500` and `LIGHT_MAX_NODES = FORCE_BUDGET`. runSweeper lines 2301-2303 then sets `const budget = force ? FORCE_BUDGET : Infinity`, so LIGHT_MAX_NODES is never used. The light path at lines 2342-2347 materialises every `*:not([data-w95-done])` and processes until `remaining` reaches zero, which cannot happen when remaining is Infinity. The nearby comment explicitly claims “The global budget still caps the work when a flood ... appears”, contradicting executable behavior. An exact production-body harness with 12000 dirty elements processed all 12000 in one `runSweeper(false)` call while reporting `LIGHT_MAX_NODES=2500`.
ISSUE: Attribute-cooldown floods are routed into a supposedly bounded light lane that is actually unlimited. A sufficiently large set of dirty elements can therefore create one large querySelectorAll allocation plus thousands of getComputedStyle/layout-sensitive process calls in a single timer callback, recreating the long-task class that FORCE_BUDGET was introduced to prevent.
OPTIMIZE: Use LIGHT_MAX_NODES as the real light-pass budget. Avoid processing more than that number in one slice. If querySelectorAll materialisation itself is too expensive for an overflow, use an incremental cursor/TreeWalker or a bounded dirty-element registry rather than creating a full unmatched NodeList. Continue outstanding light work through the existing MIN_SWEEP_GAP scheduler.
GUARDRAIL: A light overflow must eventually converge and must not silently promote every light mutation to an unnecessary full force lap. Preserve the settled-page fast path where no dirty elements means effectively zero work.
VERIFY: Add 2500, 2501, 12000 and large-shadow-root light fixtures. Assert one light slice performs no more than LIGHT_MAX_NODES process calls, additional slices remain MIN_SWEEP_GAP-limited, all dirty nodes eventually converge, and a settled page schedules no continuing work.

[P1] [PERF-004] STRONGLY EVIDENCED WASTE wintage.user.js:piercedRoots / shadowObserver lifecycle
EVIDENCE: `piercedRoots` is a strong Set at line 1410. pierceShadow lines 1412-1427 adds every discovered ShadowRoot and registers it with the shared shadowObserver. Detached-root pruning exists only inside runSweeper at line 2286, while onMutations lines 2021-2157 examines attributes and addedNodes but never removedNodes and does not request any sweep for a removal-only batch. Suspension clears the Set only after the circuit breaker permanently disables the repainter. An exact onMutations harness started with one root whose host was disconnected, delivered a childList containing only removedNodes, and ended with `piercedRoots.size === 1`, `forceRequests === 0`, `lightRequests === 0`. Because a quiet SPA may perform no later sweep, that detached root can remain strongly reachable indefinitely; the shared MutationObserver also has no per-root `unobserve` operation in this design.
ISSUE: Long-lived SPAs that create and discard shadow-host components can accumulate detached ShadowRoots/subtrees in repainter ownership even while the page is otherwise idle. The existing “prune at next sweep” logic has no guarantee that such a sweep will ever occur after a removal, undermining the memory-bound claim in the suspension comments.
OPTIMIZE: Give shadow-root observation an explicit detach lifecycle. On child removals, perform a cheap coalesced cleanup of the small root registry and remove roots whose hosts are disconnected. Because one shared MutationObserver cannot unobserve a single target, either rebuild its observation set after pruning or use per-root observers whose disconnect lifecycle is tied to the root/host. Avoid introducing periodic polling.
GUARDRAIL: Connected shadow roots must continue receiving style injection and mutation handling; cleanup must not traverse entire removed subtrees merely to discover roots already present in the registry. Suspension must still release all registries immediately.
VERIFY: Add a DOM-capable test that mounts and themes hundreds of shadow hosts, removes them without subsequent additions, drains mutation callbacks, and asserts registry/observer ownership returns to the connected-root baseline without requiring a force sweep. Repeat mount/remove cycles and verify retained root count does not grow.

[P2] [PERF-005] PROVEN BOTTLENECK wintage.user.js:requestLightSweep/scheduleSweep lightPending consumption
EVIDENCE: requestLightSweep lines 2276-2281 sets `lightPending = true` and schedules a light pass. scheduleSweep's callback lines 2235-2238 runs `runSweeper(false)` without first consuming/clearing that pending flag. Immediately afterward lines 2246-2250 sees the still-true `lightPending`, clears it, and schedules another light sweep. The comments claim this branch is for light work “arrived meanwhile”, but the flag still represents the original request. An exact scheduler harness issued one request with one dirty element and no subsequent work; result: 2 timers fired, querySelectorAll ran twice, only the first pass processed an element, and the second was empty.
ISSUE: Every isolated light request pays for one guaranteed redundant follow-up scan. On mutation-heavy pages this systematically doubles light-lane selector work and defeats the coalescing contract even when no work arrives during the first pass.
OPTIMIZE: Consume the currently pending light request immediately before entering its light run. Any requestLightSweep call that occurs during runSweeper then sets the flag again and legitimately schedules a follow-up. Keep force debt precedence unchanged.
GUARDRAIL: Do not lose a light request that arrives during a force or light pass, and do not allow light work to starve behind force continuation. MIN_SWEEP_GAP remains mandatory.
VERIFY: One isolated light request must produce exactly one light pass. A request injected during that pass must produce exactly one additional pass. Multiple requests before the first timer must coalesce to one pass. Force-plus-light interleavings must retain the current no-starvation semantics.

[P2] [PERF-006] PROVEN BOTTLENECK desktop/targets/electron/shim.cjs:WCO_FIX resize/geometrychange scheduling
EVIDENCE: WCO_FIX apply lines 647-677 clears all touched transforms, queries every matching interactive control with `document.querySelectorAll(SEL)`, and calls getBoundingClientRect for each. Lines 684-685 attach resize and geometrychange handlers that each call `requestAnimationFrame(apply)` with no frameQueued/coalescing flag. requestAnimationFrame does not deduplicate callbacks: all callbacks requested before a frame execute in that frame. An exact WCO_FIX harness with 1000 matching controls fired 100 resize events before draining RAF; it queued 100 callbacks and produced 101 querySelectorAll passes and 101000 rect reads including the initial pass.
ISSUE: Interactive window resizing can multiply a full-document control scan and layout reads many times inside the same render frame. This creates precisely the resize jank that rAF is normally used to prevent; merely wrapping each event independently in rAF does not coalesce it.
OPTIMIZE: Add one `frameQueued` flag/shared scheduleApply function used by both resize and geometrychange. At most one apply may be pending per animation frame. If geometry changes again while apply is running, one later frame is sufficient.
GUARDRAIL: Preserve the four bounded startup settle passes and the current measurement-based control displacement. Never debounce so aggressively that the native caption overlay visibly lags behind window geometry.
VERIFY: Fire 100 resize and geometrychange events before one RAF tick and assert exactly one apply/full selector scan is queued. Then fire another event after that frame and assert one additional apply. Use a large control fixture and confirm rect-read count scales with controls × frames, not controls × raw events.

[P2] [PERF-007] STRONGLY EVIDENCED WASTE desktop/targets/electron/shim.cjs:web-contents-created document epoch / insertCSS lifecycle
EVIDENCE: lines 788-797 dedupe injection by `injectedEpoch`. A true main-document navigation increments the epoch at line 833. Line 834 also increments it on `did-navigate-in-page`, which is a same-document navigation. `inject()` is wired to `did-frame-finish-load` at line 829 and always calls insertCSS at lines 818-824 for a new epoch; the returned key merely overwrites `wc.__wintageCssKey`, and the code never calls removeInsertedCSS. An exact-source lifecycle harness produced one insertCSS call on initial dom-ready, then emitted a main-frame did-navigate-in-page followed by did-frame-finish-load; insertCSS increased from 1 to 2 and executeJavaScript calls from 4 to 8 while the document itself had not been replaced.
ISSUE: The document-epoch fix correctly handles same-URL full reloads but conflates same-document history/hash/SPA navigation with new documents. A later frame-load event can therefore inject another full Wintage stylesheet into the existing document and overwrite the only removal key. Repeated same-document navigation can accumulate duplicate author stylesheets and increase CSSOM/matching work for no visual benefit.
OPTIMIZE: Advance the injection epoch only for navigation events that create a new document. Do not bump it for did-navigate-in-page unless a separate feature genuinely requires same-document reinjection; if such reinjection is required, explicitly remove/replace the previous insertCSS key rather than stacking another stylesheet. Keep script payload guards for renderer-side one-time fixes.
GUARDRAIL: Preserve the historical PERF-004 requirement that Ctrl+R or an application-triggered full reload to the identical URL receives a fresh stylesheet. Child-frame events must not consume or manufacture a main-document epoch.
VERIFY: Add an EventEmitter-style regression around the production injector: initial document inserts once; repeated dom-ready/did-finish-load/did-frame-finish-load in one epoch remain one; full same-URL reload increments to two; 100 did-navigate-in-page events plus child-frame load events remain at one insertion for the same document; no stale insertCSS key accumulates when a real new document arrives.

[P2] [PERF-008] STRONGLY EVIDENCED WASTE desktop/WintageInstaller.ps1:Draw-Bevel/DrawItem/preview Paint disposable drawing resources
EVIDENCE: Draw-Bevel lines 136-144 constructs a new `Drawing.Pen` for every individual DrawLine and never disposes it. One Draw-Bevel call creates eight Pen instances, and the preview Paint handler invokes Draw-Bevel repeatedly across title/body/buttons/input/scrollbar and palette swatches. Lines 803-811 and 822-885 likewise construct new `Drawing.SolidBrush` objects inline for essentially every FillRectangle/DrawString and never call Dispose. The same file demonstrates awareness of IDisposable ownership elsewhere, explicitly disposing sound/player/file objects, but there is no corresponding paint-resource cleanup. Refresh-Swatches also constructs a ColorDialog per edit without disposing it, though that is a much lower-frequency instance of the same lifecycle drift.
ISSUE: Repaint frequency controls allocation of native-backed System.Drawing objects. The preview alone creates well over a hundred disposable Pen/Brush wrappers per paint, producing unnecessary GC/finalizer/GDI+ resource pressure; owner-drawn palette rows add more on every redraw. Under repeated invalidation/theme switching or window exposure this can degrade GUI responsiveness and, on Windows under pressure, contribute to drawing-resource exhaustion.
OPTIMIZE: Create Pens/Brushes in the narrowest useful scope and deterministically Dispose them with try/finally. Draw-Bevel should create one top-left and one bottom-right Pen per call, reuse each for both bevel depths/edges, then dispose both. Preview Paint can cache per-paint brushes keyed by the small palette token set and dispose the cache at the end of the event. Dispose ColorDialog after ShowDialog. Dispose the two long-lived Fonts when the form closes after controls no longer need them.
GUARDRAIL: Do not cache Graphics objects across Paint events and do not retain disposed brushes/pens between events. Rendering output and palette colors must remain byte/visually equivalent; resource cleanup must not dispose the event-owned Graphics object.
VERIFY: On Windows, repeatedly invalidate the preview and owner-drawn theme list for thousands of paint cycles while sampling Process GDI object count/private bytes. Counts must stay near a stable baseline instead of trending upward, and paint latency should not deteriorate. Add a focused smoke test for repeated theme selection/Refresh-Swatches and verify the installer remains responsive and visually unchanged.

PERFORMANCE_DONE_WHEN: PERF-001 through PERF-008 are repaired without weakening CORE/W2 transaction invariants; fuse/archive operations use bounded streaming/on-disk recovery rather than whole-binary RAM copies; mutation intake is bounded before retention in both userscript and Electron SCROLL_FIX; light sweeps honor LIGHT_MAX_NODES and a single light request produces one pass; detached shadow roots are released without periodic polling; WCO resize/geometry events coalesce to one full scan per frame; same-document navigation cannot stack duplicate insertCSS sheets while same-URL full reload still reinjects; WinForms paint resources are deterministically disposed; the new large-binary, 20k/100k mutation, light-scheduler, shadow-lifecycle, WCO and injection-epoch regressions pass; existing Node gates pass including test-electron-state.js and test-theme-switch.js after their CORE repairs; tests/Run-Tests.ps1 and GDI stress verification pass in a supported Windows environment.

---
ALL_3_DONE_WHEN: 3/3 waves validated and combined into canonical handoff package.