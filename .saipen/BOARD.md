# BOARD

## DOING
- [/] T-234 [P1] Execute external audit inbox layer audit/2.md (SRC-004); 18 of 19 findings VERIFIED, the last one (PERF-005, GUI one-blocking-child-per-target) split out as T-240 because its verify bar needs a live WinForms session | verify: every actionable clause of SRC-004 is terminal with evidence; linked Work DONE; source closure succeeds; audit/2.md consumed by the journaled audit inbox cleanup | source_receipts: SRC-004 | owner: opencode | claim_time: 2026-09-03T23:27:24Z

## DONE
- [x] T-239 [P1] Execute external audit inbox layer audit/1.md (SRC-003): the 2026-09-02 quick3 campaign, 17 tickets across 3 waves, baseline main@ffc13fd. Two of its findings were independently reproduced at E-772 and shipped in v1.31.0 as T-235 (CORE-001 relocation-revert data loss, CORE-003 exclusion-guard disarm); the rest are untriaged. Triage every finding against the post-v1.31.0 tree, ticket what is still live, and disposition the rest with evidence so the receipt can close. | verify: every actionable clause of SRC-003 is terminal with evidence; each live finding has its own ticket verified and DONE; saipen source close SRC-003 succeeds; audit/1.md consumed by the journaled audit inbox cleanup | owner: opencode | claim_time: 2026-09-03T22:23:02Z
- [x] T-237 [P2] ORPHAN_RECEIPT SRC-001 has been failing --gate core since E-755 and blocks the convergence closure bar (canonical validator PASS). SRC-001 is the superseded 41-ticket AUDIT_ALL_3 handoff from the T-222 era: its meta records an uppercase digest 5F862E63... while the stored body hashes f31d4156..., so 'source show' reports SOURCE_CORRUPTION, and the Work it was linked to no longer exists on BOARD (CLEAN pruned T-222). It was explicitly superseded by SRC-002 at E-726 and the audit content was re-derived and shipped as T-223/T-224. Route it through the sanctioned lifecycle (source close -> cold archive + tombstone) instead of leaving a permanent red gate. | verify: python tools/validate.py --gate core no longer reports ORPHAN_RECEIPT SRC-001; saipen source status SRC-001 reports the tombstone; .saipen/archive/source/SRC-001.* exists and the body is retained for forensics; SRC-002/SRC-003/SRC-004 remain active and untouched | owner: opencode | claim_time: 2026-09-03T22:19:20Z
- [x] T-236 [P2] Unattributed theme-layer + docs pass in the dirty tree (SHIP attribution gate, same class as T-231): wintage.user.js Google/Material-3 surface tokens behind a new IS_GOOGLE host flag, Tailwind-prose + text-token variable coverage, Reddit title/overlay/click-catcher rules, media-stack and SVG repaint guards, raised mutation budgets, always-bounded force sweep; desktop/targets/betterdiscord/template.css +661; 32 locale READMEs relocated to locales/ with a language table in root README; wiki Home/_Footer stamped 1.30.0. tools/test-theme-switch.js was RED on this pass (ReferenceError: IS_GOOGLE is not defined) because its sliced harness context never declared the new host flag. | verify: node tools/test-theme-switch.js 44/44 PASS with IS_GOOGLE in the harness context and 3 new host-attribute assertions; instrument control (data-w95-google line removed) -> 1 FAIL exit 1, source restored byte-identical; node tools/check-css.js PASS; node tools/build-desktop.js --check up to date; node tools/check-wiki-mirror.js PASS | owner: opencode | claim_time: 2026-09-03T22:11:52Z
- [x] T-235 [P1] CORE-001 + CORE-003 audit fixes (E-772, ticketed as T-233 in LOG but never reached BOARD because SAIOPS refused every mutation): install-electron.js relocation Revert lost BOTH archives on a failed rollback -- captureAppDir never captured the archive, and targets.ps1 classified themed-relocated as repaintOnly so the parent snapshot omitted it too; wintage.user.js set __wintageExcludedReload before location.reload() without clearing it on a refused navigation, permanently disarming the OAuth/captcha/payment exclusion guard for that document. | verify: node tools/test-electron-state.js PASS incl. relo ROLLBACK_ASAR (original archive bytes preserved on disk, output reports rollback INCOMPLETE, no false 'exact pre-operation state' claim); node tools/test-spa-exclude.js PASS; node tools/test-fs-retry.js PASS; tools/test-ownership.ps1 PASS for the targets.ps1 -Operation switch | owner: opencode | claim_time: 2026-09-03T22:11:10Z
- [x] T-228 qBittorrent desktop target + native-font policy: new `qbittorrent` install target (unpacked Qt theme: config.json + stylesheet.qss + the two INI keys), and the Verdana_m1 face is NAMED by qbittorrent/obs/mpchc but never installed or removed. | verify: tools/test-ownership.ps1 66/66 PASS (13 qBittorrent assertions), Run-Tests ALL PASSED, build-desktop --check clean | owner: opencode | claim_time: 2026-09-03T22:06:37Z
- [x] T-231 unattributed unshipped work in the tree (SHIP attribution gate): PERF-008 chunked fuse scanner, PERF-009 GDI disposal, W2-007 keep/finalize recovery ordering and three suites were dirty since the T-224 era with no ticket. Also two "gate nobody runs" defects: test-terminal-recorded-set aborted on its own subject (native stderr under EAP=Stop) and five suites were reachable from neither Run-Tests nor release.ps1. | verify: test-terminal-recorded-set 18/18 PASS (was 12+abort), 9/9 tool suites wired and green, Run-Tests ALL PASSED [E-765..E-766]
- [x] T-230 install-electron reported every Windows sharing violation as "the application is running": the release gate was red ~1 run in 20 on correct code and real users were told to close an app that was not open. Bounded fsRetry (EBUSY/EPERM/EACCES, 25-400ms, 6 attempts) on every mutating rename/copy in both transactions and their rollback steps. | verify: node tools/test-fs-retry.js 22/22 PASS, 16/16 clean electron-state runs (baseline 3 red in 12), FileShare::None control still refuses, 2 instrument controls red [E-762..E-764]
- [x] T-229 tools/test-perf-bounded.js ColorDialog assertion was stuck RED on correct code for a release cycle (it demanded the allocation INSIDE the try, which is the bug it should reject) and E-738 mis-triaged that red as a missing fix. Assertion repaired, suite wired into release.ps1. | verify: node tools/test-perf-bounded.js PASS, 2 instrument controls red, WintageInstaller.ps1 untouched [E-759..E-760]
- [x] T-227 CORE-015 silent catches in the hover CSSOM surgery + shadow pierce: 4 swallows now counted via DIAG/noteSuppressed, first error retained, `window.__wintageDiag()` reporter, shim prelude mirrors it. | verify: node tools/test-diag-counters.js 18/18 PASS, instrument control red on a reverted catch [E-748..E-750]
- [x] T-226 CORE-014 refused reload left a silent split brain (storage on the new palette, page painting the old): try/catch + one console.warn + `⟳ Apply pending theme` menu row gated on STORED_THEME_ID. | verify: node tools/test-theme-switch.js 40/40 PASS, control red on bare reload() [E-745..E-747]
- [x] T-225 CORE-013 setupRouteGuard not idempotent: a second run wrapped the first wrapper (double guard(), stacked listeners, unreachable layer one). window.__wintageRouteGuard latch. | verify: node tools/test-spa-exclude.js 26/26 PASS, control red with the latch removed [E-742..E-744]
- [x] T-224 SRC-002 (SHA e6534421) 22-ticket AUDIT_ALL_3 re-audit: triage + CORE-008 fix verified. 17/22 FIXED_LIVE, 5 LIVE, 0 unverifiable; user scope = CORE-008 only. | verify: node tools/test-theme-switch.js PASS, 9 JS gates green [E-738]
- [x] T-223 SRC-002 18-ticket AUDIT_ALL_3 repair shipped v1.29.0 (9ff576c): net-new CORE-002/004, W2-004/006, PERF-001/002/003/004/005/006/007; 8 pre-fixed verified. | verify: Run-Tests ALL PASSED, 9 JS gates PASS, v1.29.0 pushed main+tag [E-726..E-728]

## TODO
- [ ] T-240 [P2] PERF-005 (SRC-004:R017): the installer GUI spawns one BLOCKING install.ps1 child per checked target ON the WinForms thread, so a multi-target Apply serialises full process startup plus a 4359-line PowerShell reparse per target while the window is frozen. It also re-runs `node tools/build-desktop.js --check` once per target although install.ps1's own `-Target all` path already caches that verification once, and Load-Targets then re-scans every Electron executable for the fuse sentinel with no unchanged-file cache (the audit measured 128 MiB read for one unchanged 64 MiB exe across two listings). Repair: a batch mode reusing install.ps1's existing $names dispatcher in ONE worker process, one build verification shared across the selected batch, and a fuse-verdict cache keyed on path+size+mtime. Split out of T-234 because the verify bar is process count and UI responsiveness during a real multi-target Apply, which a fixture cannot measure. | verify: a multi-target Apply spawns ONE child process rather than N; build-desktop --check executes once per Apply rather than once per target; an unchanged executable is fuse-scanned once and served from cache on every later listing; the WinForms thread stays responsive for the duration; single-target CLI and `-Target all` behaviour are byte-identical to today; tools/test-reapply.ps1 and tests/Run-Tests.ps1 stay green
- [ ] T-238 [P2] saitranslate carries TWO kitchens and the tree tracks the wrong one. .saipen/saitranslate/kitchen (legacy, 128 files, tracked) and .saipen/extensions/subs/saitranslate/kitchen (canonical, 97 files, untracked) hold 96 byte-identical files; the OUTBOX differs and the canonical one is the current SAIT-003 package bound to ffc13fd. The legacy path additionally holds 31 files with no canonical twin (29 kitchen-root README.*.md, surface.md, TRANSLATION_CONTRACT.md), so it cannot simply be deleted -- E-770 already recorded the mismatch and stopped. Decide the single canonical location, migrate the 31 orphans or prove them obsolete, remove the duplicate, and make the OUTBOX payload path match reality so eee stops describing files that live somewhere else. | verify: exactly one saitranslate kitchen exists in the tree; the SAIT OUTBOX payload paths resolve to real files; no file present only in the removed location; python tools/validate.py --gate collect:saitranslate parses the OUTBOX strictly; git ls-files shows no duplicated translation payload

## BLOCKED
- [ ] T-065 YouTube Studio analytics bars disappear under the theme | blocker: requires authenticated user session
