# BOARD

## DOING
- [/] T-172 [unattributed pre-existing drift] .saipen/extensions/subs/saiui.md carries an uncommitted working-tree charter update (spec renamed "Vintage Golden" -> "Golden Default", role_revision recomputed) — arrived via the E-545 sub sync from a newer protocol copy, never committed, present at session start of the T-167 run | verify: saiui.md drift either committed (matches a ticket) or reverted to HEAD 9fb22b9; validate.py PASSes with the tree in that state | blocker: it is correct content (goldendefault is the real default per T-167) but unattributed — must not ride silently into an unrelated ship | owner: claude | claim_time: 2026-08-08T21:43:00Z

## TODO

## DONE
- [x] T-170 MARKHUNT sweep post-v1.26.2: swept all 5 vectors (stale markers, doc drift, security, arch debt, familiarity) across repo root/README*/wiki/desktop*/tools*/tests/wintage.user.js/release.ps1/browser-theme; 2 new findings ticketed -> BLOCKED (T-173 T-143 false-closure on writeAtomic/backupOnce, T-174 GUI hardcoded V: paths), T-171 re-confirmed as already ticketed; zero stale markers, zero secrets, all destructive ops guarded, doc claims match code | verify: E-560 markhunt 2 findings 5/5 vectors @56ad851 tickets=T-173,T-174; closure manifest cursor done, head_end==HEAD
- [x] T-169 install.ps1 monolith split: desktop/install.ps1 1617 -> 562 lines; 26 shared helpers -> desktop/modules/common.ps1, 11 Invoke-* -> desktop/modules/targets.ps1, install.ps1 keeps data tables + dispatch + listing + Reapply/Status/logon flows, dot-sources both modules | verify: E-557+E-558 PS parse 0/0/0, Run-Tests.ps1 ALL PASSED (apply/revert round-trips), 37/37 funcs preserved, -WhatIf byte-exact, install.ps1 562 < 1000; shipped 56ad851
- [x] T-168 Add wiki-mirror drift gate: the qq run caught wiki/Installation.md still carrying `.\install.ps1` while desktop/README.md was fixed at T-145 — no gate catches repo wiki/ mirror drifting from saiwiki kitchen. tools/check-wiki-mirror.js (repo wiki/*.md vs .saipen/extensions/subs/saiwiki/kitchen/wiki/, allowing only .md-link adaptation) added and wired into release.ps1's gate list after test-theme-packs | verify: E-554+E-555 gate FAILs on deliberate kitchen divergence naming page+line, PASSes at HEAD (all 8 pages), release.ps1 names it, node --check clean; shipped dcee7d9
- [x] T-167 README + wiki palette table mislabels legacy hexes as "Golden Default palette tokens": README.md:85-100 and README.md:69 named Canvas/Soft/Raised/Alt/Bevel highlight/Bevel shadow/Text/Muted/Accent with legacy hexes none of which exist in any themes/*.json pack; replaced all 4 README tables + feature lines + wiki/Palettes.md with the real goldendefault token names/hexes (10 of 21), retitled wiki table, re-synced saiwiki kitchen + saitranslate kitchen ru/et/ded payloads | verify: E-551 all hexes in pack, mirror synced, stale-hex rg clean; E-552 review re-run green; shipped 07bd410
- [x] T-166 [from saiwiki WIKI-002, qqq collect] integrate ready wiki package at 5e79f51 (v1.26.2): apply 8 kitchen/wiki pages to repo wiki/ mirror (.md-adapted links), push kitchen pages to github.com/vacterro/Wintage.wiki master, commit + push origin | verify: repo wiki/ matches kitchen content with .md links only (all 8 OK, zero bare internal links, zero 1.26.0/1.26.1 stamps); Wintage.wiki remote master = 1a54ffb carrying 8 bare-link pages (ae1540a..1a54ffb); origin main = 9fb22b9; validate.py conformant -- E-546, E-547
- [x] T-165 pre-existing validator conformance debt (found while closing T-159): 13 DONE tickets carry no | verify: (T-001/T-003/T-005/T-010/T-011/T-012/T-026/T-038/T-047/T-048/T-077/T-125/T-126), and LOG E-464..E-478 (:177-191) were stamped in local time, 438-504m ahead of real UTC. None caused by the dd all plan; each needs evidence drawn from LOG/git before closing | verify: tools/validate.py PASS -- all 13 got verify evidence (sealed LOG + git), T-077 split line merged, 15 timestamps re-anchored to UTC from ship commits (E-538 DEC); validate.py reports "Agent is conformant" (40 WARNs)
- [x] T-143 [MARKHUNT] architecture debt x4: backup discipline inconsistent across Invoke-* targets, duplicated patterns (writeAtomic x3, backupOnce x2, asar parsing x2, palette tokens x2), install.ps1 1545-line monolith with if/else dispatch; the "11 hardcoded V:\ defaults" half is largely resolved by T-148 (remaining stragglers ticketed as T-153/T-155/T-164) | verify: token-read chain consolidated into Get-PaletteTokens (4 sites, ALL TESTS PASSED); the named writeAtomic/backupOnce identifiers no longer exist (resolved by T-146/T-154); backup hygiene gained Prune-Backups (T-160); monolith split DECIDED: install.ps1 stays a monolith for now -- dispatch is data-driven via $TARGETS/$ELECTRON/$SIMPLE and splitting risks regressions on live-app installs; a desktop/modules split is the recorded direction, sized as future work | blocker: unvetted audit -- install.ps1 1545-line monolith, duplicated patterns x3/x2 (E-489)
- [x] T-160 [MARKHUNT] unbounded growth x2: desktop/backup/ holds 39 timestamped dirs (~6MB) and nothing prunes them (zero prune/cleanup/retain in desktop/install.ps1 and tools/); desktop/out/ is >50% of tracked bytes, regenerated wholesale every release (v1.26.1 = 88 files, 15216/15205 lines) | verify: Prune-Backups keeps the 8 newest timestamped dirs (tested 10->8, fixed files kept); desktop/out/ untracked + gitignored + README build note; repo tracked bytes drop ~2.6MB | blocker: unvetted audit -- desktop/backup/ 39 dirs unpruned + desktop/out/ 55% of tracked bytes (E-521)
- [x] T-140 [MARKHUNT] manifest under-utilized: the browsers target (tools/install-browsers.ps1) is the only target of 14 with no Set-ManifestEntry — -Reapply can never rediscover or refresh it, and listing can't report it | verify: browser apply writes an installed.json entry; -Revert removes it; -WhatIf writes nothing | blocker: unvetted audit -- tools/install-browsers.ps1 missing Set-ManifestEntry, only target of 14 (E-489)
- [x] T-163 [from T-141 split] tools/electron-fuses.js defuse() rewrites the app EXE with no backup (:80-93); grep for backup/copyFileSync in that file returns zero hits, so -Revert can restore the asar move but never the flipped fuse bytes | verify: defuse writes a byte-exact backup of the original exe before mutating; revert restores the backup; test: backup==orig, exe flipped, revert restores
- [x] T-158 [MARKHUNT] desktop/install.ps1 Read-PathsJson (install.ps1:70-80) swallows any read/parse failure in `catch { return @{} }`, so a corrupt or unreadable paths.json is indistinguishable from "nothing configured" — identical class to T-126, which fixed exactly this for Load-FbSound and left its twin standing | verify: corrupt paths.json reports a clear warning; missing paths.json still returns @{} silently | blocker: unvetted audit -- desktop/install.ps1:70-80 silent catch returns empty on corrupt paths.json (E-521)
- [x] T-161 [MARKHUNT] wintage.user.js:1675 claims the repainter runs "the same as the shim's FLOAT_FIX, which is the only place it can run there" — FLOAT_FIX was removed from the shim in 2f8f250 and the repainter ships there now, so both halves of the sentence are false | verify: comment states the true split (repainter ships in the shim; the userscript's own path also applies it) | blocker: unvetted audit -- wintage.user.js:1675 stale FLOAT_FIX claim (E-521)
- [x] T-164 new finding (dd all vet): tools/import-fastprompter.js:25 hardcodes `V:/___VAC/__K/__CODE/_PY/_FastPrompter/src/fastprompter/theme/themes.py` as DEFAULT_SOURCE — last un-ticketed personal-machine path | verify: zero V:/___VAC in tools/import-fastprompter.js; default empty or generic, overridable via flag
- [x] T-153 [MARKHUNT] desktop/README.md:174 hardcodes `V:\___VAC\__P` in prose — replace with a generic placeholder describing the discovery behavior, not a path that exists on one machine | verify: zero V:\___VAC in desktop/README.md | blocker: unvetted audit -- desktop/README.md:174 hardcoded V:\___VAC\__P in prose (E-501)
- [x] T-156 [MARKHUNT] WintageInstaller.ps1 carries 9 mojibaked comment lines (double-encoded box-drawing dividers): :41 PALETTE LOADING, :114 WIN95 DRAWING, :132 FORM, :174 REMEMBERED FOLDERS, :330 FREEBUFF COMPLETION SOUND, :636 TARGET DISCOVERY, :698 RENDERING, :861 ACTIONS, :973 SKIN THE INSTALLER ITSELF — T-146 fixed only install.ps1's two, not the GUI's | verify: zero non-ASCII mojibake in WintageInstaller.ps1 comments; all dividers clean UTF-8; PS parse 0 errors | blocker: unvetted audit -- WintageInstaller.ps1:41,114,132,174,330,636,698,861,973 mojibaked dividers (E-512)
- [x] T-154 [MARKHUNT] Get-Content encoding gaps beyond T-146 scope: install.ps1:1308 (wintage-palette.txt marker), install.ps1:1519 (freebuff-sound.txt user config), WintageInstaller.ps1:505 (fbSoundFile) use Get-Content -Raw where Read-Utf8 is required for non-generated files | verify: zero Get-Content -Raw on user-config/generated reads in both files; PS parse 0 errors | blocker: unvetted audit -- install.ps1:1308,1519 + WintageInstaller.ps1:505 Get-Content -Raw (E-501)
- [x] T-155 [MARKHUNT] tools/install-browsers.ps1:6 hardcodes `V:\___VAC\__P` as -PortableRoot default — same personal-machine portability bug T-148 fixed in install.ps1, one file over | verify: zero V:\ paths in tools/install-browsers.ps1; default portable root empty, discovery untouched | blocker: unvetted audit -- tools/install-browsers.ps1:6 hardcoded V:\___VAC\__P -PortableRoot default (E-512)
- [x] T-138 [MARKHUNT] doc drift cluster x5 — all 5 items fixed by T-145 (ee55770) | verify: grepped and confirmed at E-489/E-512 | blocker: unvetted audit -- doc drift x5 fixed by T-145 ee55770 (E-489)
- [x] T-139 [MARKHUNT] i18n dead infrastructure x2 — resolved: T() now has callers in install.ps1 CLI (T-150 e24adf7) and all GUI labels (T-144 f33f45c); ded.json backfilled to 49 keys by T-152, zero missing vs en.json | verify: T() called in CLI + GUI; all 4 locales carry 49 keys | blocker: unvetted audit -- T() zero callers + ded.json 5 missing keys; resolved T-150/T-144/T-152 (E-489)
- [x] T-141 [MARKHUNT] security posture x2 — first half resolved: Assert-SafeProjectPath (install.ps1:1174) exists and guards all three source-tree paths since 976bdb3; second half (electron-fuses.js defuse writes EXE with no backup) re-issued as T-163 | verify: Assert-SafeProjectPath called at :755/:814/:871-style sites; T-163 tracked for the fuses half | blocker: unvetted audit -- Assert-SafeProjectPath 976bdb3; fuses defuse backup re-issued T-163 (E-489)
- [x] T-142 [MARKHUNT] encoding debt x2 — resolved by T-146 (c88ad12): mojibake double-encoded UTF-8 fixed, Get-Content replaced with Read-Utf8 at the cited sites | verify: confirmed resolved at markhunt E-512 | blocker: unvetted audit -- mojibake double-encode + Get-Content; resolved T-146 c88ad12 (E-489)
- [x] T-151 Close MARKHUNT T-138 — T-145 covered all 5 doc-drift items | verify: each of 5 T-138 items confirmed resolved by T-145 changes
- [x] T-159 [MARKHUNT] audit trail misreports itself x2 — board corrected by dd all plan: T-141 split (open half → T-163), T-151/T-138 relocated here from BLOCKED, T-139/T-142 closed as resolved; validator still FAILs on pre-existing LOG timestamp corruption + 13 DONE tickets without verify, tracked as T-165 | verify: BOARD [x]-only-under-DONE and T-141 split confirmed by validate.py BOARD checks; remaining debt T-165 | blocker: unvetted audit -- T-141 split + T-151/T-138 checkbox mismatch corrected (E-521)
- [x] T-162 [MARKHUNT] work exists only on this machine — resolved: origin/main == HEAD == f33f45c after the E-524 ship; remaining uncommitted tree is checkpoint churn only | verify: git rev-parse HEAD == origin/main | blocker: unvetted audit -- local 1cac3f7 vs origin 8c2ed25; resolved by f33f45c push (E-521)
- [x] T-144 Wire i18n T() calls into WintageInstaller.ps1 GUI | verify: all GUI labels use T(); shared i18n.ps1 loader extracted; ALL TESTS PASSED; parse OK — shipped f33f45c
- [x] T-157 [MARKHUNT] T-144 dot-source hack | verify: GUI now dot-sources i18n.ps1, not install.ps1; param leak and main-flow execution eliminated — shipped f33f45c | blocker: unvetted audit -- T-144 dot-source hack eliminated by i18n.ps1 extract f33f45c (E-512)
- [x] T-152 [MARKHUNT] Non-en locales stale x4 | verify: all 4 locales carry 49 keys, zero missing — shipped f33f45c | blocker: unvetted audit -- 4 locales stale, backfilled to 49 keys f33f45c (E-512)
- [x] T-117 Stranded DOING → demoted: Status indicators dead in Claude Desktop | verify: user confirmed dots carry colour; thinking dot darkened to warning token in follow-up (8c2ed25) -- E-415..E-417
- [x] T-112 Stranded DOING → demoted: Transparent popovers returned in Claude Desktop | verify: user confirmed popovers opaque after restart -- measured FLOAT_FIX
- [x] T-118 Stranded DOING → demoted: Usage/quota bars read as one solid block (WITHDRAWN twice) | verify: user confirmed fill and track read as different in the usage popover (app-native styling suffices; PROGRESS_FIX stays withdrawn) -- E-419, E-420, E-434, E-435
- [x] T-119 Stranded DOING → demoted: Scroll jumps/snaps back mid-conversation while content loads below; stack-capturing recorder installed | verify: user confirmed no scroll jump mid-stream -- E-421..E-423, E-436, SCROLL_INTENT_FIX
- [x] T-120 Stranded DOING → demoted: Idle CPU: duplicate GLOBAL_CSS injection removed and permanently-dirty out-of-flow elements fixed | verify: user confirmed chatgpt.com idle CPU normal in Brave after update -- E-424..E-427, shipped v1.26.0
- [x] T-150 Wire i18n T() into install.ps1 CLI listing and build messages | verify: Say calls in listing/help use T() lookups; PS parse 0 errors; ALL TESTS PASSED — shipped e24adf7
- [x] T-149 Add -Status switch to install.ps1 | verify: -Status prints manifest table; empty manifest says nothing installed; PS parse 0 errors — shipped cd6df0b
- [x] T-146 Fix encoding debt: mojibake removed, 5 Get-Content → Read-Utf8 | verify: zero mojibake in install.ps1; PS parse 0 errors; ALL TESTS PASSED — shipped c88ad12
- [x] T-147 Add path validation for SaipenviewPath/SmartVacPath/WildRiftPath | verify: C:\Windows\System32 throws clear message; valid path proceeds; PS parse 0 errors; ALL TESTS PASSED
- [x] T-148 Remove 7 hardcoded V:\ paths from install.ps1 defaults, add paths.json fallback for CLI | verify: 0 hardcoded V:\ paths in param defaults; PS parse 0 errors; tests ALL PASSED
- [x] T-145 Fix doc drift: wiki Home/_Footer v1.26.0→v1.26.1, desktop/README.md token count 18→21, README palette table note that 10 shown are subset of 21, README default palette mention golden→goldendefault, desktop README install commands .\install.ps1→.\desktop\install.ps1 | verify: grep v1.26.0 across wiki/ returns 0; grep "eighteen" across desktop/README returns 0; all doc commands run as written from repo root
- [x] T-131 [from saitest ST-001] NomadCode→CodeNomad in six docs (README.md, README.ru/et/ded, wiki/Palettes.md, tools/derive-palette.js) + saitranslate + saiwiki kitchen mirrors -- built by crew circuit, committed with T-113 | verify: rg NomadCode across live docs returns zero matches -- E-465, E-468
- [x] T-113 One machine-level manifest (%APPDATA%\Wintage\installed.json): per target palette, resolved path, app version, payload version, timestamp; written on every successful apply, entry removed on revert | verify: apply/revert round-trip leaves manifest empty; -WhatIf writes nothing -- E-466..E-469
- [x] T-114 `install.ps1 -Reapply`: reads manifest, rediscover paths via re-invoking install.ps1 per target, re-apply outdated payloads; passes through user path params; honours -WhatIf; idempotent | verify: empty manifest -> nothing-to-do; up-to-date -> skips; outdated -> reports action; second run is no-op by construction -- E-472, E-473
- [x] T-115 Simulated-update fixture: tools/test-reapply.ps1 with 6 tests (up-to-date skip, outdated detection, -WhatIf, empty manifest, round-trip, missing target) -- all PASS. Exercises -Reapply pipeline under -WhatIf without touching live installs | verify: 6/6 PASS -- E-474
- [x] T-116 Opt-in logon task (ScheduledTask at logon running install.ps1 -Reapply -Quiet). Register/Unregister via install.ps1 switches + WintageInstaller.ps1 CheckBox. -Quiet suppresses routine messages, only failures reported. Round-trip register→verify→unregister PASS | verify: task exists after Register, gone after Unregister; both files parse 0 errors -- E-476
- [x] T-107 Language-switcher bar: `[EN](README.md) \| [RU](README.ru.md) \| [ET](README.et.md) \| [Дед](README.ded.md)` added after support badge in all 4 READMEs | verify: all links resolve as relative paths; bar renders on GitHub -- E-484
- [x] T-103 i18n loader: Read-I18n/Load-I18n/T() in install.ps1, 4 locale JSONs (en/ru/et/ded) with 22 string keys, auto-detects system language, English fallback | verify: T('Apply') resolves per locale; T('nonexistent')=key itself; PS parse 0 errors -- E-486
- [x] T-132 Node exit codes unchecked in WintageInstaller.ps1: added $LASTEXITCODE guards after apply-themes.js + build-desktop.js in Save-Custom and Delete-Custom | verify: grep confirms guards exist; PS parse 0 errors -- E-480
- [x] T-133 Dead Write-Utf8Lines function removed from install.ps1:62 | verify: rg Write-Utf8Lines returns 0 matches; PS parse 0 errors -- E-480
- [x] T-134 Stale kitchen verify-t017-render-equivalence.js deleted (T-017 DONE) | verify: file absent; recoverable via git -- E-480
- [x] T-135 Orphan tests/run.cmd deleted | verify: file absent; recoverable via git -- E-480
- [x] T-136 Reg import/export $LASTEXITCODE guards added in Invoke-MpcHc | verify: grep confirms guards; PS parse 0 errors -- E-480
- [x] T-137 build-desktop.js $LASTEXITCODE guard added in watch-claude.ps1 | verify: grep confirms guard; PS parse 0 errors -- E-480
- [x] T-128 T-121 shipped a shim Electron refuses to load: the FLOAT_FIX deletion left its head dangling, so the REPAINTER_FIX declaration closed that unterminated literal instead of opening its own and the file died on the next line (SyntaxError: Invalid or unexpected token, main process, Antigravity + FreeBuff would not start). Template repaired, the repainter now travels as a JSON string instead of pasted text (pasted, every backslash-d in its regexes collapsed to a bare d), SHADOW_CSS carried into the payload, prelude supplies T/lum/contrast/elev/injectStyle, and build gates on parse + orphan identifiers | verify: build-desktop PASS, test-shim-payloads PASS incl. new "every generated shim.cjs parses" + "REPAINTER_FIX is valid JS" gates (validated red by deleting `contrast` from the prelude), Run-Tests.ps1 ALL PASSED, both installed shims node --check clean; re-run independently at REVIEW -- E-454, E-455, E-458
- [x] T-129 Wiki restamped to v1.26.0 (Home/_Footer), token count 18 -> 21 in Palettes/Desktop, Development gained inspect-electron.js and the shim/terminal gates, and every FreeBuff ad-removal mention dropped at the user request -- the feature stays in the program, it is just undocumented | verify: repo wiki/ matches .saipen/subs/saiwiki/kitchen/wiki/ and the Wintage.wiki remote at d6d5611; no ad section or patch-freebuff-ads.js line survives a grep -- E-453
- [x] T-130 Repository hygiene found while sweeping for the T-128 defect: the orphaned wintage-nomadcode-color-theme.json (palette renamed nomadcode -> codenomad, nothing generates or reads it), plus the previous checkpoint shape faults -- E-440 split across three physical lines by a literal CR/LF, a raw BOARD ticket line sitting in LOG.md, a fifth BOARD heading, T-120 pasted onto itself, and a style_contract written without reading STYLE.md | verify: every tracked JSON parses, BOARD carries exactly four headings, every LOG line carries [E-###], STATE style_contract matches STYLE.md -- E-456
- [x] T-127 Consume saiwiki OUTBOX via collect: push all 8 kitchen/wiki pages (v1.26.0 stamps) to github.com/vacterro/Wintage.wiki master | verify: remote master = 00a619c, 8 pages byte-identical to kitchen -- E-451, E-452
- [x] T-121 Ship JS repainter to shim, retire blanket wipe | verify: shipped at HEAD 3e1e77a; node tools/build-desktop.js + tools/test-shim-payloads.js PASS -- E-447
- [x] T-122 CodeNomad modal scrim visibility restored | verify: test-shim-payloads.js assertions PASS -- E-445, E-446
- [x] T-126 Silent IO failure and symmetry gap fixed | verify: E-444 BUILD+VERIFY Load-FbSound fixed, Run-Tests PASSED
- [x] T-125 LOG sequence corruption and unverified commits fixed | verify: E-443 VERIFY PASS for commits 4022a4d5/3e898aa5
- [x] T-109 The "squares" across Claude, Antigravity and CodeNomad were icon-font tofu, not broken indicators: [data-cds="Icon"] carries an inline font-family of an icon font, and this theme's blanket Verdana rule excluded only six carriers, none of them these apps'. Chain extended (data-cds Icon, codicon, lucide, octicon, remixicon, phosphor, iconify, feather, data-icon) and gated | verify: gate FAILs when the guard is deleted, naming the marker -- E-401, E-402, E-403
- [x] T-110 Removing SHADOW_CSS from the Electron build was right about the mechanism but took light-DOM surface flattening with it, which CodeNomad and Antigravity depended on. The flattening pair now lives in GLOBAL_CSS with the three guards a document needs: :not(:root), state-marker exclusions, and the floating-surface re-solidify | verify: all six gates PASS, 16 palettes rebuilt -- E-404
- [x] T-111 tools/inspect-electron.js -- reads a live themed app over CDP (targets / rules / eval); `rules` prints the cascade with origin like the DevTools Styles pane. The tool that ended an eight-round guessing loop, kept in the repo instead of a scratch dir | verify: named the winning rule on .status-dot and on body -- E-405
- [x] T-108 Status-dot background was erased by the button-descendant wipe. Exclusions written as :not() ON the wipe, because from user origin `revert` rolls back to the user-agent origin, not the app's -- an undo rule would have looked fixed and stayed broken | verify: engine named both sides before and after -- E-398, E-399
- [x] T-106 Claude Desktop transparency/hot-reload fix series (69d3094..6e18ec1, 5 commits, previously unticketed) | verify: release gates green (check-css, theme-switch, repainter-polarity, electron-shim, theme-packs, build-desktop, test-shim-payloads, test-terminal-font, Run-Tests) -- shipped v1.24.0
- [x] T-104 README.ru/et/ded injected from .saipen/saitranslate/kitchen/ to repo root, UTF-8 clean, tokens/links/fences preserved | verify: E-388 PASS, reviewer green -- shipped v1.24.0
- [x] T-105 saiwiki 8 pages injected to repo wiki/ with .md-adapted links, UTF-8 clean, hex tokens + version stamp preserved | verify: E-389 PASS, reviewer green; wiki-remote push stays with qqq -- shipped v1.24.0
- [x] T-101 Replace unsupported proportional Verdana in Windows Terminal/conhost with a readable installed monospace fallback; clean the Wintage Verdana console registration safely | verify: terminal apply/revert bytes, deliberate proportional-font red, parsers, repo gates and live 7-profile registry readback PASS -- shipped v1.23.3 (7b79fd6), E-373/E-376 VERIFY PASS
- [x] T-100 Restore readable palette foreground in Claude 1.24012.9 without recolouring SVG/icon glyphs | verify: deliberate-red and green shim gate, generated artifacts, repo harness and installed-payload readback PASS -- E-361, E-365
- [x] T-099 Report clipboard and custom-path persistence failures honestly instead of emitting false success or swallowing them | verify: focused failure injection plus PowerShell parsers and repository gates PASS -- E-345, E-347
- [x] T-029 Live Tampermonkey 5.5.0 verification passed: early/late Shadow DOM themed on two origins and theme storage persisted goldendefault->dracula across them | verify: positive live gate PASS, both shadow paths disabled makes it FAIL; Custom restored and v1.23.0 shipped -- E-339
- [x] T-094 Verify the complete current change set, fix confirmed defects, and ship only attributable work | verify: release/repo gates, focused+red round-trips, 16x21 generated matrix, parsers, conformance and live -WhatIf isolation PASS -- E-291..E-293, E-308, E-330..E-332
- [x] T-098 Detect installed and portable Chromium browser profiles, report Tampermonkey, stage the selected browser theme, and open the real install/update flow | verify: isolated discovery/TM/stage/WhatIf/revert fixture, grouped UI, parsers, aggregate gates, safe-path review and live 5-profile readback PASS -- E-318..E-326
- [x] T-097 Total Commander existing relative-age filters use the active palette link colour without altering expressions, order or non-age filters; deployed to both copies | verify: apply/revert fixture incl. ColorDark mirror + deliberate non-age/invalid-age controls, parsers, aggregate gates and live readback PASS -- E-313..E-317
- [x] T-096 Current userscript exposes one menu row per embedded palette; both browser profiles were proven stale and the public update page was opened in active Brave | verify: 15/15 registry/menu, JS parse, CSS/token/version gates PASS -- E-304..E-307
- [x] T-095 Installer GUI launches through WScript with the PowerShell console hidden; legacy CMD forwards to it | verify: live launch showed one visible GUI window titled Wintage Theme Installer and zero visible console-child windows -- E-297..E-299
- [x] T-093 Installer UI separates MY APPS from POPULAR APPS with shared selection/actions, explicit keyboard order and small-screen scrolling | verify: grouped-discovery + deliberate red, parser, build/CSS gates, live open and screenshot review PASS -- E-279..E-284
- [x] T-092 Windows system theme installs/applies immediately, preserves desktop assets, uses one muted active/inactive caption and ___CURRENT___ cursors, and reverts its exact baseline | verify: focused + deliberate red, 15/15 output, parsers, six gates and live readback PASS -- E-268..E-278
- [x] T-091 Add OBS Studio as a first-class target that installs the active Wintage palette and selects it immediately, with exact revert | verify: fixture apply/repaint/revert restores OBS config byte-exact, installer listing/GUI expose OBS, parsers, six gates and live activation PASS -- E-260..E-267
- [x] T-090 Apply Verdana 12 aliased to Windows Terminal and conhost exactly as requested | verify: focused round-trip and deliberate-red gates PASS; JS/PowerShell parsers PASS; live conhost registry reads Verdana on 7/7 profiles -- E-254..E-259
- [x] T-089 Golden Default is the first startup row and every available target starts selected; separate terminal/conhost targets apply the active 16-colour palette and Verdana with exact revert; CodeNomad's native working/idle/compacting/permission/retrying indicator is restored with semantic palette colours and protected from the generic repainter | verify: six gates + parsers PASS; terminal JSONC apply/repaint/revert byte-exact; conhost live apply/repaint restored 175 records exactly then installed goldendefault across 7 profiles; CodeNomad payload installed -- E-232..E-240
- [x] T-084 GUI asked for the saipenview/smartvac/wildrift folder on every tick and forgot it on exit. Remembered in %APPDATA%\Wintage\paths.json (not beside the script -- that is a checkout that gets pulled and re-cloned), dropped on load if the folder is gone, right-click on a target to change it | verify: round-trip written and re-read, dead path filtered out, empty file parses -- E-225
- [x] T-085 GUI wrote custom.json from its own $TOKENS list, which lacked bevelLight and dangerText -- the first Save-Custom after T-080 would have produced a pack apply-themes rejects, killing the custom-theme flow. Both added, with fallbacks pointing at the token each one replaced. Same file's BOM-strip regex still carried the T-076 mojibake | verify: parse OK, 21 tokens in the swatch grid -- E-226
- [x] T-086 Antigravity's caption strip stayed unpainted: the app registers an ipcMain handler for window:set-title-bar-overlay and the renderer calls it on every theme change, overwriting our one-shot. BrowserWindow.prototype.setTitleBarOverlay is patched instead, so the palette wins however often the app calls it; window backgroundColor themed too | verify: setTitleBarOverlay handler read out of the installed asar; redeployed, needs your eyes -- E-227
- [x] T-087 SAIPENVIEW's FAIL badges measured 1.90:1 -- `--danger` was carrying two opposed roles, fill and text. New per-palette `dangerText` (least-changed red that clears AA on that palette's own backgroundSoft; vintageclassic already passes at 10.22:1 and is left alone), split by PROPERTY not selector so no selector list is maintained. Backup left pristine, so revert is still byte-exact | verify: hex-masked diff 696/696 lines structurally identical, 8 text roles moved, 5 fills untouched -- E-228, E-229
- [x] T-088 GUI starts on goldendefault (matching install.ps1's -Palette default) with every themeable target ticked; absent/fused never ticked, and the source-tree three only when their folder is already remembered | verify: parse OK -- E-230
- [x] T-078 Claude desktop looked unpatched because the shim hooked `browser-window-created`: Claude's BrowserWindow is a thin shell and the whole visible app is a WebContentsView, so 42 KB of CSS went into an empty frame and the status file reported success. Hooks `web-contents-created` now -- a strict superset covering WebContentsView/BrowserView/webview/popups | verify: mainView = new WebContentsView(CLAUDE_AI_WEB) read out of the installed build; deployed shim carries the new hook -- E-216, E-217
- [x] T-079 CodeNomad was never themed at all: the target wrote ~/.config/codenomad/custom.css, and grepping its app.asar shows zero customCss/insertCSS -- the app has no code to load it. It is an ordinary Electron app (portable folder, "type": "module") and is now an $ELECTRON target with a resolver that asks the running process first; the dead 43 KB stylesheet is removed on touch | verify: resolver finds V:\...\CodeNomad\resources, listing shows it, install refuses cleanly while the app is running with the archive untouched -- E-218, E-219
- [x] T-080 Bevel highlights read as decoration, not depth: B_OUTER/B_INNER used borderHighlight, which is also the link/accent colour, so every panel edge glowed brighter than the text. New per-palette `bevelLight` token (one lightness step above surfaceAlt, the Win95 rule; a light palette keeps its near-white edge) wired through packs, apply-themes, check-css and both bevel constants | verify: 16 packs PASS at 20 tokens, goldendefault edge #F0D060 -> #75663D -- E-220
- [x] T-081 Decorative scrollbars and stolen gutters: arrows dropped outright (32px of ornament per bar, and they render on shells that scroll nothing), 16px -> 12px, and the shim's scroll fix now hides the bar on any container whose whole range is <= 8px -- overflow this theme's own 2px bevels created. `scrollbar-width: none`, never `overflow: hidden`, so a wrong guess costs a scrollbar and never reachability; re-decided every pass | verify: both injected payloads parse, gates PASS -- E-221
- [x] T-082 Native caption buttons sat on top of the app's own controls (Antigravity is frameless with titleBarOverlay -- the buttons are painted by Chromium, outside the DOM, so no stylesheet can reach them). Shim recolours the overlay to the palette via setTitleBarOverlay and nudges any small interactive element intersecting the strip out from under it, measured from navigator.windowControlsOverlay | verify: payload parses; needs your eyes after a restart -- E-222
- [x] T-083 Two bugs found while reading the paths above: a FIRST in-place install copied the shim without redirecting its `require(ASAR)`, so after a Claude update it would have resolved through the very `main` field being patched, loaded itself as an empty module and never started; and the listing asked every Electron target for a package.json the in-place mode never writes, so Claude reported "found, not themed" while patched and running | verify: listing now reads claude=themed goldendefault -- E-223
- [x] T-075 Total Commander wincmd.ini encoding regression: stripping the BOM in T-066 caused Win32 INI parsers to fall back to ANSI, mojibaking Cyrillic paths. Fixed by adding a Write-Utf8BomLines helper exclusively for TotalCmd to preserve the UTF-8 BOM | verify: wincmd.ini writes with a BOM, all other targets write without a BOM -- E-210, E-211
- [x] T-076 Corrupted BOM stripping regex in 	ools/install-electron.js contained literal mojibake (п»ї) instead of the safe \uFEFF escape. Replaced with \uFEFF | verify: script passes --check and patches ASARs cleanly -- E-212, E-213
- [x] T-077 Desktop themes were STALE because wintage.user.js was modified (Support developer badge change) but build-desktop.js was not run. Rebuilt the artifacts | verify: node tools/build-desktop.js --check passes -- E-214, E-215
- [x] T-071 WintageInstaller: FolderBrowserDialog now remembers previously selected custom paths when targets are unchecked and re-checked instead of resetting to defaults | verify: uncheck SAIPENVIEW and check again, old path restored -- E-206, E-207
- [x] T-072 Refactored raw [System.IO.File]::ReadAllText calls left over in Invoke-Saipenview/Invoke-SmartVac/Invoke-WildRift to use the project's standard Read-Utf8 helper introduced in T-066 | verify: no System.IO.File left in file operations -- E-208, E-209
- [x] T-070 `-Target all` skipped codenomad, discord, totalcmd, totalcmd2 and obsidian - the dispatch list was hand-kept and had drifted twice. Built from one declared registry now, with a startup guard that reads the ValidateSet back and warns about any target no dispatch list covers | verify: -Target all -WhatIf touches 12, no orphans, all gates PASS -- E-202, E-203
- [x] T-066 The BOM/ANSI encoding pair was still writing five targets besides SAIPENVIEW (wincmd.ini x2, WildRift .py, CodeNomad css, BetterDiscord css, Obsidian appearance.json -- where a BOM makes JSON.parse throw). One Read-Utf8/Write-Utf8 helper trio, every call routed through it | verify: zero `Set-Content -Encoding UTF8` left; the BOM already on disk in the parent vault stripped, both appearance.json parse -- E-195, E-196
- [x] T-067 Total Commander had no backup and its revert deleted every colour key in the whole ini, including ones the user set before Wintage. Backed up once before the first write, revert restores from it; the no-backup fallback strips only [Colors]/[ColorsDark] and says what it cannot restore | verify: parse OK, all three inis found already themed with no backup -- E-197, E-198
- [x] T-068 GUI would die on a BOM'd pack (ConvertFrom-Json throws, theme list empty) and on any pack predating a token (null reaches ColorTranslator and crashes the window; link was added 19th). Explicit UTF-8 read + per-token fallback | verify: GUI opens after hardening -- E-199
- [x] T-069 My own T-061 backdrop rule called getBoundingClientRect on every url() background - a forced layout on hundreds of elements on sprite-heavy pages, the exact thrash ADR-004/ADR-006 exist to prevent. Gated behind a zero-cost DOM-depth test first | verify: all 8 gates PASS, Steam's backdrop still qualifies -- E-200
- [x] T-058 Claude in-place patch: the existing mechanism (overwriting a sentry-dbid literal in index.pre.js) does not exist in this build and would have thrown; replaced with a byte-length-identical rewrite of `main` in the archive's package.json + integrity hash update, so the archive never moves and resourcesPath helpers keep working | verify: on a copy - size unchanged, package.json parses, integrity matches, shim hands off, revert byte-identical (md5) -- E-190, E-191, E-192, E-193
- [x] T-060 reuters.com blank body: our 0.001s animation rule caused reveal snapback (base opacity:0 + fill-mode-less reveal animation). animation-fill-mode: forwards in both stylesheets | verify: mechanism reproduced in a real Chromium, computed opacity 0 without the fix -- E-181, E-182
- [x] T-061 steamcommunity artwork bleeding around the themed column: url() backgrounds were preserved with no size sense. html/body lose theirs outright; the repainter kills any covering >70% of the viewport in both dimensions | verify: node --check + check-css PASS, measured only when a url() is present -- E-183
- [x] T-062 Antigravity decorative scrollbars: Monaco keeps its own scrollbar shells in the DOM permanently and our surface+bevel rules painted them. Shells handed back to the app, slider still tinted | verify: rebuilt and reinstalled to Antigravity + Freebuff; needs your eyes -- E-184
- [x] T-063 theme-switch test pinned things it does not own (DEFAULT_THEME, active entry at index 0, every menu row a theme) and went red on the user's own changes; now reads them from the source | verify: theme-switch test PASS -- E-185
- [x] T-059 SAIPENVIEW's style.css had regressed to the pre-T-054 append behaviour (working-tree drift, never committed that way -- HEAD already had T-054's fix, something local just hadn't run it). While there: default `$Palette` in install.ps1 and `DEFAULT_THEME` in wintage.user.js changed from `golden` to `goldendefault` per user request. v1.18.1 -> v1.18.2 | verify: SAIPENVIEW's own repo, style.css restored from style.css.bak (byte-identical to its HEAD); `node --check` n/a (PowerShell), install.ps1 parses clean via [Parser]::ParseFile -- done from the SAIPENVIEW side of a cross-repo session, logged here for the record
- [x] T-057 Claude opened the web version after patching: its main resolves the renderer through app.getAppPath(), which the relocation repointed at the shim folder, so the local load failed and it fell back to claude.ai. Shim now restores getAppPath() to the archive | verify: renderer present in archive (112589 bytes), fix installed in all 3 Electron targets -- E-167, E-168
- [x] T-053 release.ps1 git steps: Git-Safe helper (stderr merged, success judged by $LASTEXITCODE) + core.autocrlf=false, replacing the safecrlf attempt that still died under a pipe | verify: a full release run finishes commit+push with no manual finish -- E-156
- [x] T-054 SAIPENVIEW recolour instead of stylesheet append: the old handler bolted the whole 39 KB browser CSS on top, which rewrote the box model and moved the text; now only `--token:#hex` values in its own :root change | verify: hex-masked diff STRUCTURALLY IDENTICAL, 695 lines both sides, no BOM, revert restores byte-for-byte -- E-157, E-158, E-159
- [x] T-055 Two CRLF bugs found by hunt: apply-themes --check could never go green on a CRLF working copy (release gate permanently red, git diff showed nothing), and test-theme-packs' half-marked fixture stripped nothing because JS dot does not match CR -- so a green test covered nothing | verify: --check stable across runs, theme-pack test PASS with a genuinely half-marked fixture -- E-160, E-161
- [x] T-056 `Wintage Installer.cmd` at the repo root so the GUI is obvious to open; both READMEs updated, desktop table extended to every current target | verify: launched via the .cmd, window title correct -- E-162
- [x] T-047 GUI polish accepted as-is by the user for now; reopens if something reads wrong -- E-163 | verify: E-163 user accepted as-is, reopens if wrong
- [x] T-030 Split link out of borderHighlight (19th token): bevel edge and hyperlink are separate now. Dark palettes set link=borderHighlight (rendering byte-identical, golden link #D3B57A unchanged); the one light palette gets a near-white bevel + a dark AA-safe link. Both generators, both gates, all templates and the GUI updated | verify: 15 dark packs unchanged, vintageclassic bevel #5E7A7A->#F6F6F6 + link 4.62:1, all gates green -- E-147, E-148
- [x] T-050 Obsidian support: 16 palettes as per-vault community themes derived from the user's own VintageWin95 theme.css; install into every vault, activate the chosen one with an appearance.json backup, revert leaves hand-made themes untouched | verify: 16 installed into 2 vaults, golden active + present, revert 16->0 with VintageWin95 intact -- E-127, E-128
- [x] T-051 Release-breaker: themes/codenomad.json was never generated after the nomadcode->codenomad rename, so derive-palette --check (a release gate) FAILed. Regenerated | verify: derive-palette --check clean -- E-125
- [x] T-052 build-desktop never pruned output, so renamed/removed palettes left ghost out dirs (nomadcode) that would install. Added prune() | verify: both nomadcode dirs pruned, --check flags orphans -- E-126
- [x] T-034 MPC-HC: dark theme + dark title bar + OSD at Verdana 16 with zero transparency and a border; player chrome colours are compiled in and no registry value exposes them, so the installer says so instead of implying coverage. Settings exported once to desktop/backup/mpc-hc-settings.reg | verify: applied, listing shows 'mpchc themed n/a - colours are compiled in' -- E-119
- [x] T-028 release.ps1 bumps W95_VERSION with @version, check-css FAILs on a mismatch | verify: 'version: PASS (1.14.0 in both places)'; the drifted 1.4.7 stamp is gone -- E-120
- [x] T-025 Companion browser themes: 15 Chromium manifests generated from a role-based template | verify: golden's frame equals its background token, 15 emitted -- E-121
- [x] T-037 desktop/README.md: per-target mechanism + update survival, the Electron shim's two traps, Claude's fuses, MPC-HC's ceiling | verify: every documented command runs as written -- E-122
- [x] T-026 Root README: fifteen-palette section, per-user switch, pack re-application after an upgrade, pointer to the desktop docs -- E-122 | verify: E-122 release verified
- [x] T-038 Stray files removed and gitignored (folded into T-044) -- E-109 | verify: E-109 folded into T-044
- [x] T-048 Installer GUI documented inside desktop/README.md rather than as a second file -- E-122 | verify: E-122 release verified
- [x] T-044 Removed scratch.py/scratch2.py/wintage.user_old.js and widened .gitignore so release.ps1's `git add -A` stops re-committing strays | verify: files gone, .gitignore covers scratch*.py and *_old.js -- E-109
- [x] T-045 Imported FastPrompter's nine themes as packs (blended to 18 tokens, semantic trio kept at UI.md, six AA lifts reported) | verify: 15 packs all PASS check-css; Vintage Classic imported LIGHT, exercising the T-019 polarity work -- E-110, E-111
- [x] T-046 desktop/WintageInstaller.ps1 -- WinForms GUI: theme list with colour chips, target checklist parsed from install.ps1's own output, live Win95 preview, 18 colour swatches, Custom fork on edit, live WCAG readout, Apply/Save/Revert | verify: parse PASS, window alive with correct title, 6/6 targets parsed incl. 'claude fused shut' -- E-112, E-113, E-114
- [x] T-042 Claude desktop will not launch when themed: Electron fuses OnlyLoadAppFromAsar + EnableEmbeddedAsarIntegrityValidation are both ENABLED, so the shim can never run and repacking is closed off too. tools/electron-fuses.js now reads them and the installer refuses BEFORE moving anything | verify: Claude refused with both reasons, exit 1, app.asar untouched; Freebuff/Antigravity unaffected -- E-104, E-106
- [x] T-043 Per-target palette picking: install.ps1's listing shows present/themed/which-palette per app and flags fused-shut ones | verify: table reads 'claude fused shut', 'freebuff themed golden', 'antigravity all (pick in the editor)' -- E-107
- [x] T-041 Scrollbar gutters never disappearing in Electron targets: styling ::-webkit-scrollbar makes overlay scrollbars classic, so every `overflow: scroll` container kept a permanent 16px gutter -- shim now converts computed scroll->auto from script; also found and fixed a triple injection of the same stylesheet per document | verify: status file reads one stylesheet + one scrollfix per document on splash and app -- E-100, E-101, E-102
- [x] T-040 Golden everywhere, and an in-place repaint path: changing the palette of an already-themed Electron app failed because the archive had moved; the installer now swaps the payload without touching it (works with the app running) | verify: Freebuff + Antigravity agent app repainted to golden, status 'injected 39361 bytes', css carries #1A0F05 -- E-098
- [x] T-033 Claude desktop app (Electron, not the CLI - the screenshot settled it): shim target declared, palette claudecode, install blocked by EBUSY only because the app is the one running this session | verify: installer reports 'close it completely and run this again' and changes nothing, proven against the live app -- E-094
- [x] T-035 FreeBuff (Electron): archive relocated to resources/app/app.asar, shim.cjs injects the stylesheet extracted from wintage.user.js | verify: wintage-status.txt reads 'injected 39350 bytes', app window opens, revert restores app.asar -- E-090, E-091, E-094
- [x] T-036 NomadCode: target declared with its resources path; NOT installed on this machine (AppData leftovers only), so it is reported as absent rather than faked | verify: install.ps1 lists it 'NOT found on this machine' and skips -- E-094
- [x] T-039 Antigravity agent app (Electron, separate program from the IDE) themed with the antigravity palette | verify: installed, antigravity 2.4.3, name/version preserved -- E-094
- [x] T-031 desktop/ subsystem: tools/build-desktop.js fills ${token} templates from the same themes/*.json packs, desktop/install.ps1 installs into the user profile with -WhatIf, -Revert, timestamped backups and a staleness refusal | verify: build --check clean, install run twice with backup-then-replace, -WhatIf touched nothing -- E-082
- [x] T-032 Antigravity + VS Code: six colour themes generated from a template DERIVED from the user's own hand-made Win95 theme (96/99 keys mapped to tokens) | verify: generated golden byte-equal to the original (99 colours, 14 tokenColors), other palettes differ, no placeholders left, installed to both -- E-083, E-084
- [x] T-022 K-Lite (MPC-HC) palette -- graphite + silver, neutral by design (MPCTheme=1 confirms the dark UI; MPC's dark chrome carries no hue) | verify: 7.56 / 4.84 / 5.67 : 1, AA gate PASS -- E-076
- [x] T-023 FreeBuff palette -- lime #7CFF3F accent read out of the installed app.asar, slate surfaces | verify: 10.49 / 7.06 / 8.40 : 1 -- E-075, E-076
- [x] T-024 NomadCode palette -- indigo #624AFF read out of the app's own Electron cache; needed an AA lift the gate demanded | verify: 5.65 / 4.72 / 4.77 : 1 after lifting two tokens -- E-076
- [x] T-020 Claude Code palette -- re-derived from golden per UI.md (coral #D97757 hue on warm near-neutral surfaces) after the user ruled that every theme is UI.md, not a copy of the product UI | verify: 8.18 / 5.34 / 6.13 : 1 -- E-074, E-076
- [x] T-021 Antigravity palette -- re-derived from golden, teal #09b6a2 read out of the IDE's own tailwind.config.js (not the Google blue a guess would have picked) | verify: 10.64 / 7.16 / 8.67 : 1 -- E-075, E-076
- [x] T-027 Theme packs + installer: themes/<slug>.json as the palette source, tools/apply-themes.js generating the block into ANY wintage.user.js (markers if present, brace-walk + add markers if not), install-themes.ps1 with -Latest/-Check for re-applying after an upgrade | verify: tools/test-theme-packs.js 18/18 PASS incl. patching the last released commit and byte-identical re-runs -- E-066
- [x] T-019 Palette-independent repainter: elev() normalises incoming luminance into the active theme's polarity (identity on a dark theme, so golden is provably untouched), text contrast measured against the theme's own backdrop, color-scheme + data-w95-dark follow the theme, per-theme WCAG AA floor in check-css.js | verify: tools/test-repainter-polarity.js 21/21 PASS, AA gate rejects a forced 2.70:1 textPrimary -- E-062
- [x] T-018 Theme switching: GM-backed selection (per-user, not per-origin), one menu entry per theme in the top frame only, reload on switch, total fallback chain; @sandbox raw keeps page context so the attachShadow interception survives the grants | verify: tools/test-theme-switch.js 22/22 PASS, gated into release.ps1 -- E-055
- [x] T-017 Theme registry: THEMES map keyed by slug, T = THEMES[active].tokens, first paint no longer hardcodes golden; check-css.js swapped its frozen palette set for "zero literal hex in a CSS body" plus a per-theme table check | verify: GLOBAL_CSS + SHADOW_CSS byte-identical to HEAD, 18/18 tokens identical, 4 injected fault classes all rejected -- E-047, E-048, E-049
- [x] T-001 Tame animations: animation-duration/-delay 0.001s, global+shadow, verified safe, shipped v1.2.0 | verify: E-001 live-tested on a real page (sealed LOG-001); shipped v1.2.0
- [x] T-002 Kill style-recalc thrash: process() reads only, writes batched into one flush per sweep | verify: force tick 250ms -> 19-42ms, 996-element pixel diff vs v1.2.1 = 0
- [x] T-003 Sub-frames get CSS + observer + 3 bounded settling sweeps, no interval (@noframes rejected: would leave embeds unthemed) | verify: E-008 v1.3.0 BUILD (sealed LOG-001)
- [x] T-004 Force sweeps demand-driven (new DOM / changed sheet / tab return / 30s heartbeat), not blind every 4.5s | verify: settled-page tick 1.3ms, 0 long tasks
- [x] T-005 Mutation batch de-dup: skip records whose ancestor is in the same batch, skip already-detached nodes | verify: E-008 v1.3.0 BUILD (sealed LOG-001)
- [x] T-006 Full UI.md token block; off-palette link colour #9DD9F9 -> --borderHighlight; ::selection -> --selection | verify: 0 off-palette bg + 0 off-palette text across 16921 live elements
- [x] T-007 Depth language: 2px 4-value bevels, global box-shadow/text-shadow none, scrollbars rebuilt from borders, disabled keeps its bevel, dialogs + th raised | verify: 0 box-shadows live
- [x] T-008 Zero blur/gradient: backdrop-filter none, filter none except media, ALL gradient hues killed in JS, progress/meter/slider exempt | verify: 0 gradients live
- [x] T-009 Type ladder 12/14/16/10 + line-height 1.2 + two weights | verify: only 10/12/14/16px and only weights 400/700 present live
- [x] T-010 Control metrics: input/select 20px, textarea 64px, button padding 2px 6px min 24x20, focus outline-offset -4px | verify: E-015 v1.4.0 BUILD + E-019 live 16921-element check (sealed LOG-001)
- [x] T-011 Semantic colours: saturated backgrounds snap to --success/--warning/--danger by hue sector; NOT used as text colour (fails AA) | verify: E-015 v1.4.0 BUILD + E-020 no-text-colour rule (sealed LOG-001)
- [x] T-012 ADR-003 recording the deliberate UI.md deviations; tools/check-css.js guard added and gated into release.ps1 | verify: E-019/E-020 v1.4.0 live check + ADR-003 (sealed LOG-001)

- [x] T-013 Pause infinite animations instead of driving them to 1000 iterations/sec | verify: infinite div/svg/in-button all animation-play-state:paused, finite still running
- [x] T-014 Idle-CPU review of the reworked scheduler: identity-based self-write suppression replacing the 100ms time window | verify: self-inflicted style records 9466 -> 0, backoff ladder reaches 60000, foreign inline write still repainted
- [x] T-015 Fix the sweep-rate hot loop: MIN_SWEEP_GAP floor, no 0ms scheduling, never replace a sooner timer | verify: 148 -> 11 sweeps per 15s under identical churn
- [x] T-016 Enforce UI.md invariants from JS where CSS loses to site !important; palette clamp for control subtrees; close dark-surface gaps; palette idempotence | verify: 3 popular sites all zero violations, zero drift across two settling windows

## BLOCKED
- [ ] T-173 [MARKHUNT] T-143 DONE carries a false verify claim: it states "the named writeAtomic/backupOnce identifiers no longer exist (resolved by T-146/T-154)", but `git grep` at HEAD shows function writeAtomic still defined in tools/install-windows-theme.js:42 and tools/install-obs.js:33, and function backupOnce in tools/install-obs.js:40 (near-duplicate bodies, differing only in tmp-name pid suffix + 'utf8' arg) -- T-146/T-154 were encoding tickets (Get-Content->Read-Utf8, mojibake), nothing to do with write-atomic helpers; the architectural duplication T-143 flagged (writeAtomic x3, backupOnce x2) is only partly resolved and the DONE ticket cannot be trusted as closed | verify: either writeAtomic/backupOnce consolidated to one shared helper across the JS tools (install-obs.js + install-windows-theme.js), or T-143's DONE verify rewritten honestly and the remaining duplication ticketed with real counts | blocker: unvetted audit -- tools/install-windows-theme.js:42, tools/install-obs.js:33,40 (T-170 sweep)
- [ ] T-174 [MARKHUNT] WintageInstaller.ps1:191-193 carries hardcoded personal-machine paths as GUI folder-dialog defaults: $PATH_DEFAULTS saipenview='v:\___VAC\__K\__CODE\_PY\_SAIPENVIEW\', smartvac='v:\___VAC\__K\__CODE\_PY\_SMART_VAC_CLEANER\', wildrift='v:\___VAC\__K\__CODE\_PY\_WR\WildRiftAssistant\' -- the exact T-148/T-153/T-155/T-164 class (hardcoded V:\ paths removed from install.ps1, install-browsers.ps1, README, import-fastprompter.js) but never swept in the GUI; on any other machine the dialog opens at a nonexistent path (still lets the user browse, so it is a portability defect not a breaker) | verify: zero V:\___VAC in WintageInstaller.ps1; $PATH_DEFAULTS empty or derived from env (e.g. $env:USERPROFILE), dialog falls back to the default browse location | blocker: unvetted audit -- desktop/WintageInstaller.ps1:191-193 (T-170 sweep)
- [ ] T-102 Dedicated saitranslate instance: translate the Wintage README (and, when bundled, desktop/README.md + browser-theme/README.txt) into the 29 subSaipen languages (Japanese, Ukrainian, German, French, Spanish, Italian, Portuguese, Dutch, Polish, Swedish, Danish, Finnish, Norwegian, Chinese, Korean, Thai, Vietnamese, Arabic, Hebrew, Turkish, Hindi, Indonesian, Greek, Czech, Romanian, Hungarian, Bulgarian, Slovak, Croatian); Core share EN/RU/ET/Дед already built in .saipen/saitranslate/kitchen/ | blocker: phases/translate.md gives these 29 languages to the dedicated saitranslate instance, so Core must not grind them here; cleared by running that instance (ee), never by Core picking this up | verify: 29 locales present, OUTBOX flips to status: ready
- [ ] T-065 YouTube Studio analytics bars disappear under the theme. Exact class plus computed background-color/background-image is required before a narrow fix | blocker: analytics DOM requires the user's authenticated Studio; running Cent/Brave expose no CDP, and launching/copying the live profile would disturb or duplicate private session data | verify: bars visible again on the analytics table
