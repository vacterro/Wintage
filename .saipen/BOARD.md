# BOARD

## DOING
- [ ] T-222 Audit handoff implementation (AUDIT_ALL_3, 41 tickets: 7 DONE in this goal run, 14+ net-new remain). CORE wave complete (T-205..T-211). Source SRC-001 captured. | in progress (W2/PERF waves remain)

## DONE
- [x] T-211 CORE-012 README docs contract: README advertised Consolas 12 (live is Terminus TTF), "kept byte-for-byte" Terminal Revert (live is owned-field merge), "No application byte is rewritten" Electron (live supports fuse defuse with byte-exact <exe>.wintage-fuse.bak, fail-closed UNVERIFIABLE). Fix: README updated to live contracts. | verify: test-readme-contract 11 PASS (live-vs-readme Terminus + merge + defuse, anti-patterns gone) [E-716]
- [x] T-210 CORE-011 Terminal recorded-set Reapply health: Terminal marker probe walked the full discovery (Get-WindowsTerminalSettingsPaths) and demanded a marker on every file -- contradicting the multi-item ownership contract. A new unowned Preview settings file turned a healthy install unhealthy. Fix: probe now uses manifest-recorded item set ($recorded) so newly discovered unowned files are reported informationally, not adopted. | verify: test-terminal-recorded-set 7 PASS (structural: branch uses $recorded, not full discovery; behavioural: 2/2 recorded markers all present, full-discovery would have flagged 2/3) [E-715]
- [x] T-209 CORE-010 production force-sweep continuation: wintage.user.js requestForceSweep owed ONE 2500-window per call with NO continuation, capped at 2. A 6000-element page covered one window and idled with a remainder never re-verified. Fix: requestForceSweep anchors a whole-lap debt (forceLapOrigin), runSweeper re-arms floor-limited (MIN_SWEEP_GAP) slices until cursor wraps past origin, then closes lap and clears debt. Old per-request cap removed. | verify: test-force-sweep-continuation 7 PASS (6000-element stub DOM fully covered in 3 slices, finalDebt 0, continuation re-arms via MIN_SWEEP_GAP); node --check PASS [E-714]
- [x] T-208 CORE-005 absent-dir rollback representation: Save-DirPreState returned $null for absent dirs; Restore no-op for null. First-install commit failure left a newly-created directory behind while the manifest stayed at its previous (absent) state. Fix: structured snapshot {Existed, SnapshotPath}; Restore removes current dir when Existed=false (first-install), temp-sibling swap when Existed=true; snapshot cleaned in finally. | verify: test-dir-prestate 25 PASS (existing + first-install + lost-snapshot + no-op + cleanup) [E-713]
- [x] T-207 CORE-009 portable Electron manifest path semantics: Resolve-PortableElectron treated the manifest-recorded path as an APP ROOT and appended 'resources', but install.ps1 records $e.Resources (the resources dir) -- so Reapply against a moved target tested <resources>/resources. Fix: manifest-recorded candidate used byte-exact; explicit/remembered/process/default remain app roots with 'resources' appended. | verify: test-resolve-portable 11 PASS [E-710]
- [x] T-206 W2-009 durable-recovery consumption ordering: SmartVac/WildRift/SAIPENVIEW Revert deleted <file>.bak BEFORE Invoke-TargetCommit, stranding manifest-without-recovery on process death. WIP's provenance stamping made the orphan worse. Fix: Remove-Item moved INSIDE the commit scriptblock; Restore-FilePreState keeps in-memory bak safety net. | verify: test-recovery-consumption 15 PASS [E-708]
- [x] T-205 CORE-008 corrupt install-epoch fail-closed: Get-InstallEpoch silently rotated a corrupt/missing-id epoch file, disconnecting every provenance-stamped recovery. WIP amplifies: stamps every recovery. Fix: existing file parsed strictly (object + non-empty string id), corrupt files THROW and preserve original bytes, no rotation; absent created atomically. | verify: test-epoch 21 PASS [E-705]

## TODO

## DONE
- [x] T-207 CORE-009 portable Electron manifest path semantics: Resolve-PortableElectron treated the manifest-recorded path as an APP ROOT and appended 'resources', but install.ps1 records $e.Resources (the resources directory itself) -- so Reapply against a moved target tested <resources>/resources, never resolved the recorded install, and fell through to a different installation or "missing". Fix: manifest-recorded candidate is now used byte-exact as the already-resolved resources directory; explicit/remembered/process/default candidates remain app roots and still append 'resources'. | verify: test-resolve-portable 11 PASS (new; manifest path returned byte-exact, never /resources/resources, precedence intact), test-epoch 21, test-ownership 39, test-freebuff 72, test-recovery-consumption 15, test-reapply 183+1 pre-existing unrelated FAIL [E-709..E-710]
- [x] T-206 W2-009 durable-recovery consumption ordering: SmartVac/WildRift/SAIPENVIEW Revert deleted <file>.bak BEFORE Invoke-TargetCommit, so a process death at that seam stranded manifest-says-installed + no recovery = permanent fail-closed Revert; the WIP's provenance stamping made the orphan worse (a stamped orphan a fresh Apply could adopt). Fix: backup removal moved INSIDE the commit scriptblock (runs only after Remove-ManifestEntry succeeds), Copy-Item restored first, Restore-FilePreState keeps in-memory bak safety net. | verify: test-recovery-consumption 15 PASS (new; structural no-pre-commit-removal for all 3 call sites + commit scriptblock performs the removal + Save/Restore-FilePreState byte-exact round-trip), test-epoch 21, test-ownership 39, test-freebuff 72, test-reapply 183+1 pre-existing unrelated FAIL [E-708]
- [x] T-205 CORE-008 corrupt install-epoch fail-closed: Get-InstallEpoch silently rotated a corrupt/missing-id epoch file into a fresh GUID, disconnecting every provenance-stamped recovery file from the install that wrote it -- and the WIP stamps every recovery with this epoch, so one bit-flip would lock the user out of every target. Fix: existing file is parsed strictly (object root + non-empty string id), corrupt/empty/non-object files THROW and preserve original bytes exactly, no rotation; absent file created atomically (temp+rename) exactly once and stable across calls. | verify: test-epoch 21 PASS (new; absent-create-once-stable, valid-unmodified, corrupt/emptyid/array THROW + bytes preserved + retry still throws, same-epoch provenance accepted, foreign still rejected), test-ownership 39, test-freebuff 72, test-reapply 183+1 pre-existing unrelated FAIL [E-705..E-707]
- [x] T-204 Terminal scrollbar missing (user report) -- 64-bit PowerShell conhost profile buffer height (43) == window height (43) = zero scrollback = no scrollbar; conhost rewrites ScreenBufferSize into the registry on window resize, silently collapsing Wintage's 9001 floor after apply (marker intact, health probe never noticed). Fix: conhost Reapply health scans every conhost key for a buffer below the 9001 floor (Test-TargetNeedsReapply 'conhost' case, shared $CONSOLE_SCROLLBACK_HEIGHT constant) so Reapply re-asserts it; also fixed the latent crash this exposed -- the generic path-moved comparison ran GetFullPath on registry-key targets (conhost/mpchc HKCU:\ path), which throws NotSupportedException under PS5.1 .NET Framework, crashing every such Reapply; GetFullPath now skipped for HKCU/HKLM/Registry:: paths. Live 64-bit PowerShell buffer restored to 9001 (width 79 preserved). | verify: test-reapply 184 PASS (new test 36 reasserts a collapsed buffer through -Reapply), test-ownership 39 PASS, test-freebuff 72 PASS, Run-Tests ALL PASSED, live buffer verified 9001 [E-700]
- [x] T-203 Ship v1.28.0 -- verified-unshipped scope T-201 + T-202 (T-196..T-199 already shipped in v1.27.0): release.ps1 untracked-refuse guard, Terminus console font, installer language selector (en default). Bumped 1.27.0->1.28.0 (both stamps) + CHANGELOG, staged 10 feature files by explicit path (4 E-692 audit harnesses excluded), gates green (13 node + Run-Tests ALL PASSED), atomic push main+tag v1.28.0 landed. | verify: v1.28.0 pushed 5518514, remote main+tag verified, all release gates PASS [E-699]

- [x] T-201 release.ps1 stages with `git add -A`, so any untracked file present at release time is published with it. Added a refuse guard: `git ls-files --others --exclude-standard` is checked before `git add -A`, and any untracked file aborts the release, listing exactly what would have ridden along. `.gitignore` extended with `.saipen/recovery/{settled,conformance}/` (transient engine receipts) so only genuine blockers trip it. | verify: guard refuses (counts + lists) on the 4 E-692 audit harnesses left untracked, release.ps1 parses with 0 errors [E-697]
- [x] T-202 Terminal font -> Terminus (TTF) for Windows everywhere + GUI/CLI language selector, English default (user requests). $CONSOLE_FONT/TERMINAL_FONT + Run-Tests asserts updated; HKCU\Console + 6 profiles FaceName applied live; i18n.ps1 defaults 'en' + per-machine pick persisted, GUI ComboBox re-strings live, install.ps1 -Language with hard-fail. | verify: node tools/test-terminal-font.js PASS (conhost/WT agree, not Verdana), WintageInstaller.ps1/install.ps1/i18n.ps1 parse 0 errors, i18n smoke PASS (en default, 33 locales, T lookup), CLI -Language ru/unknown verified, Run-Tests terminal+font+i18n asserts PASS (3 unrelated env-flaky subsuites FAIL, pre-existing class) [E-694..E-696]
- [x] T-199 Every desktop target failed under PowerShell 7 with `manifest schema invalid: <target>.applied: not a string`: PS6+ retypes timestamp-looking JSON strings into `[datetime]`, so `Test-ManifestSchema` rejected the installer's own manifest before any work started, while 5.1 (what the GUI and the logon task spawn) was fine. `Read-Manifest` now normalises date-typed manifest fields back to ISO-8601 UTC strings for every host; `-DateKind` was not usable because 5.1 has no such switch. | verify: Run-Tests ALL PASSED under pwsh 7.6.4 and 5.1, new host-independent typing gate (rejects the date-typed manifest, accepts the normalised one, asserts the ISO shape) plus an opportunistic `pwsh install.ps1` listing check; test-reapply 178 PASS, test-ownership 39 PASS, test-freebuff 72 PASS
- [x] T-198 tests/Run-Tests.ps1 carried two pre-existing red gates that blocked every ship: the terminal round-trip fixture compared a CRLF here-string against install-terminal.js's LF output, and the `$TARGETS`/`$ELECTRON` extraction regex closed on the first NESTED brace so fully wired targets (workbuddy, codenomad, antigravity-app, vscode) were reported as unimplemented. Fixture normalised to LF; both block patterns now close on a column-0 brace and keys are anchored per line. | verify: Run-Tests.ps1 ALL PASSED, both failures reproduced first at clean HEAD in a detached worktree, and a bogus ValidateSet target still fails the impl gate
- [x] T-197 FreeBuff's `/api/ad/slot` orchestrator patch stopped matching when the app moved to `app.ads.slotAd(threadId, recent)`; the exact+regex matcher is argument-agnostic now. | verify: tools/test-freebuff.ps1 72 PASS, 0 FAIL
- [x] T-196 WorkBuddy AI target finished on both surfaces: CLI already had the ValidateSet entry, `-WorkBuddyPath`, the `$ELECTRON` row and `Get-WorkBuddyResources`; the GUI now groups it with the portable/source apps, and `Save-CustomPaths` MERGES paths.json instead of rebuilding it from `$PATH_TARGETS` -- that rebuild deleted every CLI-owned key (`codenomad`, `workbuddy`, `portable`) on any unrelated folder pick. | verify: install.ps1 lists `workbuddy WorkBuddy AI found, not themed`, `-Target workbuddy -WhatIf` plans the relocation install with app.asar and installed.json hashes unchanged, new behavioural paths.json test PASSes while the pre-fix function lifted from HEAD wipes codenomad+portable in the same harness

- [x] T-195 Tampermonkey UI bugfixes: ::selection overrides fixed, heavy-site (err.ee) white blocks fixed via body reset + mutation limit bump, Radix/Monaco popup transparent backgrounds fixed, inline code line-height overlap fixed. | verify: completed per E-683 -- fixes live in wintage.user.js (err.ee body reset + mutation limit, ::selection overrides, Radix/Monaco popup backgrounds, code line-height)
- [x] T-194 Fix target installation failures: terminal (wt.exe/settings.json resolution), smartvac (WIN95_SURFACE anchor mismatch), obs (ReadAllBytes on config directory instead of user.ini), browsers (works as designed for unowned old stages). | verify: all failing targets apply correctly without errors or anchor mismatches.
- [x] T-193 FreeBuff 0.0.55 theme re-assert and ad-patch matcher fix | verify: test-freebuff 72 PASS, install.ps1 -WhatIf green, shipped v1.26.9 (4d09163) [E-678..E-681]
- [x] T-192 [user surgical correctness pass] audit at HEAD 121a7de (base ac5a6cf) - DONE. Reproduced against current code: P0#1/#2/#3/#5-#13 + P1#17/#19/#21/#22/#24 already shipped in T-190/T-191; this pass fixed the still-failing P0#4 conhost revert recovery ordering (backup retained until manifest transition, themed-state rollback on failure, regression test 31), P0#14 browser stage ownership (owner marker + refuse unowned + staged atomic swap, regression test 32), P1#15 vscode/antigravity persistent non-pruned recovery under WINTAGE_APPDATA/recovery with CREATED_BY_WINTAGE/REPLACED_PREEXISTING + repaint-never-overwrites-pristine (regression test 30 extended), P1#18 SAIPENVIEW provenance rebase (owned tokens from old pristine, never wholesale themed live CSS, regression test 33), P1#20 manifest schema validation (Test-ManifestSchema on read+write, syntax-valid garbage rejected, unknown keys preserved, regression test 34), P1#23 test-reapply -List rebuilt to 34 tests, P1#25 release atomic git push --atomic branch+tag (fail-before-split, tag availability pre-checked), P1#26 GUI Save/Delete-Custom transactional via Invoke-CustomMutation (pack restored + outputs regenerated on generator failure), P1#27 windows theme failure rollback (DWM accent + Wintage.theme pre-state restored on activation failure, DWM backup kept until manifest transition). P2 hunt A-P: B/C wrapped terminal + electron + totalcmd manifest commits (snapshot rollback + backup retention), G verified (FreeBuff baseline model), L verified (bounded polls only), O/P verified (recovery.json + owner marker consumed); no remaining provable findings. | verify: mandatory matrix (conhost manifest-remove failure keeps backup + themed state + retry succeeds; unowned browser stage never deleted; vscode repaint never overwrites pristine + revert restores original; saipenview themed-live never becomes pristine + unrelated selector survives; schema-invalid manifests rejected + preserved byte-exact; unknown future keys readable; atomic push fails before split publication); all T-187/189/190/191 suites green; full gate ALL PASSED.
- [x] T-191 [user audit pass] Wintage third-pass audit at c1535fc - DONE, shipped as v1.26.7 (bb1e776): per-target transactions (manifest precheck + commit-failure rollback), per-target mutex, FreeBuff EXE/fuse snapshot + baseline reconciliation/pruning, Electron repaint/revert transactions + fuse health, Terminal/Obsidian effective health, browser stage ownership, VSCode backup-as-revert, revert copy-over ordering, no-browser-reopen on reapply, GUI atomic paths, single release gate, atomic publish verify, paths.json schema. | verify: suites green at ship -- released v1.26.7 (bb1e776), full gate green [E-665/E-666]
- [x] T-190 [user post-T-189 audit] Wintage state/transaction audit at ac5a6cf (do not redo T-189). P0 FreeBuff: persistent pristine BASELINE snapshot for all Wintage-owned files OR LIFO transaction chain so a later sound-only Apply never shadows earlier recovery sources; handle app-update new-generation baseline (never baseline from a Wintage-patched file); FreeBuff target atomic as a whole (preflight BOTH layers before any mutation, restore EXACT pre-state on second-layer failure - never blind --revert on a failed repaint); Get-FreeBuffPatchArgs helper used identically for dry-run/apply/verify (missing configured sound fails closed); --status-json patch health (renderer/orchestrator/sound, desired-sound-state satisfied). P0 Electron: --dry-run must be ZERO-mutation (--revert --dry-run currently calls restoreFuseBackup before the dry-run branch); defuse() only AFTER classify+preflight+calculate (fuse belongs to the install transaction, auto-restore on later failure); installRelocation fully transactional (stage new appDir in temp sibling, preserve old until commit, restore ASAR/unpacked/old-appDir/fuse on ANY mutation failure - injected failures A-G); installInPlace transactional (calculate patched bytes + integrity header BEFORE opening ASAR, snapshot sidecars, restore byte-exact on injected failure). P1: delete duplicate Set/Remove-ManifestEntry pair + static FunctionDefinitionAst duplicate-name test; handle AbandonedMutexException as acquired-not-timeout + dispose on real failure + fail-closed corrupt-manifest-after-abandonment test; concurrency test captures child handles + bounded wait + exit-code assertions + barrier seam (no sleep roulette); Obsidian multi-item ownership (manifest `items:` array, canonicalized path SET comparison - no `;`-joined fake path), revert walks RECORDED owned vaults not today's discovery, missing recorded vault fails closed; Terminal same multi-item model + transactional multi-settings apply with rollback; Sync-SourceBackup provenance rebase (newPristine = live non-owned content + OLD pristine owned regions - never wholesale-copy a themed live file); deepen health probes (marker VALUE == manifest.palette, source-tree owned tokens == desired, generated CSS marker) with tamper-repair tests; revert strictness (Assert-RevertSource everywhere recovery required, deletion-only targets clean stale manifest only after proving no owned state); FreeBuff --verify/--status-json includes desired sound; `-Target all` build gate only fails PRESENT build-consuming targets (native/source-tree still execute, aggregate failures). P2: Write-Manifest tmp finally-cleanup (no .tmp-* garbage on failure); remove dead/unreachable Electron revert branches. | verify: regression matrix (FreeBuff sound-B-after-sound-A Apply then Revert restores all stock; upstream replaces one owned bundle then Reapply+Revert = consistent current-gen stock; FreeBuff second-layer-failure restores exact pre-state incl. palette-A-not-uninstalled; FreeBuff missing sound fails WhatIf+Apply with zero mutation; FreeBuff renderer/orchestrator/sound tamper each detected+repaired by Reapply; electron --revert --dry-run zero bytes changed + backup intact; ambiguous-state Apply with defuse needs exits nonzero + EXE intact + no fuse side effect; relocation injected failures A-G each exact pre-state; in-place injected failures exact pre-state; duplicate function names zero; abandoned mutex recovery; concurrent writers both exit 0 + both entries + no tmp garbage; Obsidian 2-vault Apply+Reapply no work scheduled + old-vault revert fails closed; Terminal 2-file rollback + recorded-second-item revert; source themed-live provenance rebase test; marker-tamper + smartvac-token-tamper + betterdiscord-css-tamper all repaired by Reapply; -Target all no-Node smartvac executes + present generated consumer fails; Write-Manifest failure leaves old manifest + no tmp garbage); all T-187/T-189 tests remain green; full Run-Tests + test-reapply + test-electron-state + test-freebuff + ownership/build/theme gates green; report confirmed-fixed/new-bugs/regressions-added/deferred
- [x] T-189 [user second-pass audit] Wintage correctness audit round 2 at 3c3daaa: close update/reapply/transaction/rollback holes (do not redo T-187). P0: Reapply must detect target staleness not just Wintage version (health decision = payload OR resolved-path OR appVersion OR marker/state OR unresolved; moved-target test must NOT fake payload 1.9.0); -Reapply -WhatIf must invoke child dry-run preflight (plannedWork vs mutatedWork, child failure propagates, no false "all up to date"); strict explicit-target semantics (bulk-all absent = SKIP, explicit/Reapply-recorded unresolved = FAIL preserving manifest, Revert-with-recovery-but-missing-source = FAIL, never-anything-owned Revert = NOOP not "restored"). P0 Electron state machine (install-electron.js): explicit state classification STOCK/THEMED_RELOCATED/UPDATED_RELOCATED/THEMED_INPLACE/UPDATED_INPLACE/AMBIGUOUS not from backup/marker alone; relocation update recovery preserves NEW root asar as rollback source (Revert restores v2 never v1); in-place UPDATED_INPLACE detects stock-vs-patched current asar, stale backup never rollback authority; --version works after relocation + --status-json machine-readable status; atomic relocation transaction with rollback on unpacked-move failure. P0 FreeBuff single transaction: top-level Revert undoes shim + ad/sound patch, manifest removed only after both, failures preserve evidence; missing mandatory helper = throw (never record FreeBuff without post-step); WhatIf validates both layers (dry-run both, stale matcher/missing sound/incompatible build = nonzero); patch-freebuff-ads preflight-first all-read-then-all-validate-then-one-transaction-backup-then-write-then-verify with rollback; one transaction backup dir with metadata, Revert rejects partial dirs. P1 rollback ownership: Terminal/OBS/Obsidian/TotalCmd revert only Wintage-owned fields merged into CURRENT config, unrelated post-Apply user edits survive (replace whole-file byte-exact invariants with ownership tests); Obsidian manifest mutation outside vault loop (all-or-nothing, canonical-path backup identity); SmartVac/WildRift backup provenance (pristine-vs-patched hashes, upstream v2 change survives repaint/revert, never rebuild from obsolete backup); Get-CssShape must normalize only Wintage-owned tokens; manifest concurrency (named lock over read-modify-write, unique temp per writer, parallel writers keep both entries); build verification contract target-aware (desktop/out consumers fail without Node unless -Force). P2: GUI status colour reset after failure; GUI listing parser preserves browser 'listing failed'; Get-ClaudeResources/Get-* version-dir TryParse malformed suffix. | verify: mandatory matrix (app update same payload triggers Reapply; moved target same payload triggers; Reapply WhatIf invokes child validation; WhatIf child failure nonzero; no false all-up-to-date; recorded missing target Reapply fails; -Target all absence SKIPs; Electron relocated v1+new v2 recovery; in-place stale-v1-backup+new-v2 recovery; Revert restores v2 never v1; --version after relocation; relocation unpacked-fail rolls back; FreeBuff missing helper hard-fails; FreeBuff WhatIf validates helper; FreeBuff dry-run stale matcher nonzero; FreeBuff cross-file preflight zero mutation on mismatch; FreeBuff transaction backup restores all; top-level FreeBuff Revert restores shim+patches+sound; Terminal/OBS/Obsidian/TotalCmd unrelated edit survives Revert; Obsidian second-vault failure does not advance/remove manifest; concurrent manifest writers keep both; source-tree upstream v2 survives repaint/revert); all T-187 regressions preserved; full repo gates green; no weakened checks; report confirmed-fixed/new-bugs/regressions-added/deferred
- [x] T-188 [eee collect] consume SAIT-007 (saitranslate): verify-only translation integration - payload already live byte-identical at cdbe140 (zero diffs across README x32 / desktop x32 / browser-theme x32 / locales), SAIT-007 marked reviewed; no repo mutation required; ship the collect | verify: --gate collect:saitranslate PASS on SAIT-007 (cdbe140/c66baf69/f241e6b8), byte-identity 0 diffs re-checked at collect time, SAIT-007 status reviewed, collect committed + pushed
- [x] T-187 [user audit pass] Wintage audit/fix: fix confirmed correctness/rollback/state bugs, then hunt adjacent untested failure paths. P0 exit-code/state propagation: install.ps1 -Reapply aggregates failures and exits nonzero when any attempted reapply fails (successful siblings preserved; Quiet never swallows failures; regression fixture one-success+one-fail => exit != 0); payloadVersion vs currentVer compared semantically via [version] (1.9.0 < 1.26.3, malformed/unknown => needs-reapply, never silently up-to-date); Electron dispatch propagates child failure (apply/revert/-WhatIf), manifest mutation only after confirmed success, Revert removes manifest only after confirmed revert; FreeBuff post-step order apply-shim -> patch-freebuff-ads -> Set-ManifestEntry so partial failure leaves target incomplete/not-current; GUI APPLY/REVERT captures $LASTEXITCODE per powershell child, logs PASS/FAIL per target, success only when all requested ops succeeded, never infer from last stdout; Save-Custom returns/throws explicit result and Apply aborts before install.ps1 when apply-themes/build-desktop failed (same fail-closed for Delete-Custom regeneration). P0 rollback/state integrity: SmartVac preserves first pre-Wintage backup across repaint (original->apply A->apply B->Revert byte-identical); regex patch verifies required anchor replacement count (zero matches => hard failure before manifest, verify output block, preserve non-colour shape); MPC-HC failed reg import => hard failure keeping manifest+backup, failed first backup/export aborts Apply before registry mutation; Read-Manifest treats malformed non-empty installed.json as distinct corrupt state (Status reports corruption, Set/Remove never overwrite corrupt manifest, atomic temp-write+readback replace). P1 tests: test-reapply implements the promised moved-target rediscovery fixture (install A, manifest A, app moves to B, reapply chooses B not stale manifest path) or deletes the false claim; test isolation allows app-data/manifest root override, all fixtures use unique temp dirs, never touches live %APPDATA%\Wintage\installed.json even transiently. P1 palette SSOT: GUI WCAG roles == build gate WCAG roles via one shared definition (add Vintage Classic regression fixture; borderHighlight decorative split never false-FAILs); ONE canonical 21-token schema/validator reused by apply-themes/check-css/WintageInstaller.ps1/build-desktop (tests must not use golden.json as schema authority; fix stale "18 token names" comment); theme identity validator rejects duplicate slug, duplicate user-facing label, missing slug, and themes/<filename>.json basename != pack.slug. P1 encoding: fix mojibake in tools/build-desktop.js comments, generated VS Code displayName, tools/install-electron.js comments; regenerate outputs; add source/generated-text gate for known mojibake signatures (РІР‚ РІвЂќ Гўв‚¬ etc) on text files only. P2: audit every Invoke-* handler for SKIP(nonfatal absence) vs FAIL(dependency/action failure) vs FAIL+recovery(partial mutation) contract, manifest only on success; dispatch consistency check: no target in >1 dispatch collection, exactly one handler owns each non-all target; extract only proven-drift shared invariants (token schema, theme identity validation, sorting, WCAG role definition) -- no giant refactor | verify: every regression gate fails on current main then passes after fix (STATE frontmatter parses; semver 1.9.0 vs 1.26.3; Reapply child failure bubbles to exit code; Electron apply/revert failure bubbles; GUI no success after failed child; Save-Custom generator failure aborts Apply; SmartVac A->B->revert byte-for-byte; zero-match source patch fails; MPC failed import keeps manifest; MPC failed initial backup blocks mutation; corrupt installed.json not overwritten; manifest atomic round-trip; real moved-target rediscovery; tests never touch live %APPDATA% manifest; GUI WCAG roles == build gate; duplicate slug/label rejected; filename/slug mismatch rejected; known mojibake absent; all target dispatch ownership exactly once); Run-Tests.ps1 + check-css + apply-themes --check + build-desktop --check + test-reapply + relevant electron/target gates all PASS; generated artifacts regenerated clean; `git diff` no accidental generated noise/mojibake; STATE/BOARD/LOG protocol-conformant and STATE YAML parses; final report lists fixed confirmed bugs, newly found bugs, tests added, deferred items with reason

## BLOCKED
- [ ] T-200 Audit handoff intake (Core 19 + Second Wave 11 + Performance 12 tickets) cannot start: the audit was written against an archive with 117 uncommitted changes (`Wintage_20-08-2026-T21-46-45(1).zip`, SHA256 63bbe278...fd91) whose PowerShell recovery layer is absent here -- no `.wintage-owner.json`, no `wintage.theme.css.pristine`, no `recovery.json`, no `epoch.txt`, no `Get-RecoveryEpoch`/`Write-Provenance`/`Test-ElectronRunning`, and every cited line number is 100-150 lines off in targets.ps1. The four untracked `tools/test-*-audit.*` harnesses are from that same tree and two of them fail here for exactly that reason. | blocker: the archive or its unpacked tree has to be supplied before any of the 42 tickets can be implemented against real evidence
- [ ] T-065 YouTube Studio analytics bars disappear under the theme. Exact class plus computed background-color/background-image is required before a narrow fix | blocker: analytics DOM requires the user's authenticated Studio; running Cent/Brave expose no CDP, and launching/copying the live profile would disturb or duplicate private session data | verify: bars visible again on the analytics table
